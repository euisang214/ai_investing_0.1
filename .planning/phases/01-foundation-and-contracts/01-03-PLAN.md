---
phase: 01-foundation-and-contracts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_investing/api/main.py
  - src/ai_investing/api/__init__.py
  - src/ai_investing/cli.py
  - README.md
  - docs/runbook.md
  - Dockerfile
  - docker-compose.yml
  - tests/test_api.py
  - tests/test_cli.py
autonomous: true
requirements:
  - API-01
  - API-02
  - OPS-01
  - COV-01
  - COV-02
user_setup: []
must_haves:
  truths:
    - "Operators can initialize the database, ingest sample data, manage coverage including `next_run_at`, run analysis/refresh flows, inspect memos/deltas, and mutate agent state from the CLI."
    - "FastAPI exposes the required coverage, company run, memo, delta, and agent management endpoints through an app-factory/lifespan startup path, including coverage disable/remove and agent reparent."
    - "Docker remains the primary local boot path, with a separate supported host workflow only when Python 3.11+ is present."
  artifacts:
    - path: src/ai_investing/api/main.py
      provides: FastAPI application factory and HTTP route surface for operator workflows
      min_lines: 80
    - path: src/ai_investing/cli.py
      provides: CLI commands for coverage, ingestion, analysis, memo, delta, and agent management
      min_lines: 90
    - path: tests/test_api.py
      provides: HTTP contract smoke coverage for the API surface
    - path: tests/test_cli.py
      provides: Command-level smoke coverage for the CLI surface
    - path: docs/runbook.md
      provides: Docker-first local workflow documentation with verified operator steps
      min_lines: 20
  key_links:
    - from: src/ai_investing/api/main.py
      to: src/ai_investing/application/context.py
      via: lifespan-managed app context
      pattern: "AppContext|lifespan|state\\.context"
    - from: src/ai_investing/cli.py
      to: src/ai_investing/application/services.py
      via: operator command delegation
      pattern: "CoverageService|AnalysisService|IngestionService|AgentConfigService"
    - from: README.md
      to: docs/runbook.md
      via: operator quick-start handoff
---

<objective>
Harden the operator-facing interface layer so the Phase 1 skeleton is actually usable from both CLI and API, with a clear local run path.

Purpose: A structurally sound backend needs more than internal services; operators must be able to drive coverage, ingestion, refreshes, memos, deltas, and agent configuration through stable interfaces and docs.
Output: App-factory FastAPI, fuller CLI/API management coverage, smoke tests, and an updated Docker-first runbook.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-VALIDATION.md
@README.md
@docs/runbook.md
@Dockerfile
@docker-compose.yml
@src/ai_investing/api/main.py
@src/ai_investing/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor FastAPI startup into an app factory with explicit lifecycle management</name>
  <files>src/ai_investing/api/main.py, src/ai_investing/api/__init__.py</files>
  <action>Move the API surface away from import-time global context initialization and into a `create_app(...)` pattern with lifespan-managed startup. Keep the route surface thin over existing services, and introduce a consistent success/error response posture where it materially improves operator predictability. Do not bury new business logic inside the HTTP layer.</action>
  <verify>pytest -q tests/test_api.py</verify>
  <done>The API can be instantiated cleanly in tests, initializes shared context at startup, and preserves the required endpoint set.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the CLI and API coverage-management surface</name>
  <files>src/ai_investing/cli.py, src/ai_investing/api/main.py, tests/test_cli.py, tests/test_api.py</files>
  <action>Add any missing operator commands and routes needed for Phase 1, especially coverage list/disable/remove paths, explicit `next_run_at` control, agent reparent support, and the expected coverage/company/memo/delta workflows. Validate that ingest routes do not silently ignore the URL `company_id`; either enforce agreement with the manifest/profile or fail clearly. Keep mutation scope intentionally narrow: coverage lifecycle plus `enable-agent`, `disable-agent`, and `reparent-agent`. Avoid inventing broad runtime-edit APIs that bypass the YAML contract model.</action>
  <verify>pytest -q tests/test_cli.py tests/test_api.py</verify>
  <done>CLI and API both expose the required operator workflows, including coverage lifecycle and agent reparent, and smoke tests lock down the intended command/route set.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh Docker-first operator docs and local boot guidance</name>
  <files>README.md, docs/runbook.md, Dockerfile, docker-compose.yml</files>
  <action>Update the quick start and runbook so Docker remains the primary path, while a host workflow is documented separately and only for Python 3.11+. Make the documented commands match the actual CLI/API surface, and tighten any container wiring needed to keep the local path trustworthy. Do not let host-specific assumptions replace the Docker-first posture.</action>
  <verify>pytest -q tests/test_cli.py tests/test_api.py</verify>
  <done>The repo documents one clear Docker-first path, a guarded host path, and the commands in the docs match the implemented interfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_api.py tests/test_cli.py`
- [ ] `ruff check src tests`
- [ ] Follow `docs/runbook.md` through one Docker-backed analysis flow
</verification>

<success_criteria>

- All tasks completed
- CLI and API cover the required Phase 1 operator workflows
- Coverage disable/remove behavior is reachable through an interface, not only internal services
- Runbook and quick start match the actual supported local path

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-03-SUMMARY.md`
</output>

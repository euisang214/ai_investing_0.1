---
phase: 13
plan: 01
status: ready
---

# Phase 13-01: Operator Documentation & Milestone Closeout

## Objective
Extend the README with production setup and test/prod toggling guides covering all 4 providers plus OpenAI-compatible endpoints. Produce a formal v2.0 milestone closeout artifact. Update the "Next Work" section and mark all v2.0 requirements as complete.

## 1. README — Production Setup Section
Add a new `## Production Setup` section after "Quick Start" with numbered steps:

1. **Choose a provider** — brief table of provider options (OpenAI, Anthropic, Google Gemini, Groq, OpenAI-compatible).
2. **Create API keys** — subsections for each provider with:
   - Account creation link (e.g., `https://platform.openai.com/signup`)
   - API key generation steps (e.g., "Navigate to API Keys → Create new secret key")
   - Dashboard link for managing keys
   - Required env var name (e.g., `OPENAI_API_KEY`)
3. **Configure environment** — show `.env` setup with `AI_INVESTING_PROVIDER`, API key vars, and `model_profiles.yaml` model name vars.
4. **Set up API authentication** — document `AI_INVESTING_API_KEYS` format with role examples (`operator`, `readonly`).
5. **Configure database** — document `AI_INVESTING_DATABASE_URL` for production Postgres.
6. **Deploy** — `docker compose --profile prod up --build -d` with required env vars listed.
7. **Configure cost controls** — document `AI_INVESTING_MAX_TOKENS_PER_RUN`.

## 2. README — Test vs Production Section
Add a `## Test vs Production` section showing the exact env var diff:

```
# Test (default — no API keys needed)
AI_INVESTING_PROVIDER=fake
AI_INVESTING_AUTH_ENABLED=false

# Production
AI_INVESTING_PROVIDER=openai        # or anthropic, google, groq, openai_compatible
AI_INVESTING_AUTH_ENABLED=true
AI_INVESTING_API_KEYS=sk-prod-abc:operator,sk-reader-xyz:readonly
AI_INVESTING_DATABASE_URL=postgresql+psycopg://user:password@host:5432/ai_investing
```

Include notes on `AI_INVESTING_ALLOW_FAKE_FALLBACK` behavior and `AI_INVESTING_LOG_LEVEL`.

## 3. README — Update Next Work Section
Replace the stale "Next Work" section with a brief statement that v2.0 is complete and a pointer to the closeout document.

## 4. Milestone Closeout Artifact
Create `.planning/v2.0-CLOSEOUT.md` covering:
- **Milestone summary**: what v2.0 achieved (productionization from dev-only to deployment-ready).
- **Phase-by-phase delivery**: one-line summary per phase (9 through 13).
- **Requirement traceability**: table mapping each v2.0 requirement ID to its delivering phase and final status (all marked complete).
- **Final system state**: what an operator can now do that they couldn't before v2.0.

## 5. Mark v2.0 Requirements Complete
Update `REQUIREMENTS.md` traceability table to mark all v2.0 requirements (`PROV-02` through `DOC-02`) as `Complete` with their delivering phases.

## Acceptance Criteria
- [ ] `PROV-04` satisfied: README documents API key creation for OpenAI, Anthropic, Google, Groq with vendor dashboard links.
- [ ] `DOC-01` satisfied: README includes numbered production deployment steps.
- [ ] `DOC-02` satisfied: README includes test vs production env var toggling guide.
- [ ] `.planning/v2.0-CLOSEOUT.md` exists with phase summaries and requirement traceability.
- [ ] `REQUIREMENTS.md` shows all v2.0 requirements as Complete.
- [ ] "Next Work" section in README is updated to reflect v2.0 completion.

---
phase: 01-foundation-and-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_investing/domain/models.py
  - src/ai_investing/persistence/tables.py
  - src/ai_investing/persistence/db.py
  - src/ai_investing/persistence/repositories.py
  - src/ai_investing/ingestion/file_connectors.py
  - src/ai_investing/application/services.py
  - tests/test_repository_semantics.py
  - tests/test_ingestion.py
  - tests/conftest.py
  - alembic.ini
  - alembic/env.py
  - alembic/versions/0001_phase1_baseline.py
autonomous: true
requirements:
  - COV-01
  - COV-02
  - ING-01
  - ING-02
  - MEM-01
  - MEM-02
user_setup: []
must_haves:
  truths:
    - "Coverage entries can be created, disabled, and removed without corrupting structured history or company memory."
    - "Public and private file-bundle ingestion persist immutable raw artifacts plus normalized evidence with provenance, quality, and staleness metadata."
    - "Claims, verdicts, memos, deltas, and tool logs remain structured records with non-destructive active/superseded semantics and a real migration path."
  artifacts:
    - path: src/ai_investing/domain/models.py
      provides: Typed contracts for coverage, evidence, claims, verdicts, memos, deltas, and tool logs
      min_lines: 180
    - path: src/ai_investing/persistence/tables.py
      provides: Projected persistence rows for structured investment memory
      min_lines: 120
    - path: src/ai_investing/persistence/repositories.py
      provides: Repository semantics for active reads, history preservation, and current memo retrieval
      min_lines: 250
    - path: src/ai_investing/ingestion/file_connectors.py
      provides: File-based ingestion path with raw landing-zone copies and normalized evidence creation
      min_lines: 70
    - path: alembic/versions/0001_phase1_baseline.py
      provides: Baseline schema migration for the current contract set
  key_links:
    - from: src/ai_investing/application/services.py
      to: src/ai_investing/persistence/repositories.py
      via: coverage, ingestion, and run orchestration services
      pattern: "Repository\\("
    - from: src/ai_investing/ingestion/file_connectors.py
      to: src/ai_investing/domain/models.py
      via: normalized evidence and company profile creation
      pattern: "EvidenceRecord|CompanyProfile"
    - from: src/ai_investing/persistence/db.py
      to: alembic/env.py
      via: migration-aware database initialization path
---

<objective>
Harden the structured persistence and ingestion backbone so Phase 1 has stable contracts, history semantics, and a safe schema evolution path.

Purpose: The project cannot scale panel logic or weekly reruns if coverage records, evidence, claims, memos, and deltas are only partially typed or depend on destructive persistence behavior.
Output: Hardened domain/persistence contracts, explicit coverage lifecycle semantics, ingestion guarantees, and a baseline Alembic path.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-VALIDATION.md
@src/ai_investing/domain/models.py
@src/ai_investing/persistence/tables.py
@src/ai_investing/persistence/db.py
@src/ai_investing/persistence/repositories.py
@src/ai_investing/ingestion/file_connectors.py
@src/ai_investing/application/services.py
@tests/test_repository_semantics.py
@tests/test_ingestion.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align typed memory contracts with projected persistence rows</name>
  <files>src/ai_investing/domain/models.py, src/ai_investing/persistence/tables.py, src/ai_investing/persistence/repositories.py</files>
  <action>Audit the current typed models and table projections against the phase requirements for evidence, claims, verdicts, memo sections, memo updates, memo snapshots, monitoring deltas, tool logs, and coverage records. Add missing query-critical columns or status-handling behavior where needed, but keep the canonical record content in structured payloads. Do not collapse the model into prose-first blobs or destructive overwrite updates.</action>
  <verify>pytest -q tests/test_repository_semantics.py tests/test_analysis_flow.py</verify>
  <done>Repository methods expose current-state reads cleanly while preserving superseded history for investment memory records.</done>
</task>

<task type="auto">
  <name>Task 2: Harden coverage lifecycle and file-bundle ingestion semantics</name>
  <files>src/ai_investing/ingestion/file_connectors.py, src/ai_investing/application/services.py, tests/test_ingestion.py, tests/conftest.py</files>
  <action>Make coverage creation, disable, and removal semantics explicit in the service layer, and ensure both public and private file-bundle ingestion preserve raw copies plus normalized evidence metadata that downstream agents rely on. Remove connector-config drift by consuming declared connector metadata such as `manifest_file` rather than hardcoding it, and keep namespace/provenance/staleness fields intact because reruns depend on them. Prefer clear typed service behavior over hidden side effects.</action>
  <verify>pytest -q tests/test_ingestion.py tests/test_repository_semantics.py</verify>
  <done>Coverage lifecycle behavior is testable, connector config is actually consumed, and ingestion records carry the metadata required for downstream claim and memo generation.</done>
</task>

<task type="auto">
  <name>Task 3: Add a baseline migration path for Phase 1 persistence</name>
  <files>src/ai_investing/persistence/db.py, alembic.ini, alembic/env.py, alembic/versions/0001_phase1_baseline.py</files>
  <action>Introduce an Alembic baseline for the current schema and adjust database initialization so local development can evolve the schema without relying exclusively on `metadata.create_all`. Keep the developer experience simple, but do not invent a custom migration system because Phase 1 is the right point to stop schema drift from becoming implicit.</action>
  <verify>pytest -q tests/test_repository_semantics.py tests/test_ingestion.py</verify>
  <done>Alembic can represent the current schema contract, and the repo has a documented, executable migration baseline for Phase 1 tables.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_repository_semantics.py tests/test_ingestion.py`
- [ ] `pytest -q tests/test_analysis_flow.py`
- [ ] `ruff check src tests`
</verification>

<success_criteria>

- All tasks completed
- Coverage, ingestion, and structured memory semantics are explicit and regression-tested
- Active/superseded behavior is preserved for claim and verdict history
- A baseline migration path exists for the Phase 1 schema

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-02-SUMMARY.md`
</output>

# n8n Integration

These workflows are examples of how n8n should call the FastAPI service. n8n stays outside the reasoning runtime and is responsible only for scheduling, webhook intake, and notifications.

Example flows:

- `weekly_watchlist_refresh.json`
- `weekly_portfolio_refresh.json`
- `new_evidence_webhook.json`
- `refresh_notification.json`


---
phase: 06-productionize-remaining-panels
plan: 06
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - README.md
  - docs/architecture.md
  - docs/runbook.md
  - docs/factor_ontology.md
  - scripts/generate_phase2_examples.py
  - examples/generated/README.md
  - examples/generated/ACME/initial/result.json
  - examples/generated/ACME/initial/memo.md
  - examples/generated/ACME/initial/delta.json
  - examples/generated/ACME/continued/result.json
  - examples/generated/ACME/continued/memo.md
  - examples/generated/ACME/continued/delta.json
  - examples/generated/ACME/rerun/result.json
  - examples/generated/ACME/rerun/memo.md
  - examples/generated/ACME/rerun/delta.json
  - tests/test_generated_examples.py
  - .planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - V2-01
requirement_scope: slice
requirement_slice_id: V2-01F
user_setup: []
must_haves:
  truths:
    - "Phase 6 documentation must describe the shipped runtime honestly: remaining panels are now productionized through config-driven rollout policies, but support and skip semantics still apply per run."
    - "Docs and generated artifacts must preserve analytical separation among company quality, expectations, `security_or_deal_overlay`, and `portfolio_fit_positioning`."
    - "Checked artifacts must demonstrate both complete and partial-support outcomes where relevant; examples may not imply that skipped overlays silently vanished or that unsupported contexts fail the whole run."
    - "Final verification must prove `V2-01` against the full phase outcome, not just individual plan slices, and must include docs, generated artifacts, tests, and lint."
    - "The closeout plan may summarize the new rollout policies, but it must not make `weekly_default` pretend that every future panel is always selected by default if the repo intentionally keeps a narrower operator default."
  artifacts:
    - path: README.md
      provides: Top-level truthful description of the now-productionized panel surface and rollout policies
      min_lines: 140
    - path: docs/architecture.md
      provides: Architecture explanation for shared runtime seams, support checks, and overlay separation
      min_lines: 280
    - path: docs/runbook.md
      provides: Operator guidance for choosing rollout policies, reading skipped panels, and interpreting partial recommendations
      min_lines: 140
    - path: docs/factor_ontology.md
      provides: Updated ontology narrative reflecting implemented status across all top-level panels
      min_lines: 120
    - path: examples/generated/README.md
      provides: Narrative guide to the final generated artifact set, including skip and rerun interpretation
      min_lines: 40
    - path: .planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md
      provides: Final phase verification artifact closing `V2-01` at the parent-requirement level
      min_lines: 120
  key_links:
    - from: README.md
      to: docs/runbook.md
      via: operator-facing explanation of rollout-policy choice and panel skip interpretation
      pattern: "policy|skip|weak confidence|overall recommendation"
    - from: docs/architecture.md
      to: docs/factor_ontology.md
      via: truthful analytical separation across panel families after productionization
      pattern: "company quality|expectations|security_or_deal_overlay|portfolio_fit_positioning"
    - from: scripts/generate_phase2_examples.py
      to: examples/generated/README.md
      via: reproducible checked artifacts that reflect the full Phase 6 runtime contract
      pattern: "initial|continued|rerun|skip|overlay"
    - from: tests/test_generated_examples.py
      to: examples/generated/ACME/rerun/result.json
      via: regression lock on the final generated artifact surface
      pattern: "panels|memo|delta|overall_recommendation"
    - from: .planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md
      to: .planning/REQUIREMENTS.md
      via: final parent-requirement evidence for `V2-01`
      pattern: "V2-01|traceability|verification"
---

<objective>
Close Phase 6 with truthful docs, reproducible checked artifacts, and final requirement-level verification.

Purpose: By this point the remaining panels should already be productionized. This plan updates the public repo story to match that reality, regenerates checked artifacts that demonstrate the new runtime contract, and writes the final verification artifact that closes `V2-01` at the phase level rather than leaving the evidence scattered across plan summaries.
Output: Updated docs, regenerated sample artifacts, final generated-example regressions, and a phase verification document that proves the full productionized panel surface.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01F`
Slice acceptance: Repo docs, checked artifacts, and phase verification all describe and demonstrate the complete Phase 6 contract truthfully, including rollout-policy choice, explicit skip or weak-confidence behavior, overlay separation, rerun deltas, and final requirement traceability for `V2-01`.
Deferred from parent requirement: None. This plan is the phase closeout for `V2-01`.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-productionize-remaining-panels/06-CONTEXT.md
@.planning/phases/06-productionize-remaining-panels/06-RESEARCH.md
@.planning/phases/06-productionize-remaining-panels/06-VALIDATION.md
@.planning/phases/06-productionize-remaining-panels/06-02-PLAN.md
@.planning/phases/06-productionize-remaining-panels/06-03-PLAN.md
@.planning/phases/06-productionize-remaining-panels/06-04-PLAN.md
@.planning/phases/06-productionize-remaining-panels/06-05-PLAN.md
@README.md
@docs/architecture.md
@docs/runbook.md
@docs/factor_ontology.md
@scripts/generate_phase2_examples.py
@examples/generated/README.md
@tests/test_generated_examples.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs around the finished Phase 6 runtime contract</name>
  <files>README.md, docs/architecture.md, docs/runbook.md, docs/factor_ontology.md</files>
  <action>Update the repo-facing docs so they describe the now-productionized panel surface truthfully. Explain the rollout policies, the shared runtime support-check contract, weak-confidence and explicit skip behavior, and the analytical separation among company quality, expectations, security or deal overlays, and portfolio fit. Keep the docs operator-usable: they should explain what a partial recommendation means and how to interpret skipped panels without forcing engineers to infer behavior from tests.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; files = [Path('README.md'), Path('docs/architecture.md'), Path('docs/runbook.md'), Path('docs/factor_ontology.md')]; text = '\\n'.join(path.read_text(encoding='utf-8').lower() for path in files); required = ['supply_product_operations', 'market_structure_growth', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning', 'skip', 'weak confidence', 'overall recommendation']; missing = [item for item in required if item not in text]; assert not missing, missing"</verify>
  <done>The docs describe the final Phase 6 runtime accurately, including the full panel surface and the truthful skip or weak-confidence contract.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate checked examples for initial, continued, rerun, and overlay-gap outcomes</name>
  <files>scripts/generate_phase2_examples.py, examples/generated/README.md, examples/generated/ACME/initial/result.json, examples/generated/ACME/initial/memo.md, examples/generated/ACME/initial/delta.json, examples/generated/ACME/continued/result.json, examples/generated/ACME/continued/memo.md, examples/generated/ACME/continued/delta.json, examples/generated/ACME/rerun/result.json, examples/generated/ACME/rerun/memo.md, examples/generated/ACME/rerun/delta.json, tests/test_generated_examples.py</files>
  <action>Extend the checked artifact generator and golden files so they demonstrate the finished Phase 6 runtime: a normal initial run, any continued or provisional path that still exists, a rerun with delta output, and at least one result where overlay support is absent or partial but company-quality analysis still completes honestly. Update the generated-example regressions so the checked artifacts remain synchronized with the shipped generator and wording contract.</action>
  <verify>docker compose run --rm api pytest -q tests/test_generated_examples.py</verify>
  <done>Checked artifacts now reflect the final Phase 6 runtime contract and are locked by reproducible regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Write the final phase verification artifact and close the parent requirement evidence chain</name>
  <files>.planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Produce a final verification artifact for Phase 6 that proves `V2-01` at the parent requirement level. Summarize each plan slice, list the implemented panels, cite the runnable policies, note the support or skip contract, and record the final automated verification commands and results expected for sign-off. Update planning references only as needed so `V2-01` can be traced to completed Phase 6 evidence rather than remaining implicitly open.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; text = Path('.planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md').read_text(encoding='utf-8').lower(); required = ['v2-01', 'supply_product_operations', 'market_structure_growth', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning', 'skip', 'overall recommendation']; missing = [item for item in required if item not in text]; assert not missing, missing"</verify>
  <done>The phase has one final verification artifact that closes the `V2-01` evidence chain cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api python -c "from pathlib import Path; files = [Path('README.md'), Path('docs/architecture.md'), Path('docs/runbook.md'), Path('docs/factor_ontology.md')]; text = '\\n'.join(path.read_text(encoding='utf-8').lower() for path in files); required = ['supply_product_operations', 'market_structure_growth', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning', 'skip', 'weak confidence', 'overall recommendation']; missing = [item for item in required if item not in text]; assert not missing, missing"`
- [ ] `docker compose run --rm api pytest -q tests/test_generated_examples.py`
- [ ] `docker compose run --rm api python -c "from pathlib import Path; text = Path('.planning/phases/06-productionize-remaining-panels/06-VERIFICATION.md').read_text(encoding='utf-8').lower(); required = ['v2-01', 'supply_product_operations', 'market_structure_growth', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning', 'skip', 'overall recommendation']; missing = [item for item in required if item not in text]; assert not missing, missing"`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] Docs do not imply that skipped overlays silently vanished or that unsupported contexts abort the whole run
- [ ] Checked examples include rerun delta behavior and at least one explicit partial-support outcome
- [ ] Final verification artifact closes `V2-01` at the parent requirement level instead of relying on implied completion from plan summaries
</verification>

<success_criteria>

- All tasks completed
- Repo docs describe the finished Phase 6 contract truthfully
- Checked generated artifacts cover complete, rerun, and partial-support outcomes
- Final verification evidence closes `V2-01`
- Phase 6 can be audited without reconstructing its evidence from scattered files

</success_criteria>

<output>
After completion, create `.planning/phases/06-productionize-remaining-panels/06-06-SUMMARY.md`
</output>

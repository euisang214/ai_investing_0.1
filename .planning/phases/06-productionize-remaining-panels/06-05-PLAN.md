---
phase: 06-productionize-remaining-panels
plan: 05
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - config/panels.yaml
  - config/factors.yaml
  - config/agents.yaml
  - config/run_policies.yaml
  - config/tool_bundles.yaml
  - config/tool_registry.yaml
  - src/ai_investing/application/portfolio.py
  - src/ai_investing/application/services.py
  - src/ai_investing/tools/builtins.py
  - src/ai_investing/tools/registry.py
  - prompts/panels/security_overlay/advocate.md
  - prompts/panels/security_overlay/skeptic.md
  - prompts/panels/security_overlay/durability.md
  - prompts/panels/security_overlay/judge.md
  - prompts/panels/security_overlay/panel_lead.md
  - prompts/panels/portfolio_fit/advocate.md
  - prompts/panels/portfolio_fit/skeptic.md
  - prompts/panels/portfolio_fit/durability.md
  - prompts/panels/portfolio_fit/judge.md
  - prompts/panels/portfolio_fit/panel_lead.md
  - prompts/ic/synthesizer.md
  - src/ai_investing/api/main.py
  - src/ai_investing/cli.py
  - tests/test_config_and_registry.py
  - tests/test_prompt_assets.py
  - tests/test_analysis_flow.py
  - tests/test_run_lifecycle.py
  - tests/test_monitoring_semantics.py
  - tests/test_api.py
  - tests/test_cli.py
autonomous: false
requirements:
  - V2-01
requirement_scope: slice
requirement_slice_id: V2-01E
user_setup: []
must_haves:
  truths:
    - "Overlays are the final analytical wave: `security_or_deal_overlay` and `portfolio_fit_positioning` must remain separate from company quality and from each other in config, prompts, runtime inputs, memo sections, and recommendation wording."
    - "`security_or_deal_overlay` requires security-specific or deal-specific context; generic company evidence alone is not enough to call the panel supported."
    - "`portfolio_fit_positioning` requires real portfolio or book context through a narrow reusable seam; this plan may not widen into bespoke orchestration or a general autonomous tool-calling loop."
    - "Unsupported overlays must skip explicitly without invalidating already-complete company-quality work, and `overall_recommendation` must state when the recommendation is company-quality-only because overlays were skipped or unsupported."
    - "Public and private support must stay truthful under the same panel ids, even if the security or deal inputs differ by company type."
  artifacts:
    - path: config/tool_registry.yaml
      provides: Narrow additive portfolio-context tool surface if the runtime needs one for `portfolio_fit_positioning`
      min_lines: 20
    - path: src/ai_investing/application/portfolio.py
      provides: Reusable portfolio summary or overlap context source suitable for bounded agent input
      min_lines: 360
    - path: src/ai_investing/tools/builtins.py
      provides: Least-privilege handler for any new portfolio-context tool
      min_lines: 180
    - path: prompts/panels/security_overlay/panel_lead.md
      provides: Production prompt contract for security or deal overlay synthesis
      min_lines: 20
    - path: prompts/panels/portfolio_fit/panel_lead.md
      provides: Production prompt contract for portfolio-fit and sizing synthesis
      min_lines: 20
    - path: prompts/ic/synthesizer.md
      provides: Final-wave wording for complete versus company-quality-only recommendations
      min_lines: 10
  key_links:
    - from: config/tool_registry.yaml
      to: src/ai_investing/tools/builtins.py
      via: bounded portfolio-context access instead of unrestricted runtime branching
      pattern: "portfolio|context|summary|overlap"
    - from: src/ai_investing/application/portfolio.py
      to: src/ai_investing/application/services.py
      via: reusable read-side portfolio inputs for `portfolio_fit_positioning`
      pattern: "summary|segment|overlap|risk"
    - from: config/panels.yaml
      to: prompts/ic/synthesizer.md
      via: overlay ownership reflected in final recommendation wording
      pattern: "security_or_deal_overlay|portfolio_fit_positioning|overall_recommendation"
    - from: prompts/panels/security_overlay/panel_lead.md
      to: tests/test_monitoring_semantics.py
      via: explicit overlay-supported versus overlay-skipped recommendation language
      pattern: "overlay|unsupported|company-quality-only"
    - from: tests/test_api.py
      to: tests/test_cli.py
      via: consistent operator surfacing for overlay skip reasons and recommendation posture
      pattern: "overlay|portfolio|recommendation|skip"
---

<objective>
Productionize the final overlay wave without collapsing company quality into security or portfolio-fit logic.

Purpose: The overlay panels are intentionally last because they require higher-context support than the rest of the memo. This plan adds truthful security or deal reasoning, introduces a narrow reusable portfolio-context seam for portfolio fit, and locks final recommendation wording so unsupported overlays remain explicit rather than quietly treated as completed.
Output: Implemented overlay panel config, bounded portfolio-context support, truthful overlay skip behavior, and interface coverage that distinguishes full recommendations from company-quality-only output before final checked-artifact regeneration.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01E`
Slice acceptance: `security_or_deal_overlay` and `portfolio_fit_positioning` run only when their support context exists, use a narrow reusable context seam where needed, surface explicit skip reasons when unsupported, and keep `overall_recommendation` honest about whether the final overlay view is complete.
Deferred from parent requirement: Repo-wide docs, checked artifacts, and final phase completion evidence land in the closeout plan.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
@/Users/ethanshin/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-productionize-remaining-panels/06-CONTEXT.md
@.planning/phases/06-productionize-remaining-panels/06-RESEARCH.md
@.planning/phases/06-productionize-remaining-panels/06-VALIDATION.md
@.planning/phases/06-productionize-remaining-panels/06-01-PLAN.md
@.planning/phases/06-productionize-remaining-panels/06-04-PLAN.md
@config/panels.yaml
@config/factors.yaml
@config/agents.yaml
@config/run_policies.yaml
@config/tool_bundles.yaml
@config/tool_registry.yaml
@src/ai_investing/application/portfolio.py
@src/ai_investing/application/services.py
@src/ai_investing/tools/builtins.py
@src/ai_investing/tools/registry.py
@prompts/panels/security_overlay/placeholder.md
@prompts/panels/portfolio_fit/placeholder.md
@prompts/ic/synthesizer.md
@tests/test_analysis_flow.py
@tests/test_run_lifecycle.py
@tests/test_monitoring_semantics.py
@tests/test_api.py
@tests/test_cli.py
@tests/test_generated_examples.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace overlay placeholders with production config and strict support rules</name>
  <files>config/panels.yaml, config/factors.yaml, config/agents.yaml, config/run_policies.yaml, config/tool_bundles.yaml, prompts/panels/security_overlay/, prompts/panels/portfolio_fit/, tests/test_config_and_registry.py, tests/test_prompt_assets.py</files>
  <action>Enable the two overlay panels only after the earlier waves are already productionized. Replace the placeholder leads with full agent trees, mark only the overlay factors implemented, and tighten the support rules so `security_or_deal_overlay` requires security-specific or deal-specific evidence while `portfolio_fit_positioning` requires real portfolio context. Add the final rollout policy that extends the earlier waves into true `full_surface` execution without erasing the ability to run narrower interim policies.</action>
  <verify>docker compose run --rm api pytest -q tests/test_config_and_registry.py tests/test_prompt_assets.py -k "overlay or portfolio"</verify>
  <done>Overlay config and prompt inventory are production-ready, and the final-wave policy exists without weakening the support rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add a narrow reusable portfolio-context seam and truthful overlay execution behavior</name>
  <files>config/tool_registry.yaml, src/ai_investing/application/portfolio.py, src/ai_investing/application/services.py, src/ai_investing/tools/builtins.py, src/ai_investing/tools/registry.py, tests/test_analysis_flow.py, tests/test_run_lifecycle.py, tests/test_monitoring_semantics.py</files>
  <action>Expose the minimum reusable portfolio input required for `portfolio_fit_positioning`, such as current book overlap, shared-risk clusters, segment exposure, or coverage-status context, through a bounded read-side seam or builtin tool. Reuse the shared runtime and support rules so supported overlay runs execute normally, security-specific or portfolio-specific gaps skip explicitly, and already-complete company-quality memo sections remain intact. Keep the implementation narrow and reusable; do not add bespoke overlay graph branches or unrestricted runtime access to the whole portfolio service.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_monitoring_semantics.py -k "overlay or portfolio or skip"</verify>
  <done>The overlays run only when their required context exists, and portfolio fit uses a narrow reusable context seam rather than bespoke orchestration.</done>
</task>

<task type="auto">
  <name>Task 3: Lock operator surfaces to the final recommendation contract before closeout artifacts regenerate</name>
  <files>prompts/ic/synthesizer.md, src/ai_investing/api/main.py, src/ai_investing/cli.py, tests/test_api.py, tests/test_cli.py</files>
  <action>Update interface regressions so operators can see whether a run ended with a complete overlay-aware recommendation or a company-quality-only recommendation because one or both overlays were skipped. Preserve stable entrypoints, but make the overlay support state explicit in result payloads and operator-visible wording. Leave golden-file regeneration and checked-example ownership to the closeout plan.</action>
  <verify>docker compose run --rm api pytest -q tests/test_api.py tests/test_cli.py -k "overlay or portfolio or recommendation"</verify>
  <done>Operator-facing API and CLI surfaces distinguish full overlay completion from company-quality-only recommendations, and checked-artifact regeneration remains scoped to closeout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Final-wave overlay panels, bounded portfolio-context support, and explicit recommendation wording for overlay-supported versus overlay-skipped runs.</what-built>
  <how-to-verify>Review the overlay prompts, the portfolio-context seam, and at least one final-wave generated result. Confirm that `security_or_deal_overlay` and `portfolio_fit_positioning` remain separate from company quality, that the portfolio context surface is narrow and genuinely book-aware, and that `overall_recommendation` clearly states when the output is only a company-quality recommendation because one or both overlays were skipped or unsupported.</how-to-verify>
  <resume-signal>Type "approved" or describe the overlay-separation or portfolio-context issue that still needs to change.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_config_and_registry.py tests/test_prompt_assets.py -k "overlay or portfolio"`
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_monitoring_semantics.py -k "overlay or portfolio or skip"`
- [ ] `docker compose run --rm api pytest -q tests/test_api.py tests/test_cli.py -k "overlay or portfolio or recommendation"`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] `security_or_deal_overlay` skips when security or deal context is absent instead of inferring support from generic company evidence
- [ ] `portfolio_fit_positioning` uses a narrow reusable portfolio-context seam rather than bespoke orchestration or unrestricted tool access
- [ ] `overall_recommendation` stays explicit about full versus company-quality-only completion when overlays are skipped
- [ ] Earlier company-quality memo sections remain intact when overlays are unsupported
</verification>

<success_criteria>

- All tasks completed
- The final analytical wave remains overlay-only and truthfully separated
- Overlay support rules are strict and explicit
- Portfolio fit uses a reusable narrow context seam
- Operators can distinguish full recommendations from company-quality-only output before closeout regenerates the checked examples
- Blocking human review signs off on recommendation wording and portfolio-context boundaries

</success_criteria>

<output>
After completion, create `.planning/phases/06-productionize-remaining-panels/06-05-SUMMARY.md`
</output>

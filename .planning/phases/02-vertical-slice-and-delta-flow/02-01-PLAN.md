---
phase: 02-vertical-slice-and-delta-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/ai_investing/settings.py
  - src/ai_investing/domain/enums.py
  - src/ai_investing/domain/models.py
  - src/ai_investing/persistence/tables.py
  - src/ai_investing/persistence/repositories.py
  - src/ai_investing/graphs/checkpointing.py
  - src/ai_investing/graphs/state.py
  - src/ai_investing/graphs/company_refresh.py
  - src/ai_investing/graphs/subgraphs.py
  - src/ai_investing/application/services.py
  - tests/test_run_lifecycle.py
  - alembic/versions/0002_phase2_checkpoint_runtime.py
autonomous: true
requirements:
  - COV-03
  - MEM-03
  - ORCH-02
  - ORCH-03
user_setup: []
must_haves:
  truths:
    - "A company refresh can pause after `gatekeepers`, persist a queryable checkpoint-aware run state, and resume later through the same `run_id` instead of spawning a second run."
    - "Gatekeeper continuation and failed-gate override are implemented as graph-native checkpoint actions, and downstream provisional analysis stays typed and operator-visible."
    - "Due coverage execution skips disabled entries and does not silently advance cadence timestamps for runs that are still awaiting a continue or stop decision."
    - "Direct panel execution cannot bypass the mandatory gatekeeper checkpoint: `run_panel` either starts at `gatekeepers` or resumes from an existing checkpoint-aware run, and unsupported downstream shortcuts fail clearly."
  artifacts:
    - path: pyproject.toml
      provides: Durable execution dependency contract for the checkpoint-aware LangGraph runtime
    - path: src/ai_investing/domain/models.py
      provides: Typed run lifecycle, checkpoint result, and provisional-state contracts
      min_lines: 260
    - path: src/ai_investing/persistence/repositories.py
      provides: Repository semantics for run lookup, checkpoint-aware status persistence, and memo-history-safe reads
      min_lines: 360
    - path: src/ai_investing/graphs/company_refresh.py
      provides: Checkpointer-aware top-level company refresh graph compilation and invoke/resume path
      min_lines: 55
    - path: src/ai_investing/graphs/subgraphs.py
      provides: Reusable gatekeeper checkpoint routing for stop, continue, and provisional continue actions
      min_lines: 120
    - path: tests/test_run_lifecycle.py
      provides: Regression coverage for paused run persistence, resume behavior, provisional override behavior, and due-coverage scheduling semantics
      min_lines: 70
    - path: alembic/versions/0002_phase2_checkpoint_runtime.py
      provides: Phase 2 migration for any newly projected checkpoint and run-lifecycle columns
  key_links:
    - from: src/ai_investing/application/services.py
      to: src/ai_investing/graphs/company_refresh.py
      via: start and resume execution using stable `run_id` thread identity
      pattern: "build_company_refresh_graph|thread_id|continue_run"
    - from: src/ai_investing/graphs/company_refresh.py
      to: src/ai_investing/graphs/subgraphs.py
      via: gatekeeper checkpoint routing and resume command flow
      pattern: "build_gatekeeper_checkpoint|Command|interrupt"
    - from: src/ai_investing/persistence/repositories.py
      to: src/ai_investing/domain/models.py
      via: typed lifecycle persistence rather than opaque metadata-only state
      pattern: "RunRecord|RunStatus|gate_decision|awaiting_continue"
---

<objective>
Establish the checkpoint-aware vertical-slice runtime so the system can pause after `gatekeepers`, resume safely, and preserve memo history plus run state without bespoke orchestration.

Purpose: Phase 2 cannot prove the architecture if pause/continue behavior lives in ad hoc CLI branching or if reruns lose the thread identity needed for durable execution.
Output: First-class run lifecycle contracts, checkpoint-aware graph composition, and service flows that can start, pause, resume, and fail cleanly.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-CONTEXT.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-RESEARCH.md
@pyproject.toml
@src/ai_investing/settings.py
@src/ai_investing/domain/enums.py
@src/ai_investing/domain/models.py
@src/ai_investing/persistence/tables.py
@src/ai_investing/persistence/repositories.py
@src/ai_investing/graphs/state.py
@src/ai_investing/graphs/company_refresh.py
@src/ai_investing/graphs/subgraphs.py
@src/ai_investing/application/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add first-class checkpoint and run-lifecycle contracts</name>
  <files>src/ai_investing/domain/enums.py, src/ai_investing/domain/models.py, src/ai_investing/persistence/tables.py, src/ai_investing/persistence/repositories.py, alembic/versions/0002_phase2_checkpoint_runtime.py</files>
  <action>Extend the typed runtime contract so paused, gated, stopped-after-panel, and provisional outcomes are explicit and queryable. Prefer dedicated enum values and typed fields over free-form metadata strings. Keep the business-memory tables append-only where possible, and add only the projected columns needed for reliable querying and sorting. If new lifecycle details still need a JSON payload copy, mirror them there after the typed fields exist rather than relying on payload inspection as the primary query path.</action>
  <verify>pytest -q tests/test_run_lifecycle.py</verify>
  <done>Run records can represent awaiting-continue, gated-out, stopped, provisional, completed, and failed outcomes without ambiguity, and repository lookups can fetch a run by `run_id` for resume flows.</done>
</task>

<task type="auto">
  <name>Task 2: Compile the company refresh graph with durable checkpoint support</name>
  <files>pyproject.toml, src/ai_investing/settings.py, src/ai_investing/graphs/checkpointing.py, src/ai_investing/graphs/state.py, src/ai_investing/graphs/company_refresh.py, src/ai_investing/graphs/subgraphs.py</files>
  <action>Introduce an injectable checkpointing boundary that uses a durable Postgres-backed LangGraph saver in real environments and a test-safe fallback in deterministic test runs. Insert a dedicated gatekeeper checkpoint node after the gatekeeper memo update and before downstream panels. Use `interrupt()` and `Command(resume=...)` so the graph itself owns the pause and route selection. Keep the rest of the panel topology config-driven and do not spread special-case `gatekeepers` branching into unrelated service code.</action>
  <verify>pytest -q tests/test_run_lifecycle.py</verify>
  <done>A run can pause after gatekeepers, resume later through the same thread, and route to stop, continue, or provisional continue without forking a second orchestration path.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor service execution into start, resume, and due-coverage flows</name>
  <files>src/ai_investing/application/services.py, src/ai_investing/persistence/repositories.py, tests/test_run_lifecycle.py</files>
  <action>Split the current one-shot company execution flow into explicit start and resume operations with smaller durable write boundaries around run creation, gatekeeper persistence, partial memo writes, checkpoint updates, and terminal completion. Keep `run_due_coverage()` aligned with the checkpoint model: disabled entries must be skipped, paused runs must stay queryable rather than being treated as failures, and cadence timestamps should only advance on intended terminal outcomes. Preserve backward compatibility where reasonable by keeping `analyze_company`, `refresh_company`, and `run_panel` as the public service entrypoints, but have them delegate to the new checkpoint-aware internals. `run_panel` must not bypass the mandatory gatekeeper checkpoint: allow it only for `gatekeepers` starts or for explicit resume flows that continue an existing run, and fail clearly for unsupported direct downstream-panel execution.</action>
  <verify>pytest -q tests/test_run_lifecycle.py</verify>
  <done>Service-layer analyze and refresh flows can start a paused run, resume it later, keep schedule semantics correct for disabled, paused, completed, and failed states, and prevent `run_panel` from bypassing the mandatory gatekeeper checkpoint.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_run_lifecycle.py`
- [ ] `pytest -q tests/test_repository_semantics.py`
- [ ] `pytest -q tests/test_cli.py tests/test_api.py -k "run_panel or continue"`
- [ ] `ruff check src tests`
- [ ] One fake-provider run pauses after `gatekeepers` and resumes later through the same `run_id`
</verification>

<success_criteria>

- All tasks completed
- Pause, stop, continue, and provisional-continue are graph-native and persisted cleanly
- Run lifecycle state is queryable without decoding opaque metadata
- Due coverage handling remains correct for disabled and checkpointed entries

</success_criteria>

<output>
After completion, create `.planning/phases/02-vertical-slice-and-delta-flow/02-01-SUMMARY.md`
</output>

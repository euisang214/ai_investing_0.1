---
phase: 02-vertical-slice-and-delta-flow
plan: 05
type: execute
gap_closure: true
wave: 4
depends_on:
  - 02-02
  - 02-04
files_modified:
  - src/ai_investing/application/services.py
  - src/ai_investing/persistence/repositories.py
  - tests/test_analysis_flow.py
  - tests/test_monitoring_semantics.py
  - tests/test_generated_examples.py
  - tests/test_cli.py
  - tests/test_api.py
  - examples/generated/README.md
  - examples/generated/ACME/continued/result.json
  - examples/generated/ACME/continued/memo.md
  - examples/generated/ACME/continued/delta.json
  - examples/generated/ACME/rerun/result.json
  - examples/generated/ACME/rerun/memo.md
  - examples/generated/ACME/rerun/delta.json
  - .planning/ROADMAP.md
  - .planning/phases/02-vertical-slice-and-delta-flow/02-VERIFICATION.md
autonomous: true
requirements:
  - ORCH-03
  - MEMO-01
  - MEMO-03
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "When a run has no prior active baseline, resume treats null or empty baseline metadata as an explicit no-baseline state rather than falling back to the same run's partial memo, claims, or verdicts."
    - "The first completed `analyze -> continue` run emits an initial-coverage delta with `prior_run_id` unset, keeps `memo.is_initial_coverage` true, and preserves same-run untouched placeholder sections as `not_advanced` instead of relabeling them stale."
    - "Only true reruns against a prior active memo may mark carried-forward content stale or compare against prior claims and verdicts for monitoring drift."
    - "Regression tests and checked-in ACME artifacts make the corrected first-completion story inspectable and prevent the same-run baseline bug from returning silently."
  artifacts:
    - path: src/ai_investing/application/services.py
      provides: Baseline reconstruction and memo projection logic that distinguish explicit no-baseline metadata and same-run placeholder sections from a real prior active memo on resume
      min_lines: 420
    - path: src/ai_investing/persistence/repositories.py
      provides: Historical baseline lookup that can exclude the current run when legacy paused runs lack baseline metadata
      min_lines: 200
    - path: tests/test_analysis_flow.py
      provides: End-to-end regression that a continued first run is not treated as a rerun
      min_lines: 90
    - path: tests/test_monitoring_semantics.py
      provides: Memo-status regression coverage for first completion versus real rerun stale carry-forward
      min_lines: 240
    - path: tests/test_generated_examples.py
      provides: Artifact-level assertions for null prior baseline and non-stale first-completion output
      min_lines: 90
    - path: tests/test_cli.py
      provides: Command-level continue-flow coverage that reflects corrected first-completion semantics
      min_lines: 60
    - path: tests/test_api.py
      provides: HTTP continue-flow coverage that reflects corrected first-completion semantics
      min_lines: 120
    - path: examples/generated/ACME/continued/result.json
      provides: Inspectable continued-run payload with `memo.is_initial_coverage` true and no prior active baseline snapshot
    - path: examples/generated/ACME/continued/delta.json
      provides: Inspectable first-completion delta without a prior active run id
    - path: examples/generated/ACME/continued/memo.md
      provides: Inspectable first-completion memo where untouched sections remain not advanced
  key_links:
    - from: src/ai_investing/application/services.py
      to: tests/test_analysis_flow.py
      via: analyze and continue regression for same-run baseline fallback
      pattern: "continue_run|prior_run_id|baseline"
    - from: src/ai_investing/persistence/repositories.py
      to: src/ai_investing/application/services.py
      via: baseline recovery that excludes the paused run's own active records when metadata is absent
      pattern: "run_id|active_only|baseline"
    - from: src/ai_investing/application/services.py
      to: tests/test_monitoring_semantics.py
      via: `not_advanced` versus `stale` projection after initial completion and real rerun
      pattern: "MemoSectionStatus|not_advanced|stale"
    - from: tests/test_generated_examples.py
      to: examples/generated/ACME/continued/delta.json
      via: checked-in artifact parity for corrected first-completion semantics
      pattern: "prior_run_id|null|continued"
---

<objective>
Repair the resumed first-completion baseline logic so the initial `analyze -> continue` path stays distinct from a true rerun in both structured outputs and rendered memo artifacts.

Purpose: Phase 2 is not proven until the first completed run can finish without inventing a prior active memo, prior claims, or prior verdicts from the same run's partial state.
Output: Targeted baseline-selection fix, regression coverage that would have caught the bug, and refreshed ACME artifacts that tell the correct first-completion story.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-CONTEXT.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-RESEARCH.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-VERIFICATION.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-03-PLAN.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-04-PLAN.md
@src/ai_investing/application/services.py
@scripts/generate_phase2_examples.py
@tests/test_analysis_flow.py
@tests/test_monitoring_semantics.py
@tests/test_generated_examples.py
@examples/generated/README.md
@examples/generated/ACME/continued/result.json
@examples/generated/ACME/continued/memo.md
@examples/generated/ACME/continued/delta.json
@examples/generated/ACME/rerun/result.json
@examples/generated/ACME/rerun/memo.md
@examples/generated/ACME/rerun/delta.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix baseline reconstruction for resumed first-completion runs</name>
  <files>src/ai_investing/application/services.py, src/ai_investing/persistence/repositories.py</files>
  <action>Update `_baseline_memo_from_run()`, `_baseline_claims_from_run()`, and `_baseline_verdicts_from_run()` so they do not use truthiness to decide whether a baseline exists. Treat explicit `null` and empty baseline snapshots stored on the run as a real "no prior active baseline" signal. For metadata-absent legacy resumes, recover history through repository helpers that explicitly exclude `run.run_id`; do not read the current active state blindly, because the paused run may already have promoted its own partial artifacts to active. Also adjust resumed memo projection so the same run's saved gatekeeper-only placeholder sections are not mistaken for carried-forward prior-memo sections on continue; untouched same-run placeholders must remain `not_advanced`, while only true prior-run carry-forward remains `stale`. Keep the fix narrow to baseline selection and same-run memo projection behavior; do not change the broader checkpoint flow or true rerun semantics.</action>
  <verify>pytest -q tests/test_analysis_flow.py tests/test_monitoring_semantics.py -k "continue or memo"</verify>
  <done>The first completed `analyze -> continue` run no longer self-baselines, `prior_run_id` stays unset for that completion, `memo.is_initial_coverage` stays true, and untouched same-run placeholder sections remain `not_advanced` unless a real prior active memo exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for first completion versus real rerun behavior</name>
  <files>tests/test_analysis_flow.py, tests/test_monitoring_semantics.py, tests/test_generated_examples.py, tests/test_cli.py, tests/test_api.py</files>
  <action>Add focused assertions that would fail if the same run's partial memo, claims, or verdicts ever become the baseline again. Cover the first completed run's delta payload, `memo.is_initial_coverage`, the memo-status posture of untouched sections after `continue_run()`, and the contrast with a later true rerun where stale carry-forward is expected. Include a regression for metadata-absent legacy paused runs that proves baseline recovery excludes the paused run's own active records. At the artifact layer, assert that the continued example preserves null or empty stored baseline snapshots and still renders as initial coverage. Extend or tighten the existing CLI and API continue-flow tests so the public entrypoints still expose the corrected first-completion semantics. Keep the tests behavior-oriented and deterministic rather than asserting on private helper internals.</action>
  <verify>pytest -q tests/test_analysis_flow.py tests/test_monitoring_semantics.py tests/test_generated_examples.py tests/test_cli.py tests/test_api.py</verify>
  <done>The regression suite clearly distinguishes initial completion from rerun behavior, locks the public `memo.is_initial_coverage` contract, covers the legacy metadata-absent resume path, and proves the CLI and API continue flows still reflect the repaired semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate and refresh the checked-in ACME examples</name>
  <files>examples/generated/README.md, examples/generated/ACME/continued/result.json, examples/generated/ACME/continued/memo.md, examples/generated/ACME/continued/delta.json, examples/generated/ACME/rerun/result.json, examples/generated/ACME/rerun/memo.md, examples/generated/ACME/rerun/delta.json</files>
  <action>Regenerate the checked-in examples through `scripts/generate_phase2_examples.py` so `continued/` reads as the first completed coverage run, not a rerun. Refresh any README wording needed to make that story explicit, and keep `rerun/` as the only place where stale carry-forward and a populated `prior_run_id` appear. Do not hand-edit JSON or memo files in ways the generator cannot reproduce.</action>
  <verify>pytest -q tests/test_generated_examples.py</verify>
  <done>The checked-in ACME artifacts match the generator output and visibly demonstrate corrected first-completion semantics for the continued run.</done>
</task>

<task type="auto">
  <name>Task 4: Reconcile planning-status artifacts after the repair passes</name>
  <files>.planning/ROADMAP.md, .planning/phases/02-vertical-slice-and-delta-flow/02-VERIFICATION.md</files>
  <action>Once the repair, tests, and regenerated artifacts all pass, update the Phase 2 planning-status documents so they no longer claim the phase is simultaneously complete and still carrying the known baseline gap. Keep the verification record factual about what changed, and only mark the roadmap fully clean once the repair verification is actually green.</action>
  <verify>python -c "from pathlib import Path; roadmap = Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); verification = Path('.planning/phases/02-vertical-slice-and-delta-flow/02-VERIFICATION.md').read_text(encoding='utf-8'); assert 'status: passed' in verification and 'gaps: []' in verification and 'status: gaps_found' not in verification; assert 'Plan progress: `5 / 5` completed' in roadmap and '02-05' in roadmap"</verify>
  <done>The roadmap and verification docs tell a consistent post-repair story for Phase 2.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_monitoring_semantics.py tests/test_generated_examples.py tests/test_cli.py tests/test_api.py`
- [ ] `docker compose run --rm api ruff check src tests`
- [ ] `docker compose run --rm api python scripts/generate_phase2_examples.py`
- [ ] `examples/generated/ACME/continued/delta.json` shows no prior active run id, and `examples/generated/ACME/continued/memo.md` leaves never-advanced sections out of `stale`
- [ ] `.planning/ROADMAP.md` and `02-VERIFICATION.md` agree on whether Phase 2 still has open gaps
</verification>

<success_criteria>

- All tasks completed
- Resumed initial coverage completion no longer reuses the same run's partial state as its prior baseline
- First-completion memo and delta outputs preserve `not_advanced` posture, `memo.is_initial_coverage`, and initial-coverage delta semantics
- True reruns still compare against the prior active memo and may legitimately mark carried-forward sections as stale
- Checked-in examples and parity tests reflect the repaired story
- Planning-status docs reflect the verified post-repair outcome consistently

</success_criteria>

<output>
After completion, create `.planning/phases/02-vertical-slice-and-delta-flow/02-05-SUMMARY.md`
</output>

---
phase: 06-productionize-remaining-panels
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - config/panels.yaml
  - config/run_policies.yaml
  - src/ai_investing/config/models.py
  - src/ai_investing/config/loader.py
  - src/ai_investing/domain/models.py
  - src/ai_investing/domain/read_models.py
  - src/ai_investing/application/context.py
  - src/ai_investing/application/services.py
  - src/ai_investing/graphs/subgraphs.py
  - src/ai_investing/api/main.py
  - src/ai_investing/cli.py
  - prompts/memo_updates/section_update.md
  - prompts/ic/synthesizer.md
  - tests/test_config_and_registry.py
  - tests/test_analysis_flow.py
  - tests/test_run_lifecycle.py
  - tests/test_monitoring_semantics.py
  - tests/test_api.py
  - tests/test_cli.py
autonomous: true
requirements:
  - V2-01
requirement_scope: slice
requirement_slice_id: V2-01A
user_setup: []
must_haves:
  truths:
    - "Phase 6 rollout stays config-driven: wave policies, panel readiness rules, and per-run support checks must come from registry data rather than new panel-specific orchestration branches."
    - "The only hard runtime implementation gate remains `implemented`; once a panel is productionized, unsupported run context must yield an explicit structured skip or weak-confidence outcome instead of a hidden omission or a whole-run rejection."
    - "Company-quality conclusions, `security_or_deal_overlay`, and `portfolio_fit_positioning` remain analytically separate in runtime results, memo projection, and `overall_recommendation` wording."
    - "Agent `input_channels` and lead prompts become real shared runtime seams for every non-gatekeeper panel; Phase 6 may not rely on prompt files that the runtime still ignores."
    - "Memo updates, partial memo projection, and rerun delta behavior stay intact: `what_changed_since_last_run` remains written on reruns, and overlay absence must not masquerade as a fully complete IC call."
  artifacts:
    - path: config/panels.yaml
      provides: Additive panel readiness and support-rule contract for truthful per-run execution
      min_lines: 140
    - path: config/run_policies.yaml
      provides: Wave-specific rollout policies between `weekly_default` and final `full_surface`
      min_lines: 30
    - path: src/ai_investing/config/models.py
      provides: Typed support-rule, skip-result, and rollout-policy models
      min_lines: 360
    - path: src/ai_investing/application/services.py
      provides: Generic support evaluation, input-channel-aware execution, real lead execution, and structured skip surfaces
      min_lines: 1550
    - path: src/ai_investing/domain/read_models.py
      provides: Typed panel execution or skipped-panel read surfaces for service, CLI, and API results
      min_lines: 180
    - path: prompts/ic/synthesizer.md
      provides: Overlay-aware `overall_recommendation` guidance for partial or unsupported overlay runs
      min_lines: 10
  key_links:
    - from: config/panels.yaml
      to: src/ai_investing/config/models.py
      via: additive per-panel readiness and support configuration
      pattern: "implemented|support|required_context|weak_confidence"
    - from: config/run_policies.yaml
      to: src/ai_investing/application/services.py
      via: wave rollout selection without orchestration branching
      pattern: "default_panel_ids|full_surface|wave"
    - from: src/ai_investing/application/services.py
      to: src/ai_investing/graphs/subgraphs.py
      via: shared pre-panel support checks and lead execution across every debate panel
      pattern: "execute_panel|finalize_panel_verdict|input_channels|skip"
    - from: src/ai_investing/domain/read_models.py
      to: src/ai_investing/api/main.py
      via: stable structured skip surfaces for operators
      pattern: "skipped|unsupported|reason|evidence_summary"
    - from: prompts/memo_updates/section_update.md
      to: prompts/ic/synthesizer.md
      via: truthful partial-recommendation wording for skipped or weak-confidence panels
      pattern: "overall_recommendation|overlay|weak confidence|unsupported"
---

<objective>
Create the Phase 6 foundation contract before any scaffold panel is flipped on.

Purpose: The repo already has a reusable graph shape, but it still treats production readiness as a binary `implemented` flag and ignores most agent channel declarations. This plan adds the rollout contract that lets newly productionized panels run honestly: wave-specific policies, additive per-panel support rules, explicit skip surfaces, real lead execution, and overlay-aware memo wording.
Output: Typed rollout config, shared runtime support evaluation, structured skipped-panel results, truthful memo or IC wording, and regressions that lock the new behavior before any remaining panel family is enabled.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01A`
Slice acceptance: Engineers can select rollout policies narrower than `full_surface`, declare support rules in config, run implemented panels through shared input-channel-aware execution, surface unsupported panels explicitly without aborting the run, and keep memo plus rerun-delta behavior truthful when overlays are absent.
Deferred from parent requirement: Actual prompt, fixture, and agent productionization for the remaining panel families lands in later Phase 6 plans.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-productionize-remaining-panels/06-CONTEXT.md
@.planning/phases/06-productionize-remaining-panels/06-RESEARCH.md
@.planning/phases/06-productionize-remaining-panels/06-VALIDATION.md
@config/panels.yaml
@config/run_policies.yaml
@config/agents.yaml
@src/ai_investing/config/models.py
@src/ai_investing/config/loader.py
@src/ai_investing/application/context.py
@src/ai_investing/application/services.py
@src/ai_investing/graphs/subgraphs.py
@src/ai_investing/api/main.py
@src/ai_investing/cli.py
@prompts/memo_updates/section_update.md
@prompts/ic/synthesizer.md
@tests/test_config_and_registry.py
@tests/test_analysis_flow.py
@tests/test_run_lifecycle.py
@tests/test_monitoring_semantics.py
@tests/test_api.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rollout policies and additive panel support configuration</name>
  <files>config/panels.yaml, config/run_policies.yaml, src/ai_investing/config/models.py, src/ai_investing/config/loader.py, tests/test_config_and_registry.py</files>
  <action>Extend the config contract so Phase 6 rollout is policy-driven instead of branch-driven. Add intermediate run policies for the internal company-quality wave, external company-quality wave, expectations wave, and final overlay-inclusive surface. In `PanelConfig`, add typed support and readiness fields that cover company-type requirements, required evidence families, minimum factor-coverage thresholds, thin-evidence weak-confidence posture, and special-context requirements for overlays. Keep this additive: `implemented` still means globally shipped, while support rules decide whether a given run executes, weakens, or skips an implemented panel.</action>
  <verify>docker compose run --rm api pytest -q tests/test_config_and_registry.py -k "panel or policy or implemented"</verify>
  <done>The registry can load wave-specific rollout policies plus typed panel support rules, and invalid readiness or support config fails before runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Make panel execution honor shared input channels, real lead prompts, and explicit skip results</name>
  <files>src/ai_investing/domain/models.py, src/ai_investing/domain/read_models.py, src/ai_investing/application/context.py, src/ai_investing/application/services.py, src/ai_investing/graphs/subgraphs.py, src/ai_investing/api/main.py, src/ai_investing/cli.py, tests/test_analysis_flow.py, tests/test_run_lifecycle.py, tests/test_api.py, tests/test_cli.py</files>
  <action>Refactor the shared debate runtime so specialists, judges, and leads receive the channels they declare instead of hardcoded payload fragments. Add a generic pre-panel support evaluation step that consults the new config: supported panels run, thin-evidence panels can emit weak-confidence output when allowed, and unsupported panels create a typed skipped-panel result that is visible through service, CLI, API, and persisted run reads. Keep `gatekeepers` as the only checkpoint-specific special case; do not introduce panel-specific graph builders or overlay-only branches.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_api.py tests/test_cli.py -k "skip or unsupported or panel or policy"</verify>
  <done>The shared runtime executes agents from declared channels, lead prompts materially participate in verdict finalization, and unsupported implemented panels are surfaced as explicit skips instead of hidden omissions or run-start failures.</done>
</task>

<task type="auto">
  <name>Task 3: Make memo and IC synthesis truthful when panels are weak-confidence or skipped</name>
  <files>prompts/memo_updates/section_update.md, prompts/ic/synthesizer.md, src/ai_investing/application/services.py, tests/test_monitoring_semantics.py, tests/test_analysis_flow.py</files>
  <action>Update the memo and IC prompts plus the memo projection rules so section text and `overall_recommendation` stay honest once later Phase 6 plans introduce supported, weak-confidence, and skipped panel outcomes. Partial company-quality runs must still reconcile the memo, but overlay gaps need explicit wording such as overlay pending or overlay unsupported for this run. Preserve existing section IDs, memo update semantics, and rerun delta generation, especially the required `what_changed_since_last_run` section on reruns.</action>
  <verify>docker compose run --rm api pytest -q tests/test_monitoring_semantics.py tests/test_analysis_flow.py -k "memo or overall_recommendation or overlay"</verify>
  <done>Memo sections and final IC output stay explicit about what ran, what remained weak-confidence, and what was skipped, without breaking section stability or rerun delta behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_config_and_registry.py -k "panel or policy or implemented"`
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_api.py tests/test_cli.py -k "skip or unsupported or panel or policy"`
- [ ] `docker compose run --rm api pytest -q tests/test_monitoring_semantics.py tests/test_analysis_flow.py -k "memo or overall_recommendation or overlay"`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] Intermediate rollout policies exist between `weekly_default` and final `full_surface`
- [ ] Implemented-but-unsupported panels are visible as structured skips in service, CLI, and API outputs
- [ ] Lead prompts are actually executed through the shared runtime rather than represented only by a prefixed judge summary
- [ ] Rerun delta behavior still writes `what_changed_since_last_run` and does not regress memo section status projection
</verification>

<success_criteria>

- All tasks completed
- Wave rollout is config-driven and reusable
- The shared runtime honors agent channels and lead prompts for every debate panel
- Unsupported run context yields explicit skip behavior instead of run rejection or silent omission
- Memo, IC, and rerun-delta behavior remain truthful and stable while Phase 6 enables more panels

</success_criteria>

<output>
After completion, create `.planning/phases/06-productionize-remaining-panels/06-01-SUMMARY.md`
</output>

---
phase: 04-monitoring-and-connectors
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/ai_investing/domain/models.py
  - src/ai_investing/monitoring/__init__.py
  - src/ai_investing/monitoring/analog_graph.py
  - src/ai_investing/monitoring/service.py
  - src/ai_investing/application/services.py
  - src/ai_investing/tools/builtins.py
  - config/monitoring.yaml
  - prompts/monitoring/delta.md
  - tests/test_analog_graph.py
  - tests/test_monitoring_semantics.py
  - tests/test_run_lifecycle.py
  - docs/monitoring.md
autonomous: true
requirements:
  - V2-04
requirement_scope: slice
requirement_slice_id: V2-04A
user_setup: []
must_haves:
  truths:
    - "Monitoring enrichment is computed through reusable services rather than by adding more ad hoc conditionals inside `RefreshRuntime`."
    - "The existing `MonitoringDelta` fields remain intact; any richer Phase 4 fields are optional and defaulted so old rows, examples, and API payloads still deserialize cleanly."
    - "Operator-facing delta output remains balanced: a concise summary plus structured reasons for why the delta changed."
    - "When analog or base-rate support is relevant, the monitoring layer surfaces the top 1-2 references with a short explanation of why they are similar."
    - "Meaningful contradictions are surfaced at the factor level even when recommendation or risk does not change."
    - "Dependency concentration and thesis-drift output broaden into a current-state view rather than only worsening flags, while existing materiality thresholds and alert semantics remain compatible unless tests justify a deliberate change."
    - "Builtin contradiction and analog tools delegate to the same underlying services so monitoring semantics do not drift from tool outputs."
  artifacts:
    - path: src/ai_investing/monitoring/analog_graph.py
      provides: Reusable analog and base-rate ranking service over structured signals with deterministic top-reference output
      min_lines: 90
    - path: src/ai_investing/monitoring/service.py
      provides: Reusable service that computes richer contradiction, analog, concentration, and thesis-drift signals for `MonitoringDelta`
      min_lines: 170
    - path: src/ai_investing/domain/models.py
      provides: Additive and backward-compatible `MonitoringDelta` detail fields for structured reasons and supporting references
      min_lines: 260
    - path: config/monitoring.yaml
      provides: Live config mapping for drift rules, alert semantics, contradiction thresholds, analog limits, and concentration views
      min_lines: 30
    - path: src/ai_investing/tools/builtins.py
      provides: Builtin monitoring tools that delegate to the shared services instead of divergent heuristics
      min_lines: 140
    - path: tests/test_monitoring_semantics.py
      provides: Deterministic coverage for contradiction, analog or base-rate, concentration, thesis drift, and backward compatibility
      min_lines: 420
    - path: docs/monitoring.md
      provides: Documentation of the richer delta surface and preserved memo-contract compatibility
      min_lines: 90
  key_links:
    - from: config/monitoring.yaml
      to: src/ai_investing/monitoring/service.py
      via: config-driven monitoring rules instead of factor-id hardcoding
      pattern: "drift|alert|contradiction|analog|concentration"
    - from: src/ai_investing/monitoring/analog_graph.py
      to: src/ai_investing/monitoring/service.py
      via: ranked analog and base-rate retrieval feeding reusable monitoring enrichment
      pattern: "graph|analog|base_rate|rank|score"
    - from: src/ai_investing/monitoring/service.py
      to: src/ai_investing/application/services.py
      via: runtime delegation from refresh flow into the reusable monitoring service
      pattern: "compute_monitoring_delta|skip_monitoring_delta"
    - from: src/ai_investing/tools/builtins.py
      to: src/ai_investing/monitoring/analog_graph.py
      via: shared contradiction and analog logic across monitoring and tool surfaces
      pattern: "contradiction|analog|base_rate"
---

<objective>
Deepen monitoring into reusable services that explain contradiction, base-rate analogs, concentration, and thesis drift without changing memo contracts.

Purpose: The repo already has the right extension hints, but current monitoring logic is narrow and only loosely aligned with the contradiction and analog tool surface. This plan makes monitoring richer while keeping `MonitoringDelta` additive and compatible.
Output: A reusable monitoring service, deterministic analog or base-rate ranking, additive delta detail fields, live config-driven monitoring rules, and regressions that preserve the current alert and memo semantics.
</objective>

<requirement_traceability>
Parent requirement: `V2-04`
Slice id: `V2-04A`
Slice acceptance: Monitoring computes richer contradiction, analog or base-rate, concentration, and thesis-drift signals through reusable services, and the enriched delta remains backward compatible.
Deferred from parent requirement: Portfolio-level monitoring history and portfolio or watchlist summary surfaces are handled in `04-04-PLAN.md`.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-and-connectors/04-CONTEXT.md
@.planning/phases/04-monitoring-and-connectors/04-RESEARCH.md
@.planning/phases/04-monitoring-and-connectors/04-VALIDATION.md
@config/monitoring.yaml
@src/ai_investing/domain/models.py
@src/ai_investing/tools/builtins.py
@src/ai_investing/application/services.py
@prompts/monitoring/delta.md
@tests/test_monitoring_semantics.py
@tests/test_run_lifecycle.py
@docs/monitoring.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a reusable monitoring service and make rule mapping config-driven</name>
  <files>src/ai_investing/monitoring/__init__.py, src/ai_investing/monitoring/service.py, src/ai_investing/application/services.py, config/monitoring.yaml</files>
  <action>Create a dedicated monitoring service module and move enrichment logic there so `RefreshRuntime` delegates instead of accumulating more hardcoded conditionals. Replace narrow drift mapping with config-backed rule lookup wherever possible, widen dependency concentration coverage beyond the current special cases, and add current-state thesis-drift views that can describe stable concentration and dependency posture rather than only worsening signals. Keep existing materiality thresholds, alert behavior, and rerun-refresh semantics compatible unless a test-backed reason to change them emerges.</action>
  <verify>docker compose run --rm api pytest -q tests/test_monitoring_semantics.py tests/test_run_lifecycle.py -k "monitoring or drift or concentration"</verify>
  <done>The refresh flow computes richer monitoring through a reusable service, and drift and concentration rules are no longer trapped in narrow hardcoded branches.</done>
</task>

<task type="auto">
  <name>Task 2: Add analog or base-rate references, contradiction support, and additive delta fields</name>
  <files>src/ai_investing/domain/models.py, src/ai_investing/monitoring/analog_graph.py, src/ai_investing/monitoring/service.py, src/ai_investing/tools/builtins.py, tests/test_analog_graph.py</files>
  <action>Implement a concrete analog and base-rate ranking module that can return the top 1-2 supporting references with a short explanation of similarity, and have the monitoring service consume it when producing analog or base-rate output. Expand `MonitoringDelta` additively with optional, defaulted detail fields such as trigger reasons, contradiction references, analog hits, current-state concentration signals, or richer panel-change hints. Update the contradiction and analog builtins so they share the same reusable services instead of maintaining divergent heuristics. Do not require a migration that invalidates old rows or checked-in examples.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analog_graph.py tests/test_monitoring_semantics.py tests/test_run_lifecycle.py -k "analog or graph or contradiction or delta"</verify>
  <done>The richer monitoring payload stays additive and backward compatible, and contradiction plus analog or base-rate analysis now shares one coherent implementation path.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh monitoring prompt and docs, then lock compatibility with regressions</name>
  <files>prompts/monitoring/delta.md, tests/test_monitoring_semantics.py, docs/monitoring.md</files>
  <action>Update the monitoring prompt and documentation so they describe the richer delta surface, the concise-summary-plus-structured-reasons contract, and the preserved compatibility rules. Extend the regression suite to prove `what_changed_since_last_run` still refreshes on every rerun, low-confidence-only drift remains non-material, meaningful contradictions surface even without section movement, existing examples continue to load cleanly after the richer delta model ships, and analog or base-rate results remain deterministic for the same fixture data.</action>
  <verify>docker compose run --rm api pytest -q tests/test_monitoring_semantics.py tests/test_generated_examples.py tests/test_analog_graph.py</verify>
  <done>The docs reflect the actual richer monitoring contract, and the regression suite would catch a break in existing delta, contradiction, analog, or memo semantics immediately.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_monitoring_semantics.py tests/test_run_lifecycle.py`
- [ ] `docker compose run --rm api pytest -q tests/test_analog_graph.py`
- [ ] `docker compose run --rm api pytest -q tests/test_generated_examples.py`
- [ ] `docker compose run --rm api ruff check src tests`
- [ ] Analog or base-rate retrieval returns deterministic ranked references that the monitoring service can surface without rewriting memo contracts
- [ ] Existing `MonitoringDelta` payloads and generated artifacts still deserialize without manual backfill
- [ ] `what_changed_since_last_run` remains refreshed every rerun, low-confidence-only drift remains non-material, and current alert-escalation behavior stays intact unless a deliberate test-backed rule change says otherwise
</verification>

<success_criteria>

- All tasks completed
- Monitoring logic lives behind a reusable service seam instead of expanding `RefreshRuntime` conditionals
- Contradiction, analog or base-rate, concentration, and thesis-drift signals are richer and deterministic
- `MonitoringDelta` stays additive and backward compatible
- Monitoring docs and prompt assets describe the new surface truthfully without changing memo contracts

</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-and-connectors/04-03-SUMMARY.md`
</output>

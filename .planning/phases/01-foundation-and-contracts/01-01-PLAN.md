---
phase: 01-foundation-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_investing/config/models.py
  - src/ai_investing/config/loader.py
  - src/ai_investing/prompts/loader.py
  - src/ai_investing/application/context.py
  - src/ai_investing/tools/registry.py
  - src/ai_investing/providers/base.py
  - src/ai_investing/providers/openai_provider.py
  - src/ai_investing/providers/anthropic_provider.py
  - tests/test_config_and_registry.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - MEMO-02
  - TOOLS-01
  - PROV-01
user_setup: []
must_haves:
  truths:
    - "Registry loading fails fast on invalid cross-references, missing prompt files, and bad tool-bundle relationships."
    - "Agent enable, disable, and reparent remain the only supported runtime topology mutations, and registry reloads reflect them deterministically."
    - "Fake, OpenAI, and Anthropic providers stay behind one shared selection boundary, while tool execution remains least-privilege and logged."
  artifacts:
    - path: src/ai_investing/config/models.py
      provides: Strict typed config contracts for panels, agents, tools, memo sections, and model profiles
      min_lines: 80
    - path: src/ai_investing/config/loader.py
      provides: Centralized registry loading and cross-reference validation
      min_lines: 25
    - path: src/ai_investing/application/context.py
      provides: Runtime lookup boundary for registries, memo section labels, and provider selection
      min_lines: 60
    - path: src/ai_investing/tools/registry.py
      provides: Central least-privilege tool execution and logging path
      min_lines: 40
    - path: tests/test_config_and_registry.py
      provides: Regression coverage for registry validation and mutation semantics
      min_lines: 40
  key_links:
    - from: src/ai_investing/config/loader.py
      to: src/ai_investing/config/models.py
      via: typed registry validation
      pattern: "model_validate|RegistryBundle"
    - from: src/ai_investing/application/context.py
      to: src/ai_investing/providers/openai_provider.py
      via: provider selection
      pattern: "OpenAIModelProvider"
    - from: src/ai_investing/application/context.py
      to: src/ai_investing/providers/anthropic_provider.py
      via: provider selection
      pattern: "AnthropicModelProvider"
    - from: src/ai_investing/tools/registry.py
      to: src/ai_investing/persistence/repositories.py
      via: structured tool log persistence
      pattern: "save_tool_log"
---

<objective>
Harden the config-driven contract layer for registries, memo labels, providers, and tool bundles before deeper runtime work lands.

Purpose: Phase 1 must make topology, prompt references, provider routing, and least-privilege tool access safe to extend without hidden runtime assumptions.
Output: Stronger registry validation, deterministic provider selection, and regression tests for config/provider/tool boundaries.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-VALIDATION.md
@config/panels.yaml
@config/agents.yaml
@config/memo_sections.yaml
@config/tool_bundles.yaml
@config/tool_registry.yaml
@src/ai_investing/config/models.py
@src/ai_investing/config/loader.py
@src/ai_investing/prompts/loader.py
@src/ai_investing/application/context.py
@src/ai_investing/tools/registry.py
@src/ai_investing/providers/base.py
@src/ai_investing/providers/openai_provider.py
@src/ai_investing/providers/anthropic_provider.py
@tests/test_config_and_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict registry cross-reference validation</name>
  <files>src/ai_investing/config/models.py, src/ai_investing/config/loader.py, src/ai_investing/prompts/loader.py</files>
  <action>Extend the registry contract layer so load-time validation checks the relationships the runtime currently assumes: panels reference known memo sections and factors, agents reference real panels/tool bundles/model profiles/prompt files, and registry YAML stays the only source of truth. Use this pass to close config-runtime drift for currently decorative fields by either validating them centrally or wiring them into runtime contracts intentionally. Keep the validation centralized in the loader/model boundary rather than scattering ad hoc runtime checks through services, because the phase goal is to fail fast before orchestration starts.</action>
  <verify>pytest -q tests/test_config_and_registry.py</verify>
  <done>Broken config references raise clear startup-time errors, decorative config fields have an explicit contract, and current registries still load successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Make provider and memo-label resolution deterministic</name>
  <files>src/ai_investing/application/context.py, src/ai_investing/providers/base.py, src/ai_investing/providers/openai_provider.py, src/ai_investing/providers/anthropic_provider.py</files>
  <action>Refine `AppContext` lookup behavior so provider selection, optional dependency failures, `primary_provider` handling, and memo label overrides are deterministic and explicit. Preserve the shared `ModelProvider` boundary and the fake-provider default for tests, but fail clearly when a non-test run requests a real provider that is not installed or configured. Do not let provider wrappers absorb orchestration logic or prompt assembly.</action>
  <verify>pytest -q tests/test_config_and_registry.py</verify>
  <done>Provider selection behavior is predictable from config and environment, real-provider misconfiguration is not silently masked, and memo section labels retain stable IDs with optional sustainability labeling.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten least-privilege tool registry enforcement</name>
  <files>src/ai_investing/tools/registry.py, tests/test_config_and_registry.py</files>
  <action>Harden tool-bundle enforcement and handler lookup so agents can only execute declared tools, missing handlers fail clearly, and each invocation continues to write a structured tool log. Keep the privilege boundary in `ToolRegistryService`; do not duplicate permission logic inside agents or providers.</action>
  <verify>pytest -q tests/test_config_and_registry.py tests/test_analysis_flow.py</verify>
  <done>Tool execution rejects invalid or undeclared access paths with clear errors, and the existing bundle-enforcement tests still pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_config_and_registry.py`
- [ ] `pytest -q tests/test_analysis_flow.py`
- [ ] `ruff check src tests`
</verification>

<success_criteria>

- All tasks completed
- Registry validation covers runtime assumptions for prompts, bundles, labels, and provider profiles
- Agent mutation remains intentionally narrow and reload-safe
- Provider and tool boundaries stay config-driven and test-backed

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-01-SUMMARY.md`
</output>

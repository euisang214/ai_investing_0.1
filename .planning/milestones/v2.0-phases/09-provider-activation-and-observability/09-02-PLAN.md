---
phase: 09-provider-activation-and-observability
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/ai_investing/providers/base.py
  - src/ai_investing/providers/openai_provider.py
  - src/ai_investing/providers/anthropic_provider.py
  - src/ai_investing/providers/gemini_provider.py
  - src/ai_investing/providers/groq_provider.py
  - src/ai_investing/providers/openai_compatible_provider.py
  - src/ai_investing/providers/resilient.py
  - src/ai_investing/logging.py
  - src/ai_investing/domain/models.py
  - src/ai_investing/persistence/tables.py
  - src/ai_investing/persistence/repositories.py
  - src/ai_investing/application/services.py
  - src/ai_investing/application/context.py
  - src/ai_investing/api/main.py
  - alembic/versions/0004_phase9_token_usage.py
  - tests/test_logging.py
  - tests/test_token_tracking.py
  - tests/test_retry.py
  - .planning/phases/09-provider-activation-and-observability/09-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - COST-02
requirement_scope: full
user_setup: []
must_haves:
  truths:
    - "All application log output uses structlog with JSON rendering to stdout, including run_id, company_id, and panel_id fields where applicable."
    - "Every LLM invocation returns token usage metadata (input_tokens, output_tokens) which is persisted per-agent in a token_usage_log Postgres table."
    - "LLM provider calls retry with exponential backoff (1s, 2s, 4s) on 429, 5xx, timeout, and network errors — 2 retries per provider before falling to next in chain."
    - "Application errors are captured with sufficient context for debugging including run_id, panel_id, agent_id, provider, and model."
    - "The fake provider is not affected — it returns zero token usage and does not retry."
    - "All existing tests continue to pass."
  artifacts:
    - path: src/ai_investing/providers/resilient.py
      provides: ResilientProvider wrapper that adds retry logic and token extraction around any ModelProvider
      min_lines: 80
    - path: src/ai_investing/logging.py
      provides: structlog configuration function with JSON rendering and context binding
      min_lines: 40
    - path: src/ai_investing/persistence/tables.py
      provides: token_usage_log SQLAlchemy table definition
      min_lines: 550
    - path: alembic/versions/0004_phase9_token_usage.py
      provides: Alembic migration for token_usage_log table
      min_lines: 25
    - path: tests/test_retry.py
      provides: Tests for retry logic, exponential backoff, and provider chain fallback on failure
      min_lines: 80
    - path: tests/test_token_tracking.py
      provides: Tests for token usage recording and per-agent/panel/run aggregation
      min_lines: 80
    - path: tests/test_logging.py
      provides: Tests for structured log output format and context fields
      min_lines: 40
  key_links:
    - from: src/ai_investing/providers/resilient.py
      to: src/ai_investing/providers/base.py
      via: ResilientProvider wraps any ModelProvider and delegates generate_structured with retry
      pattern: "ResilientProvider|retry|backoff|generate_structured"
    - from: src/ai_investing/application/services.py
      to: src/ai_investing/persistence/repositories.py
      via: RefreshRuntime records token usage after each LLM call
      pattern: "token_usage|save_token_usage|TokenUsageRecord"
    - from: src/ai_investing/application/context.py
      to: src/ai_investing/providers/resilient.py
      via: get_provider wraps real providers in ResilientProvider
      pattern: "ResilientProvider|resilient"
---

<objective>
Add structured logging, token tracking, and retry resilience to the provider layer.

Purpose: Plan 01 delivered the provider chain architecture. This plan adds the production observability layer: structured JSON logging via structlog, per-agent token usage tracking in a new Postgres table, retry logic with exponential backoff, and error context capture.
Output: Structured logging infrastructure, token_usage_log table with Alembic migration, ResilientProvider retry wrapper, integration into the runtime pipeline, and regression tests.
</objective>

<requirement_traceability>
Parent requirements: `OBS-01`, `OBS-02`, `OBS-03`, `COST-02`
Slice acceptance:
1. All log output uses structlog JSON format with `run_id`, `company_id`, and `panel_id` context.
2. Every LLM invocation logs and persists token counts per agent.
3. Provider calls retry on 429, 5xx, timeout, and network errors with exponential backoff.
4. Application errors include sufficient debugging context.
5. Phase verification artifact closes all four requirements.
Out of scope: Per-run token budget caps (Phase 12), cost exposure in API/CLI (Phase 12), API authentication (Phase 10).
</requirement_traceability>

<context>
@.planning/phases/09-provider-activation-and-observability/09-CONTEXT.md
@.planning/phases/09-provider-activation-and-observability/09-01-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/ai_investing/providers/base.py
@src/ai_investing/providers/openai_provider.py
@src/ai_investing/providers/anthropic_provider.py
@src/ai_investing/application/services.py
@src/ai_investing/application/context.py
@src/ai_investing/persistence/tables.py
@src/ai_investing/persistence/repositories.py
@src/ai_investing/domain/models.py
@alembic/versions/0003_phase5_background_ops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured logging with structlog</name>
  <files>src/ai_investing/logging.py, src/ai_investing/application/services.py, src/ai_investing/application/context.py, src/ai_investing/api/main.py, pyproject.toml, tests/test_logging.py</files>
  <action>1. Add `structlog` to project dependencies in `pyproject.toml`.

2. Create `src/ai_investing/logging.py` with:
- A `configure_logging(log_level: str = "INFO")` function that sets up structlog with JSON rendering to stdout
- A `get_logger(name: str)` function that returns a bound structlog logger
- Context binding helpers: `bind_run_context(run_id, company_id)`, `bind_panel_context(panel_id, agent_id=None)`
- Default processors: timestamp, log level, caller info, JSON rendering

3. Call `configure_logging(settings.log_level)` in `AppContext.load()`.

4. Add structured log calls at key points in `RefreshRuntime`:
- INFO: "run_started" with run_id, company_id, company_type
- INFO: "panel_started" / "panel_completed" with panel_id, verdict summary
- INFO: "provider_selected" with provider name, model, profile tier
- WARNING: "provider_fallback" when falling to next provider in chain
- WARNING: "stale_evidence_detected" when stale evidence affects confidence
- ERROR: "llm_call_failed" with provider, model, error type, error message
- DEBUG: "llm_call_complete" with prompt length, response length

5. Add structured logging to the FastAPI startup in `api/main.py`.

6. Write `tests/test_logging.py` that verifies:
- Log output is valid JSON
- Context fields (run_id, company_id, panel_id) are present
- Log level filtering works</action>
  <verify>docker compose run --rm api pytest -q tests/test_logging.py --tb=short</verify>
  <done>All application log output uses structlog JSON format with contextual run, company, and panel fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add token tracking with persistence</name>
  <files>src/ai_investing/domain/models.py, src/ai_investing/persistence/tables.py, src/ai_investing/persistence/repositories.py, src/ai_investing/providers/base.py, src/ai_investing/providers/openai_provider.py, src/ai_investing/providers/anthropic_provider.py, src/ai_investing/providers/gemini_provider.py, src/ai_investing/providers/groq_provider.py, src/ai_investing/providers/openai_compatible_provider.py, src/ai_investing/application/services.py, alembic/versions/0004_phase9_token_usage.py, tests/test_token_tracking.py</files>
  <action>1. Add a `GenerationResult` dataclass to `providers/base.py`:
```python
@dataclass
class GenerationResult(Generic[ModelT]):
    value: ModelT
    input_tokens: int
    output_tokens: int
    provider: str
    model: str
```

2. Add `generate_structured_with_usage()` method to `ModelProvider` that returns `GenerationResult[ModelT]`. Default implementation calls `generate_structured()` and returns zero token counts. Real providers override this to extract token usage from the LangChain response metadata (`response.response_metadata["usage"]`).

3. Update each real provider adapter (OpenAI, Anthropic, Gemini, Groq, OpenAI-compatible) to:
- Use `model.with_structured_output(response_model, include_raw=True)` to get both the parsed result and raw response
- Extract `input_tokens` and `output_tokens` from the raw response metadata
- Return `GenerationResult` with the parsed value and token counts

4. Add `TokenUsageRecord` domain model in `domain/models.py`:
```python
class TokenUsageRecord(DomainModel):
    id: str
    run_id: str
    panel_id: str
    agent_id: str
    factor_id: str | None
    provider: str
    model: str
    input_tokens: int
    output_tokens: int
    estimated_cost_usd: float
    created_at: datetime
```

5. Add `token_usage_log` table in `persistence/tables.py` matching the domain model.

6. Add `save_token_usage(record: TokenUsageRecord)` and `list_token_usage(run_id: str)` to `Repository`.

7. Create Alembic migration `0004_phase9_token_usage.py` for the new table.

8. Update `RefreshRuntime._run_specialists()` and `._run_judge()` to:
- Call `generate_structured_with_usage()` instead of `generate_structured()`
- After each LLM call, persist a `TokenUsageRecord` with the agent, panel, factor context
- Use a simple cost lookup table for estimation (GPT-4o: $2.50/$10 per 1M tokens, Claude Sonnet: $3/$15, etc.)

9. Write `tests/test_token_tracking.py` that verifies:
- FakeModelProvider returns zero token counts
- TokenUsageRecord is persisted after LLM calls
- Per-panel and per-run sums are queryable
- Cost estimation produces reasonable values</action>
  <verify>docker compose run --rm api pytest -q tests/test_token_tracking.py --tb=short</verify>
  <done>Every LLM invocation records token usage per-agent in the token_usage_log table with cost estimation.</done>
</task>

<task type="auto">
  <name>Task 3: Add retry resilience and phase verification</name>
  <files>src/ai_investing/providers/resilient.py, src/ai_investing/application/context.py, tests/test_retry.py, .planning/phases/09-provider-activation-and-observability/09-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>1. Create `src/ai_investing/providers/resilient.py` with a `ResilientProvider` class:
- Wraps any `ModelProvider` instance
- `generate_structured_with_usage()` implementation:
  - Catches `httpx.HTTPStatusError` (429, 5xx), `httpx.TimeoutException`, `httpx.ConnectError`, and generic network errors
  - On retriable error: log WARNING with error details, wait exponential backoff (1s, 2s, 4s), retry
  - After 2 retries exhausted (3 total attempts), raise `ProviderExhaustedError` with the last error
- Does NOT wrap `FakeModelProvider` — only real providers get retry logic

2. Create `ProviderExhaustedError(RuntimeError)` in `providers/base.py` for clean error handling.

3. Update `AppContext.get_provider()` to wrap non-fake providers in `ResilientProvider`.

4. Update the provider chain resolution in `get_provider()`:
- When `ResilientProvider` raises `ProviderExhaustedError`, catch it, log WARNING "provider_exhausted", and try the next entry in the chain
- Track which providers were tried and why they failed
- If all entries exhausted, raise RuntimeError with the full failure history

5. Write `tests/test_retry.py` that verifies:
- Retry on 429 with exponential backoff timing
- Retry on 5xx errors
- Fallback to next provider after retries exhausted
- FakeModelProvider is not wrapped in ResilientProvider
- Clear error message when all providers fail

6. Run the full test suite and lint to confirm no regressions.

7. Create `09-VERIFICATION.md` following the Phase 08 verification pattern:
- Verify PROV-02, PROV-03, PROV-05 from Plan 01
- Verify OBS-01, OBS-02, OBS-03, COST-02 from Plan 02
- Include executable verification results

8. Update `REQUIREMENTS.md` to mark PROV-02, PROV-03, PROV-05, OBS-01, OBS-02, OBS-03, COST-02 as complete.
Update `ROADMAP.md` to mark Phase 9 complete. Update `STATE.md`. Commit all artifacts.</action>
  <verify>docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests</verify>
  <done>Retry resilience is implemented, phase verification closes all Phase 9 requirements.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_logging.py`
- [ ] `docker compose run --rm api pytest -q tests/test_token_tracking.py`
- [ ] `docker compose run --rm api pytest -q tests/test_retry.py`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] structlog JSON output verified in test
- [ ] token_usage_log table created via Alembic migration
- [ ] TokenUsageRecord persisted per-agent on each LLM call
- [ ] Retry logic with exponential backoff works on 429 and 5xx
- [ ] Provider chain fallback works after retries exhausted
- [ ] FakeModelProvider is unaffected (no retry, zero tokens)
- [ ] 09-VERIFICATION.md closes PROV-02, PROV-03, PROV-05, OBS-01, OBS-02, OBS-03, COST-02
- [ ] All artifacts committed
</verification>

<success_criteria>

- Structured logging via structlog with JSON output and contextual fields
- Per-agent token usage tracked and persisted in Postgres
- Retry with exponential backoff on transient errors
- Provider chain fallback after retries exhausted
- Clear error reporting when all providers fail
- Full test suite passes with no regressions
- Phase 9 verification artifact closes all 7 requirements

</success_criteria>

<output>
After completion, create `.planning/phases/09-provider-activation-and-observability/09-02-SUMMARY.md`
</output>

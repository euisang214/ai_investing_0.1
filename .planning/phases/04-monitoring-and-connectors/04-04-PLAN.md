---
phase: 04-monitoring-and-connectors
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/ai_investing/domain/read_models.py
  - src/ai_investing/application/context.py
  - src/ai_investing/application/portfolio.py
  - src/ai_investing/persistence/repositories.py
  - src/ai_investing/api/main.py
  - src/ai_investing/cli.py
  - tests/test_repository_semantics.py
  - tests/test_api.py
  - tests/test_cli.py
  - README.md
  - docs/architecture.md
  - docs/memory_model.md
autonomous: false
requirements:
  - V2-04
requirement_scope: slice
requirement_slice_id: V2-04B
user_setup: []
must_haves:
  truths:
    - "Phase 4 exposes company monitoring history plus one portfolio-level monitoring summary as read-only projections over structured records; it does not make `portfolio_fit_positioning` runnable and does not widen orchestration."
    - "The portfolio-level summary includes both portfolio and watchlist names by default, but it keeps them structurally and visually separated so holdings never blur with watchlist coverage."
    - "The main portfolio-level summary is organized by change type first, not by company list or severity first, and it can highlight actionable shared-risk or overlap clusters before secondary exploratory analog drill-down."
    - "Existing operator surfaces remain backward compatible: `show-run`, `show-delta`, `/runs/{run_id}`, and `/companies/{company_id}/delta` keep working as before."
    - "New API and CLI exposure is additive and documentation-backed, so engineers can inspect monitoring history and portfolio or watchlist summaries without a frontend."
  artifacts:
    - path: src/ai_investing/domain/read_models.py
      provides: Typed company-monitoring-history and portfolio-summary read models
      min_lines: 90
    - path: src/ai_investing/application/portfolio.py
      provides: Read-side service for company delta history and portfolio or watchlist monitoring summaries derived from structured records
      min_lines: 150
    - path: src/ai_investing/persistence/repositories.py
      provides: Query helpers for monitoring-delta history and coverage-segmented portfolio or watchlist aggregation
      min_lines: 280
    - path: src/ai_investing/api/main.py
      provides: Additive endpoints for monitoring history and portfolio or watchlist monitoring views
      min_lines: 330
    - path: src/ai_investing/cli.py
      provides: Additive commands for monitoring history and portfolio-summary inspection
      min_lines: 160
    - path: docs/memory_model.md
      provides: Documentation of the new read-only monitoring history and portfolio-summary contracts, including a representative example payload for manual boundary review
      min_lines: 100
  key_links:
    - from: src/ai_investing/persistence/repositories.py
      to: src/ai_investing/application/portfolio.py
      via: read-side query helpers for monitoring history and coverage-status aggregation
      pattern: "list_monitoring_deltas|portfolio|watchlist|coverage_status"
    - from: src/ai_investing/application/portfolio.py
      to: src/ai_investing/api/main.py
      via: typed service output feeding additive API surfaces
      pattern: "portfolio|watchlist|monitoring|history"
    - from: src/ai_investing/application/portfolio.py
      to: src/ai_investing/cli.py
      via: shared read model backing both API and CLI exposure
      pattern: "show|list|portfolio|watchlist"
    - from: docs/memory_model.md
      to: docs/architecture.md
      via: explanation of why portfolio monitoring stays read-only and separate from memo orchestration
      pattern: "read-only|portfolio_fit_positioning|monitoring history"
---

<objective>
Expose monitoring history and portfolio-level monitoring summaries through typed read models and additive operator surfaces.

Purpose: Richer monitoring is only operationally useful if engineers can inspect history across runs and view portfolio-wide change patterns without scraping memos or turning the scaffolded portfolio panel into runnable reasoning logic.
Output: Read-side models and services for company monitoring history and portfolio or watchlist summaries, plus additive CLI, API, and docs coverage that preserves the current single-company runtime contracts.
</objective>

<requirement_traceability>
Parent requirement: `V2-04`
Slice id: `V2-04B`
Slice acceptance: Engineers can query monitoring history and portfolio-level summaries through typed read models and additive CLI or API surfaces, with portfolio and watchlist names clearly separated, without changing memo contracts or making the portfolio panel runnable.
Deferred from parent requirement: A full portfolio-position input system, exploratory portfolio analytics UI, and productionized `portfolio_fit_positioning` reasoning remain out of scope for this phase.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
@/Users/ethanshin/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-and-connectors/04-CONTEXT.md
@.planning/phases/04-monitoring-and-connectors/04-RESEARCH.md
@.planning/phases/04-monitoring-and-connectors/04-VALIDATION.md
@src/ai_investing/application/context.py
@src/ai_investing/persistence/repositories.py
@src/ai_investing/api/main.py
@src/ai_investing/cli.py
@docs/architecture.md
@docs/memory_model.md
@README.md
@prompts/panels/portfolio_fit/placeholder.md
@tests/test_repository_semantics.py
@tests/test_api.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed read models and read-side services after validating the merged wave-2 baseline</name>
  <files>src/ai_investing/domain/read_models.py, src/ai_investing/application/context.py, src/ai_investing/application/portfolio.py, src/ai_investing/persistence/repositories.py</files>
  <action>Before touching the read-side implementation, run `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests` so wave 3 starts from a validated merged wave-2 state. If that gate fails, stop and fix or report the blocker before proceeding. Then create typed read models for company monitoring history and portfolio-level monitoring summaries, and add a dedicated read-side service that derives them from persisted structured records. Add repository helpers for monitoring-delta history and for selecting covered names by coverage status so the summary includes both portfolio and watchlist names while keeping them separate. Organize the main summary by change type first, then by affected segment, and make actionable shared-risk or overlap clusters the primary analog-oriented output whenever they exist.</action>
  <verify>docker compose run --rm api pytest -q tests/test_repository_semantics.py -k "monitoring or portfolio or watchlist"</verify>
  <done>The merged wave-2 baseline was validated before wave-3 coding began, and the repo now has typed, reusable read models for company history and segmented portfolio-monitoring views derived from persisted records rather than memo scraping.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the new read surfaces through additive API and CLI commands with a reviewable example</name>
  <files>src/ai_investing/api/main.py, src/ai_investing/cli.py, docs/memory_model.md, tests/test_api.py, tests/test_cli.py</files>
  <action>Add read-only operator exposure for company monitoring history and the new portfolio-level summary, such as history listing per company and a portfolio-summary command or endpoint that includes clearly separated portfolio and watchlist sections. Preserve current run and latest-delta surfaces exactly; the new routes and commands should be additive, not replacements. Capture a representative example payload or output in `docs/memory_model.md` so the boundary can be reviewed before the phase is finalized.</action>
  <verify>docker compose run --rm api pytest -q tests/test_api.py tests/test_cli.py -k "delta or monitoring or portfolio or watchlist"</verify>
  <done>Engineers can inspect monitoring history and segmented portfolio summaries through additive CLI and API surfaces, and the docs now contain a concrete example for boundary review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Read-only portfolio-summary example captured in `docs/memory_model.md`, backed by additive CLI and API surfaces.</what-built>
  <how-to-verify>Review `docs/memory_model.md` and confirm the example still respects the intended boundary: it is read-only, grouped by change type first, includes both portfolio and watchlist names by default, keeps those segments clearly separated, elevates actionable shared-risk or overlap clusters into the main summary when present, and does not imply that `portfolio_fit_positioning` is now runnable.</how-to-verify>
  <resume-signal>Type "approved" or describe the portfolio-boundary issue that needs to change.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document the read-only boundary, preserve the scaffold story, and finish with a full-suite gate</name>
  <files>README.md, docs/architecture.md, docs/memory_model.md</files>
  <action>Update the repo docs so they explain what the monitoring-history and portfolio-summary surfaces do, why they are read-only, how portfolio and watchlist names stay separate, and how change-type grouping should be interpreted by operators. State explicitly that `portfolio_fit_positioning` remains scaffold-only and that Phase 4 adds operator visibility, not a frontend or a new reasoning runtime. Reconcile `docs/memory_model.md` with any feedback from the checkpoint, then finish by rerunning the full suite so the final wave closes on a validated baseline.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; readme = Path('README.md').read_text(encoding='utf-8').lower(); arch = Path('docs/architecture.md').read_text(encoding='utf-8').lower(); memory = Path('docs/memory_model.md').read_text(encoding='utf-8').lower(); combined = '\\n'.join([readme, arch, memory]); required = ['portfolio_fit_positioning', 'read-only', 'monitoring history', 'portfolio monitoring', 'watchlist']; missing = [item for item in required if item not in combined]; assert not missing, missing"</verify>
  <done>The docs accurately describe the new operator surfaces, preserve the scaffold-only posture of `portfolio_fit_positioning`, and the final wave finishes with the full suite green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_repository_semantics.py tests/test_api.py tests/test_cli.py`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] `docker compose run --rm api python -c "from pathlib import Path; readme = Path('README.md').read_text(encoding='utf-8').lower(); arch = Path('docs/architecture.md').read_text(encoding='utf-8').lower(); memory = Path('docs/memory_model.md').read_text(encoding='utf-8').lower(); combined = '\\n'.join([readme, arch, memory]); required = ['portfolio_fit_positioning', 'read-only', 'monitoring history', 'portfolio monitoring', 'watchlist']; missing = [item for item in required if item not in combined]; assert not missing, missing"`
- [ ] Existing `show-run`, `show-delta`, `/runs/{run_id}`, and `/companies/{company_id}/delta` surfaces remain backward compatible
- [ ] Portfolio summaries include both portfolio and watchlist names by default, keep them clearly separated and organized by change type rather than as one blended company list, and surface actionable shared-risk or overlap clusters in the main summary when present
</verification>

<success_criteria>

- All tasks completed
- The repo exposes typed monitoring history and portfolio-summary views through additive read-side services
- Portfolio and watchlist names remain clearly separated in the summary surface
- CLI and API surfaces make the new views usable without replacing existing run and delta entrypoints
- Docs explain the read-model boundary clearly and preserve the scaffold-only posture of `portfolio_fit_positioning`
- Wave 3 begins only after a full-suite validation of the merged wave-2 state, and Phase 4 closes with the full suite green

</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-and-connectors/04-04-SUMMARY.md`
</output>

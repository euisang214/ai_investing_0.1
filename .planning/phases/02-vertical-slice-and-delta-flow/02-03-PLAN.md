---
phase: 02-vertical-slice-and-delta-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ai_investing/cli.py
  - src/ai_investing/api/main.py
  - README.md
  - docs/runbook.md
  - docs/architecture.md
  - tests/test_cli.py
  - tests/test_api.py
autonomous: true
requirements:
  - COV-03
  - MEM-03
  - ORCH-02
user_setup: []
must_haves:
  truths:
    - "CLI and API expose explicit checkpoint-aware control flows so operators can inspect a paused gatekeeper result, continue deeper analysis, or continue provisionally after a failed gatekeeper."
    - "Operator-facing responses include structured fields such as `gate_decision`, `awaiting_continue`, `gated_out`, `stopped_after_panel`, and `provisional` instead of requiring clients to infer state from prose."
    - "Coverage-driven entrypoints keep the same explicit checkpoint behavior as direct per-company runs; nothing silently auto-continues past gatekeepers."
    - "The existing `run-panel` command and per-panel API route cannot be used to jump directly into downstream panels without the required gatekeeper checkpoint or explicit resume context."
  artifacts:
    - path: src/ai_investing/cli.py
      provides: Explicit CLI commands for checkpoint inspection and continue or provisional-continue actions
      min_lines: 190
    - path: src/ai_investing/api/main.py
      provides: Typed FastAPI checkpoint endpoints and response models for paused and resumed runs
      min_lines: 260
    - path: tests/test_cli.py
      provides: Command-level coverage for analyze, continue, and checkpoint inspection flows
      min_lines: 80
    - path: tests/test_api.py
      provides: HTTP contract coverage for paused-run responses and continue endpoints
      min_lines: 140
    - path: docs/runbook.md
      provides: Operator workflow for gatekeeper checkpoint, continue, override, and rerun inspection
      min_lines: 35
  key_links:
    - from: src/ai_investing/cli.py
      to: src/ai_investing/application/services.py
      via: operator commands for analyze, continue, and run inspection
      pattern: "AnalysisService|continue_run|get_run"
    - from: src/ai_investing/api/main.py
      to: src/ai_investing/application/services.py
      via: typed response models and continue endpoints
      pattern: "continue_run|get_run|awaiting_continue"
    - from: README.md
      to: docs/runbook.md
      via: checkpoint-aware quick-start handoff
---

<objective>
Expose the checkpointed vertical slice cleanly through the operator interfaces so the mandatory gatekeeper pause is explicit, queryable, and usable from both CLI and API.

Purpose: Phase 2 is not proven if pause and continue only exist as internal services or if automation clients have to scrape prose to understand whether a run stopped, gated out, or continued provisionally.
Output: Checkpoint-aware CLI and API commands, stable response contracts, and updated operator docs.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-CONTEXT.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-RESEARCH.md
@README.md
@docs/runbook.md
@docs/architecture.md
@src/ai_investing/cli.py
@src/ai_investing/api/main.py
@src/ai_investing/application/services.py
@tests/test_cli.py
@tests/test_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit CLI controls for checkpointed runs</name>
  <files>src/ai_investing/cli.py, tests/test_cli.py</files>
  <action>Extend the CLI so operators can inspect a run by `run_id`, continue a paused run after a gatekeeper pass, and override a failed gatekeeper with an explicit provisional flag. Keep the existing analyze and refresh entrypoints, but make their checkpoint behavior obvious in output. Do not silently chain analyze into continue. If command naming needs to expand, prefer clear verbs such as `show-run` and `continue-run` over overloading unrelated commands. The existing `run-panel` entrypoint must honor the same rule by either limiting direct execution to `gatekeepers` or requiring explicit resume context for downstream work, with clear errors when an operator tries to bypass the checkpoint.</action>
  <verify>pytest -q tests/test_cli.py</verify>
  <done>CLI users can see whether a run is awaiting continue, stop or resume it intentionally, distinguish normal downstream work from provisional override output, and cannot use `run-panel` to bypass the mandatory gatekeeper checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed API routes and payloads for run checkpoint control</name>
  <files>src/ai_investing/api/main.py, tests/test_api.py</files>
  <action>Introduce typed request and response models for paused, resumed, and completed run states. Add endpoints that let API clients fetch a run by ID and continue a paused run with an explicit action. Preserve the existing per-company routes, but make sure they return the structured checkpoint fields the phase context called out. The per-panel route must honor the same checkpoint rule as the CLI by refusing unsupported direct downstream-panel execution without resume context. Keep business logic in services; the API should validate and expose it, not reimplement it.</action>
  <verify>pytest -q tests/test_api.py</verify>
  <done>API clients can programmatically detect paused runs, continue them explicitly, receive consistent payloads across analyze, refresh, and due-coverage workflows, and cannot bypass gatekeeper control through the per-panel route.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh operator documentation for the checkpointed workflow</name>
  <files>README.md, docs/runbook.md, docs/architecture.md</files>
  <action>Update the quick start, runbook, and architecture docs so they describe the mandatory gatekeeper pause, the explicit continue path, provisional override behavior, and how memo and delta artifacts look before and after continuation. Keep the docs aligned with the actual command and endpoint names implemented in this phase, and make sure the narrative still emphasizes reusable subgraphs and living memo updates rather than a one-off human approval hack.</action>
  <verify>pytest -q tests/test_cli.py tests/test_api.py</verify>
  <done>The repo documentation matches the new operator workflow and makes the checkpoint semantics obvious to another engineer reading the project fresh.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_cli.py tests/test_api.py`
- [ ] `ruff check src tests`
- [ ] Follow the documented checkpoint workflow once through CLI or API against the fake provider
</verification>

<success_criteria>

- All tasks completed
- CLI and API both expose explicit continue semantics with stable structured fields
- Paused, gated, stopped, provisional, and completed runs are understandable without reading prose-only messages
- Docs reflect the real Phase 2 operator workflow

</success_criteria>

<output>
After completion, create `.planning/phases/02-vertical-slice-and-delta-flow/02-03-SUMMARY.md`
</output>

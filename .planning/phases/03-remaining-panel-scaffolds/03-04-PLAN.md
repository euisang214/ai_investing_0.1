---
phase: 03-remaining-panel-scaffolds
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - README.md
  - docs/architecture.md
  - docs/factor_ontology.md
  - docs/panel_extension_path.md
autonomous: true
requirements:
  - V2-01
requirement_scope: prerequisite_slice
requirement_slice_id: V2-01D
user_setup: []
must_haves:
  truths:
    - "This plan delivers the documentation prerequisite slice of `V2-01`; it explains how to productionize the remaining panels later without claiming that Phase 3 already did so."
    - "Repo docs clearly separate the implemented vertical slice from the scaffold-only future panel surface."
    - "Another engineer can extend a scaffolded panel by following an explicit file-by-file checklist and worked example instead of reverse-engineering the runtime."
    - "Documentation explains that future-facing policies may exist in config before those panels are safe to execute."
  artifacts:
    - path: README.md
      provides: Accurate top-level status and operator-facing explanation of scaffold-only panel posture
      min_lines: 70
    - path: docs/architecture.md
      provides: Architecture-level explanation of how scaffolded panels fit into the config-driven runtime without becoming runnable
      min_lines: 130
    - path: docs/factor_ontology.md
      provides: Factor-level explanation of scaffold breadth and separation between implemented and scaffold-only coverage
      min_lines: 20
    - path: docs/panel_extension_path.md
      provides: Dedicated panel-extension guide with file checklist, sequence, and a worked example for one scaffold-only panel
      min_lines: 60
  key_links:
    - from: README.md
      to: docs/panel_extension_path.md
      via: top-level handoff from current project status to the scaffold extension guide
    - from: docs/architecture.md
      to: docs/factor_ontology.md
      via: explanation of how panel topology and factor ontology expand together
      pattern: "scaffold|implemented|panel"
---

<objective>
Document the scaffold posture and extension path so Phase 3 leaves behind a maintainable map from placeholder panels to future production implementation.

Purpose: The scaffold surface is only useful if another engineer can tell what is currently real, what is intentionally placeholder-only, and exactly how to take one panel to implementation readiness later.
Output: Clear repo-level docs with a short embedded extension checklist, plus a dedicated extension guide with a worked example. This documents the prerequisite slice of `V2-01` without claiming the remaining panels are already productionized.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01D`
Slice acceptance: Repo docs enumerate the currently implemented panels versus scaffold-only panels, include a short extension checklist in the existing docs, and provide a dedicated guide for moving one scaffolded panel toward production readiness.
Deferred from parent requirement: Documentation for real production implementations of the remaining panels after those panels actually exist.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-panel-scaffolds/03-CONTEXT.md
@.planning/phases/03-remaining-panel-scaffolds/03-RESEARCH.md
@README.md
@docs/architecture.md
@docs/factor_ontology.md
@docs/prompting_strategy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh top-level docs and embed a short scaffold-extension checklist</name>
  <files>README.md, docs/architecture.md, docs/factor_ontology.md</files>
  <action>Update the top-level project docs so they say plainly which panels are implemented today, which panels are scaffold-only, and why future-facing policies can appear in config before those panels are runnable. Enumerate the currently implemented panel ids (`gatekeepers`, `demand_revenue_quality`) and the scaffold-only panel ids at least once across the existing docs surface. Add a short extension checklist into the existing docs surface so a new engineer can see the high-level sequence without opening the full guide first. Keep the explanation consistent with the runtime boundary verified in the prior plan. Do not oversell the remaining panels as implemented just because they now have richer prompts and config entries.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; readme = Path('README.md').read_text(encoding='utf-8'); arch = Path('docs/architecture.md').read_text(encoding='utf-8'); ontology = Path('docs/factor_ontology.md').read_text(encoding='utf-8'); combined = '\\n'.join([readme, arch, ontology]); implemented = ['gatekeepers', 'demand_revenue_quality']; scaffolded = ['supply_product_operations', 'market_structure_growth', 'macro_industry_transmission', 'management_governance_capital_allocation', 'financial_quality_liquidity_economic_model', 'external_regulatory_geopolitical', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning']; assert all(item in combined for item in implemented); assert all(item in combined for item in scaffolded); checklist_targets = [readme, arch, ontology]; assert all('checklist' in text.lower() and 'config/agents.yaml' in text and 'prompts/' in text for text in checklist_targets)"</verify>
  <done>The repo docs describe the implemented versus scaffold-only split accurately and include a visible short checklist in the existing documentation surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated panel-extension guide with one worked example</name>
  <files>docs/panel_extension_path.md</files>
  <action>Create a dedicated guide that shows exactly how to take a scaffold-only panel to implementation readiness. Include a file-by-file checklist, the order of operations, the config and prompt contracts to preserve, the testing expectations, and the rule that runtime changes are only allowed when the abstraction truly needs expansion. Use one worked example based on `external_regulatory_geopolitical` so the guide is concrete without expanding Phase 3 scope.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; text = Path('docs/panel_extension_path.md').read_text(encoding='utf-8'); required = ['external_regulatory_geopolitical', 'config/panels.yaml', 'config/agents.yaml', 'config/factors.yaml', 'prompts/', 'tests/', 'order of operations', 'checklist', 'runtime changes', 'abstraction truly needs expansion']; missing = [item for item in required if item.lower() not in text.lower()]; assert not missing, missing"</verify>
  <done>The repo has a standalone, actionable guide with the promised checklist, order of operations, and worked example for moving a scaffolded panel toward production implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link the new guide so it is easy to find from the existing docs</name>
  <files>README.md, docs/architecture.md, docs/factor_ontology.md</files>
  <action>Add clear links from the README and the two existing domain docs to the new extension guide, and keep the link text obvious to a new engineer scanning the repo. Make the architecture and ontology docs reinforce the same file-by-file extension story rather than drifting into generic prose.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; targets = [Path('README.md').read_text(encoding='utf-8'), Path('docs/architecture.md').read_text(encoding='utf-8'), Path('docs/factor_ontology.md').read_text(encoding='utf-8')]; assert all('panel_extension_path.md' in text for text in targets)"</verify>
  <done>The extension guide is discoverable from the docs another engineer is most likely to read first.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api python -c "from pathlib import Path; assert Path('docs/panel_extension_path.md').exists()"`
- [ ] `docker compose run --rm api python -c "from pathlib import Path; text = Path('docs/panel_extension_path.md').read_text(encoding='utf-8'); required = ['checklist', 'order of operations', 'external_regulatory_geopolitical', 'config/panels.yaml', 'config/agents.yaml', 'config/factors.yaml', 'prompts/', 'tests/', 'runtime changes', 'abstraction truly needs expansion']; missing = [item for item in required if item.lower() not in text.lower()]; assert not missing, missing"`
- [ ] `docker compose run --rm api python -c "from pathlib import Path; targets = [Path('README.md').read_text(encoding='utf-8'), Path('docs/architecture.md').read_text(encoding='utf-8'), Path('docs/factor_ontology.md').read_text(encoding='utf-8')]; combined = '\\n'.join(targets).lower(); implemented = ['gatekeepers', 'demand_revenue_quality']; scaffolded = ['supply_product_operations', 'market_structure_growth', 'macro_industry_transmission', 'management_governance_capital_allocation', 'financial_quality_liquidity_economic_model', 'external_regulatory_geopolitical', 'expectations_catalyst_realization', 'security_or_deal_overlay', 'portfolio_fit_positioning']; policy_terms = ['future-facing policies', 'config', 'runnable']; assert all('panel_extension_path.md' in text for text in targets); assert all(item in combined for item in implemented); assert all(item in combined for item in scaffolded); assert all(term in combined for term in policy_terms); assert all('checklist' in text.lower() and 'config/agents.yaml' in text and 'prompts/' in text for text in targets)"`
- [ ] README, architecture, and factor ontology docs all distinguish implemented panels from scaffold-only panels using the real current panel inventory, explain why future-facing policies can exist in config before those panels are runnable, and each include the short extension checklist
</verification>

<success_criteria>

- All tasks completed
- Another engineer can locate and follow the scaffold extension path without hidden knowledge
- Repo docs reflect the true current implementation boundary
- Existing docs include a short checklist, and the dedicated guide contains the full ordered extension path plus worked example
- The guide preserves the runtime-boundary rule that runtime changes are allowed only when the abstraction truly needs expansion, and the docs explain the future-facing policy posture explicitly
- The new guide and existing docs tell one consistent story about future-facing scaffold panels

</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-panel-scaffolds/03-04-SUMMARY.md`
</output>

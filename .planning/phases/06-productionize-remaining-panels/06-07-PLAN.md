---
phase: 06-productionize-remaining-panels
plan: 07
type: execute
wave: 3
depends_on:
  - 06-04
  - 06-06
files_modified:
  - examples/acme_public/manifest.json
  - examples/acme_public_rerun/manifest.json
  - tests/conftest.py
  - tests/test_analysis_flow.py
  - tests/test_run_lifecycle.py
  - tests/test_generated_examples.py
  - scripts/generate_phase2_examples.py
autonomous: true
requirements:
  - V2-01
requirement_scope: gap_closure
requirement_slice_id: V2-01D-gap-default-acme-expectations
user_setup: []
must_haves:
  truths:
    - "The expectations panel support gate is already correct. This plan must repair the default ACME evidence contract, not relax `expectations_catalyst_realization` readiness or support rules."
    - "The default ACME analyze plus refresh path for `expectations_rollout` must become self-sufficient: it may not require bespoke supplemental connector ingestion that broader UAT and operator flows do not perform."
    - "Keep the scope on the diagnosed root cause only. Do not broaden rollout policy selection, overlay behavior, or unrelated panel logic."
    - "Preserve an explicit unsupported-path regression, but move it off the default ACME fixture once that fixture becomes expectations-ready."
    - "Generated examples and default-path tests must exercise the same ACME seed contract so shipped artifacts cannot drift from the UAT path again."
  artifacts:
    - path: examples/acme_public/manifest.json
      provides: Default ACME baseline evidence families include expectations-ready public support
      min_lines: 120
    - path: examples/acme_public_rerun/manifest.json
      provides: Default ACME rerun input carries expectation or catalyst changes that can move delta sections
      min_lines: 40
    - path: tests/conftest.py
      provides: Default seeded ACME fixture aligns with the shipped ACME analyze path
      min_lines: 80
    - path: tests/test_analysis_flow.py
      provides: Regression coverage for supported default expectations analyze plus refresh behavior
      min_lines: 650
    - path: tests/test_run_lifecycle.py
      provides: Lifecycle coverage for expectations support visibility on the default refresh path
      min_lines: 500
    - path: scripts/generate_phase2_examples.py
      provides: Checked artifact generator uses the same default ACME expectations contract as the default runtime path
      min_lines: 110
  key_links:
    - from: examples/acme_public/manifest.json
      to: config/panels.yaml
      via: default ACME seed satisfies `consensus_views`, `market_data`, and `milestone_tracking` without weakening panel readiness
      pattern: "expectations_catalyst_realization|consensus|market|events"
    - from: examples/acme_public_rerun/manifest.json
      to: tests/test_analysis_flow.py
      via: rerun fixture changes expectations sections on the same default path used in UAT
      pattern: "expectations_variant_view|realization_path_catalysts|delta"
    - from: tests/conftest.py
      to: scripts/generate_phase2_examples.py
      via: one default ACME seed contract shared by tests and checked example generation
      pattern: "acme_public|acme_public_rerun|expectations_rollout"
    - from: tests/test_run_lifecycle.py
      to: tests/test_generated_examples.py
      via: support visibility and checked artifacts stay aligned on the default expectations path
      pattern: "supported|refresh|delta"
---

<objective>
Close the diagnosed Phase 06 expectations gap by making the default ACME analyze and refresh fixtures satisfy the existing expectations support contract.

Purpose: `expectations_catalyst_realization` is correctly marked unsupported when `consensus_views`, `market_data`, and `milestone_tracking` are absent. The bug is that the default ACME fixture contract used by generic analyze or refresh flows does not provide those evidence families, while bespoke tests and generated examples manually ingest extra connector packets first. This plan repairs the default ACME manifests and regression coverage so `expectations_rollout` works on the standard path and still exposes an explicit unsupported case somewhere intentional.
Output: Expectations-ready default ACME baseline and rerun manifests, aligned test and generated-example seeding, and regressions that prove the default path populates `expectations_variant_view`, `realization_path_catalysts`, and rerun deltas without supplemental connector setup.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Gap target: Phase 06 UAT Test 5
Slice id: `V2-01D-gap-default-acme-expectations`
Slice acceptance: On the default ACME analyze plus refresh path, `expectations_rollout` supports `expectations_catalyst_realization`, advances `expectations_variant_view` and `realization_path_catalysts`, and surfaces rerun changes in `what_changed_since_last_run` and monitoring delta without preloading extra connector packets outside the default fixture contract.
Out of scope: Changing expectations panel support thresholds, changing rollout policy composition, or modifying overlay or portfolio-fit behavior.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-productionize-remaining-panels/06-UAT.md
@.planning/debug/acme-expectations-wave-stale.md
@.planning/phases/06-productionize-remaining-panels/06-04-PLAN.md
@config/panels.yaml
@src/ai_investing/application/services.py
@examples/acme_public/manifest.json
@examples/acme_public_rerun/manifest.json
@tests/conftest.py
@tests/test_analysis_flow.py
@tests/test_run_lifecycle.py
@tests/test_generated_examples.py
@scripts/generate_phase2_examples.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the default ACME baseline and rerun manifests expectations-ready</name>
  <files>examples/acme_public/manifest.json, examples/acme_public_rerun/manifest.json</files>
  <action>Fold equivalent expectations evidence into the default ACME manifests so ingesting `examples/acme_public` supplies `consensus_views`, `market_data`, and `milestone_tracking` for the public expectations panel, and ingesting `examples/acme_public_rerun` moves those same expectations or catalyst narratives on refresh. Keep the existing support gate unchanged and preserve clear evidence-family metadata on the seeded documents.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py -k "default and expectations"</verify>
  <done>The default ACME analyze and refresh inputs satisfy the expectations evidence-family contract without any extra connector seed step.</done>
</task>

<task type="auto">
  <name>Task 2: Align default-path seeding and preserve one intentional unsupported regression</name>
  <files>tests/conftest.py, tests/test_analysis_flow.py, tests/test_run_lifecycle.py, scripts/generate_phase2_examples.py</files>
  <action>Remove the hidden dependency on bespoke supplemental connector ingestion from the default supported expectations path used by seeded ACME tests and the checked example generator. Update the unsupported expectations regression so it uses an intentionally limited fixture or seed path rather than the default ACME manifest that now represents the shipped expectations-ready path.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py -k "expectations and (supported or skip or refresh)"</verify>
  <done>Supported expectations tests and generated-example seeding now use the same default ACME contract, and one explicit unsupported-path regression still proves skip behavior intentionally.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the UAT scenario with default-path example and delta regressions</name>
  <files>tests/test_analysis_flow.py, tests/test_run_lifecycle.py, tests/test_generated_examples.py, scripts/generate_phase2_examples.py</files>
  <action>Add or update regressions so `expectations_rollout` succeeds on the untouched default ACME analyze path, succeeds again on the default refresh path after `acme_public_rerun`, and updates `what_changed_since_last_run` plus the expectations memo sections without any ad hoc connector preload. Confirm the checked artifact generator reflects the same contract so the repo examples cannot mask another default-path gap.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_generated_examples.py -k "expectations or delta or generated"</verify>
  <done>The diagnosed UAT gap is covered by regressions that exercise the same default ACME path used in broader analyze, refresh, and checked-example flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py -k "expectations"`
- [ ] `docker compose run --rm api pytest -q tests/test_run_lifecycle.py -k "expectations"`
- [ ] `docker compose run --rm api pytest -q tests/test_generated_examples.py -k "expectations or generated"`
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_generated_examples.py`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] Default ACME analyze under `expectations_rollout` marks `expectations_catalyst_realization` as `supported` without supplemental connector ingestion
- [ ] Default ACME refresh after `examples/acme_public_rerun` changes `expectations_variant_view` and `realization_path_catalysts`
- [ ] An explicit unsupported expectations regression still exists, but it no longer uses the default ACME fixture
- [ ] `config/panels.yaml` and `src/ai_investing/application/services.py` do not relax the current expectations evidence-family support contract
</verification>

<success_criteria>

- All tasks completed
- The default ACME fixture contract supports the existing expectations rollout
- Default analyze plus refresh regressions cover the exact UAT scenario
- Checked artifacts use the same default ACME seed contract as runtime tests
- The unsupported expectations path remains intentional and explicit

</success_criteria>

<output>
After completion, create `.planning/phases/06-productionize-remaining-panels/06-07-SUMMARY.md`
</output>

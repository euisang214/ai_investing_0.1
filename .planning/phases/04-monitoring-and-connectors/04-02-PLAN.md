---
phase: 04-monitoring-and-connectors
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - config/source_connectors.yaml
  - src/ai_investing/ingestion/file_connectors.py
  - src/ai_investing/ingestion/http_connectors.py
  - examples/connectors/acme_regulatory_packet/manifest.json
  - examples/connectors/acme_market_packet/manifest.json
  - examples/connectors/acme_consensus_packet/manifest.json
  - examples/connectors/acme_ownership_packet/manifest.json
  - examples/connectors/acme_events_packet/manifest.json
  - examples/connectors/acme_transcript_news_packet/manifest.json
  - examples/connectors/beta_dataroom/manifest.json
  - examples/connectors/beta_kpi_packet/manifest.json
  - tests/test_ingestion.py
  - tests/test_live_connector_runtime.py
  - tests/test_analysis_flow.py
  - docs/ingestion.md
  - docs/tool_registry.md
autonomous: false
requirements:
  - V2-02
requirement_scope: slice
requirement_slice_id: V2-02B
user_setup: []
must_haves:
  truths:
    - "Phase 4 still adds exactly one lightweight live public connector path, and the rest of the richer connector surface remains deterministic and fixture-backed."
    - "To keep `V2-02` traceability honest, the planned connector inventory now names thin-but-real slices for `regulatory`, `market`, `consensus`, `ownership`, and private `dataroom` systems; `events` and `transcript/news` remain supplemental public examples rather than substitutes for the required families."
    - "Representative private evidence still centers on `dataroom` plus `KPI packet` materials, so diligence support broadens without implying a fully productized private integration stack."
    - "PDFs and spreadsheets become first-class evidence when extraction is feasible, while HTML and image artifacts remain attachment-only by default in this phase."
    - "Packet-style sources emit one `EvidenceRecord` per meaningful item or document, not one giant packet record and not ultra-granular metric-by-metric rows."
    - "Raw landing zones stay flattened, but duplicate basenames are handled with deterministic stable renames or prefixes so provenance remains clear."
    - "The lightweight live connector's outputs are explicitly time-bounded and staleness-tagged so recurring public refresh semantics remain honest."
  artifacts:
    - path: config/source_connectors.yaml
      provides: Representative Phase 4 connector inventory that explicitly covers the required `V2-02` system families, marks supplemental public families, and names the single lightweight live public path
      min_lines: 40
    - path: src/ai_investing/ingestion/file_connectors.py
      provides: Normalization and artifact-handling logic for required and supplemental fixture families, including PDF and spreadsheet extraction plus attachment-only HTML or image defaults
      min_lines: 240
    - path: src/ai_investing/ingestion/http_connectors.py
      provides: Lightweight live public connector implementation behind the generalized connector seam
      min_lines: 90
    - path: examples/connectors/acme_regulatory_packet/manifest.json
      provides: Deterministic public regulatory-evidence fixture bundle
      min_lines: 8
    - path: examples/connectors/acme_market_packet/manifest.json
      provides: Deterministic public market-evidence fixture bundle
      min_lines: 8
    - path: examples/connectors/acme_consensus_packet/manifest.json
      provides: Deterministic public consensus-evidence fixture bundle
      min_lines: 8
    - path: examples/connectors/acme_ownership_packet/manifest.json
      provides: Deterministic public ownership-evidence fixture bundle
      min_lines: 8
    - path: examples/connectors/acme_events_packet/manifest.json
      provides: Deterministic supplemental public events-evidence fixture bundle with attachment coverage
      min_lines: 8
    - path: examples/connectors/acme_transcript_news_packet/manifest.json
      provides: Deterministic supplemental public transcript and news fixture bundle
      min_lines: 8
    - path: examples/connectors/beta_dataroom/manifest.json
      provides: Deterministic private dataroom fixture bundle
      min_lines: 8
    - path: examples/connectors/beta_kpi_packet/manifest.json
      provides: Deterministic private KPI-packet fixture bundle
      min_lines: 8
    - path: tests/test_ingestion.py
      provides: Contract coverage for regulatory, market, consensus, ownership, dataroom, duplicate filenames, binary attachments, and deterministic raw-artifact persistence
      min_lines: 240
    - path: tests/test_live_connector_runtime.py
      provides: Verification that the one lightweight live public connector works through a typed transport seam with deterministic doubles
      min_lines: 90
    - path: docs/ingestion.md
      provides: Operator-facing explanation of required-system coverage, supplemental public families, first-class versus attachment-only media, and the deliberate limits of the live connector surface
      min_lines: 85
  key_links:
    - from: config/source_connectors.yaml
      to: src/ai_investing/ingestion/file_connectors.py
      via: config-backed routing from required and supplemental connector ids to richer fixture normalization rules
      pattern: "connector|manifest|kind|company_type|regulatory|consensus|market|ownership|dataroom"
    - from: src/ai_investing/ingestion/file_connectors.py
      to: tests/test_ingestion.py
      via: regression lock on extracted evidence, attachment-only media, duplicate-name handling, and raw provenance across the required system families
      pattern: "EvidenceRecord|regulatory|market|consensus|ownership|dataroom|pdf|spreadsheet|attachment|raw"
    - from: src/ai_investing/ingestion/http_connectors.py
      to: tests/test_live_connector_runtime.py
      via: one lightweight live public path validated through transport doubles instead of brittle network tests
      pattern: "http|transport|live|staleness"
    - from: docs/ingestion.md
      to: docs/tool_registry.md
      via: explanation of how required-system coverage and supplemental public examples line up with the repo's existing tool surface
      pattern: "regulatory|market|consensus|ownership|dataroom|events|transcript|news|kpi"
---

<objective>
Broaden the sample connector surface with honest `V2-02` family coverage plus exactly one lightweight live public connector path.

Purpose: The generalized runtime seam from `04-01` is only valuable if the repo ships representative adapter examples, honest media-handling rules, and an explicit account of what Phase 4 does and does not cover. This plan keeps the context-led emphasis on market, ownership, and narrative public evidence, but it also adds the thin regulatory and consensus slices needed so `V2-02` traceability no longer over-claims.
Output: Deterministic connector bundles for the required `V2-02` families, supplemental public fixtures, one lightweight live public connector, deterministic ingestion regressions, and docs that explain what is first-class evidence versus attachment-only in Phase 4.
</objective>

<requirement_traceability>
Parent requirement: `V2-02`
Slice id: `V2-02B`
Slice acceptance: The repo contains deterministic connector adapters for regulatory, market, consensus, ownership, and dataroom systems, plus supplemental events/transcript-news examples; exactly one lightweight live public connector works through the generalized runtime; and docs plus tests state clearly which families are representative fixtures versus the single live path.
Deferred from parent requirement: Additional live implementations for regulatory, consensus, ownership, and dataroom beyond the single lightweight public connector remain out of scope for Phase 4, along with premium live vendors, broad HTML extraction, and image OCR or understanding.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
@/Users/ethanshin/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-and-connectors/04-CONTEXT.md
@.planning/phases/04-monitoring-and-connectors/04-RESEARCH.md
@.planning/phases/04-monitoring-and-connectors/04-VALIDATION.md
@config/source_connectors.yaml
@src/ai_investing/ingestion/file_connectors.py
@tests/test_ingestion.py
@tests/test_analysis_flow.py
@docs/ingestion.md
@docs/tool_registry.md
@examples/acme_public/manifest.json
@examples/beta_private/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the honest connector inventory and media-policy note before adapter work</name>
  <files>config/source_connectors.yaml, examples/connectors/acme_regulatory_packet/manifest.json, examples/connectors/acme_market_packet/manifest.json, examples/connectors/acme_consensus_packet/manifest.json, examples/connectors/acme_ownership_packet/manifest.json, examples/connectors/acme_events_packet/manifest.json, examples/connectors/acme_transcript_news_packet/manifest.json, examples/connectors/beta_dataroom/manifest.json, examples/connectors/beta_kpi_packet/manifest.json, docs/ingestion.md</files>
  <action>Update the connector inventory, fixture manifests, and ingestion docs so the required `V2-02` families are explicitly represented before deeper adapter work continues. Add thin deterministic bundles for regulatory and consensus, keep market and ownership as deeper public examples, retain events and transcript/news as supplemental public bundles, and keep dataroom plus KPI packet coverage on the private side. In `docs/ingestion.md`, spell out that Phase 4 intentionally ships exactly one lightweight live public connector, that PDFs and spreadsheets are first-class when feasible, and that HTML and image artifacts stay attachment-only by default.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; docs = Path('docs/ingestion.md').read_text(encoding='utf-8').lower(); cfg = Path('config/source_connectors.yaml').read_text(encoding='utf-8').lower(); required = ['regulatory', 'market', 'consensus', 'ownership', 'dataroom']; missing = [item for item in required if item not in docs or item not in cfg]; assert not missing, missing; assert 'lightweight live public connector' in docs; assert 'attachment-only' in docs; assert 'pdf' in docs and 'spreadsheet' in docs"</verify>
  <done>The repo's config, fixture inventory, and docs now name the required system families, the single-live-path boundary, and the evidence-media policy before implementation continues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Connector inventory and scope note updated in `config/source_connectors.yaml` and `docs/ingestion.md`.</what-built>
  <how-to-verify>Review `config/source_connectors.yaml` and `docs/ingestion.md` and confirm four things: `regulatory`, `market`, `consensus`, `ownership`, and private `dataroom` coverage are all explicit; Phase 4 still plans exactly one lightweight live public connector; `events` plus `transcript/news` are labeled supplemental rather than substitutes for the required families; and PDFs/spreadsheets are first-class when feasible while HTML/images remain attachment-only.</how-to-verify>
  <resume-signal>Type "approved" or describe the scope or media-policy issue that needs to change.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the single live public path and required public normalization coverage</name>
  <files>config/source_connectors.yaml, src/ai_investing/ingestion/file_connectors.py, src/ai_investing/ingestion/http_connectors.py, tests/test_ingestion.py, tests/test_live_connector_runtime.py</files>
  <action>Implement the chosen lightweight live public connector behind a typed transport seam and extend file-backed normalization so regulatory, market, consensus, ownership, events, and transcript/news bundles all emit structured evidence with provenance, factor tags, time periods, and quality or staleness metadata. Keep regulatory and consensus intentionally thin but real, preserve the current baseline public examples so Phase 2 generated artifacts do not drift accidentally, and do not reintroduce vendor-specific branching into service logic.</action>
  <verify>docker compose run --rm api pytest -q tests/test_ingestion.py tests/test_live_connector_runtime.py -k "public or connector or live or regulatory or consensus"</verify>
  <done>The repo contains the required-system public slices plus exactly one lightweight live public connector path through the generalized runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Lock private media handling, downstream compatibility, and truthful docs</name>
  <files>src/ai_investing/ingestion/file_connectors.py, tests/test_ingestion.py, tests/test_analysis_flow.py, docs/ingestion.md, docs/tool_registry.md</files>
  <action>Complete dataroom and KPI packet handling, keep PDFs and spreadsheets first-class where extraction is feasible, keep HTML and images attachment-only by default, and make raw-artifact collision handling deterministic. Update ingestion and tool-registry docs so they distinguish required-system coverage, supplemental public examples, and the single live path honestly, then add downstream regressions that prove regulatory, market, consensus, ownership, and dataroom fixtures still land as typed `EvidenceRecord` data compatible with analysis flows.</action>
  <verify>docker compose run --rm api pytest -q tests/test_ingestion.py tests/test_analysis_flow.py -k "ingest or evidence or dataroom or regulatory or consensus"</verify>
  <done>Private media policy, truthful `V2-02` traceability, and downstream evidence compatibility are locked by docs and regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_ingestion.py`
- [ ] `docker compose run --rm api pytest -q tests/test_live_connector_runtime.py`
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py -k "evidence or ingest"`
- [ ] `docker compose run --rm api ruff check src tests`
- [ ] Public connector coverage explicitly includes regulatory, market, consensus, and ownership as named families; private coverage explicitly includes dataroom; and any events/transcript-news bundles are documented as supplemental rather than substitutes
- [ ] Exactly one lightweight live public connector runs through the generalized runtime, and the docs record that broader multi-system live coverage remains deferred
- [ ] Private fixtures cover at least one dataroom family and one KPI packet family, PDFs and spreadsheets become first-class evidence when feasible, and HTML or image assets stay attachment-only by default
</verification>

<success_criteria>

- All tasks completed
- The repo ships deterministic connector coverage for regulatory, market, consensus, ownership, and dataroom, with events/transcript-news retained only as supplemental examples
- Exactly one lightweight live public connector proves the connector runtime can reach a real public source through a typed seam
- Docs and tests tell a truthful story about thin required-system slices versus deferred broader live coverage
- Every richer fixture still lands as typed, provenance-rich `EvidenceRecord` data
- Raw-artifact persistence remains deterministic even as evidence media variety grows

</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-and-connectors/04-02-SUMMARY.md`
</output>

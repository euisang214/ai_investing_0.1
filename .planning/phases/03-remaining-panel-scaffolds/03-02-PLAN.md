---
phase: 03-remaining-panel-scaffolds
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - config/factors.yaml
  - prompts/panels/supply_product_operations/placeholder.md
  - prompts/panels/market_structure_growth/placeholder.md
  - prompts/panels/macro_industry_transmission/placeholder.md
  - prompts/panels/management_governance/placeholder.md
  - prompts/panels/financial_quality/placeholder.md
  - prompts/panels/external_regulatory/placeholder.md
  - prompts/panels/expectations_catalyst_realization/placeholder.md
  - prompts/panels/security_overlay/placeholder.md
  - prompts/panels/portfolio_fit/placeholder.md
  - docs/prompting_strategy.md
  - tests/test_prompt_assets.py
autonomous: true
requirements:
  - V2-01
requirement_scope: prerequisite_slice
requirement_slice_id: V2-01B
user_setup: []
must_haves:
  truths:
    - "This plan delivers the scaffold-content prerequisite slice of `V2-01`; it prepares the remaining panels for later implementation without making them production-ready or runnable."
    - "No scaffold-only factor description remains as generic placeholder text; the ontology must describe the actual subject matter of each remaining panel."
    - "Every scaffold prompt uses one shared structure while still being panel-specific about purpose, memo impact, factor coverage, evidence expectations, and handoff guidance."
    - "Prompt specificity stays in Markdown and ontology meaning stays in YAML; Phase 3 must not move scaffold detail into Python source."
  artifacts:
    - path: config/factors.yaml
      provides: Panel-specific descriptions for every remaining scaffolded factor instead of generic placeholder text
      min_lines: 110
    - path: prompts/panels/external_regulatory/placeholder.md
      provides: Representative scaffold prompt structure that the remaining panel placeholder prompts mirror with panel-specific content
      min_lines: 20
    - path: docs/prompting_strategy.md
      provides: Documented scaffold prompt contract and maintenance rules for future panel expansion
      min_lines: 20
    - path: tests/test_prompt_assets.py
      provides: Deterministic checks for required scaffold prompt sections, non-generic factor descriptions, and prompt-to-config alignment for memo sections and factor coverage
      min_lines: 60
  key_links:
    - from: config/factors.yaml
      to: prompts/panels/external_regulatory/placeholder.md
      via: factor ontology that must match the panel-specific prompt coverage
      pattern: "factors|placeholder|PanelVerdict"
    - from: config/panels.yaml
      to: tests/test_prompt_assets.py
      via: memo-section and factor mappings that each scaffold prompt must mirror
      pattern: "memo_sections|factor_ids|prompt_path"
    - from: docs/prompting_strategy.md
      to: prompts/panels/portfolio_fit/placeholder.md
      via: shared scaffold prompt headings and handoff rules
      pattern: "scaffold|evidence|handoff"
---

<objective>
Replace the remaining generic scaffold content with panel-specific factor descriptions and prompt files that read like real implementation specs without making the panels executable yet.

Purpose: Phase 3 does not help future implementation if the remaining ontology and prompts stay vague. The scaffold needs to carry real domain meaning even while execution remains blocked.
Output: Panel-specific factor descriptions, panel-specific scaffold prompts, and tests/docs that lock the shared placeholder prompt contract as a prerequisite slice of `V2-01`.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01B`
Slice acceptance: Every scaffold-only factor description and placeholder prompt is panel-specific, structurally consistent, and aligned to configured memo sections and factor coverage without making the panels runnable.
Deferred from parent requirement: Real model-ready production prompts, specialist workflows, and executable panel implementation.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-panel-scaffolds/03-CONTEXT.md
@.planning/phases/03-remaining-panel-scaffolds/03-RESEARCH.md
@config/factors.yaml
@config/panels.yaml
@docs/factor_ontology.md
@docs/prompting_strategy.md
@prompts/panels/supply_product_operations/placeholder.md
@prompts/panels/market_structure_growth/placeholder.md
@prompts/panels/macro_industry_transmission/placeholder.md
@prompts/panels/management_governance/placeholder.md
@prompts/panels/financial_quality/placeholder.md
@prompts/panels/external_regulatory/placeholder.md
@prompts/panels/expectations_catalyst_realization/placeholder.md
@prompts/panels/security_overlay/placeholder.md
@prompts/panels/portfolio_fit/placeholder.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace generic scaffolded factor descriptions with panel-specific ontology text</name>
  <files>config/factors.yaml</files>
  <action>Rewrite the remaining scaffold-only factor descriptions so they describe each factor's actual analytical meaning instead of repeating `Placeholder factor`. Keep the descriptions concise, stable, and tied to the owning top-level panel. Do not rename factor IDs in this phase unless a name is truly broken, because the future extension path depends on stable IDs. Treat "panel-specific" as a contract: the descriptions should be meaningfully distinct, not generic replacements with the old placeholder text removed.</action>
  <verify>docker compose run --rm api python -c "import yaml; from pathlib import Path; factors_data = yaml.safe_load(Path('config/factors.yaml').read_text(encoding='utf-8')); factors = factors_data if isinstance(factors_data, list) else factors_data.get('factors', []); scaffolded = [factor for factor in factors if factor.get('panel_id') not in {'gatekeepers', 'demand_revenue_quality'}]; banned = {'placeholder factor', 'generic factor', 'factor to assess', 'panel factor'}; assert scaffolded;\nfor factor in scaffolded:\n    desc = ' '.join(str(factor.get('description', '')).lower().split());\n    tokens = [token for token in factor.get('id', '').lower().split('_') if len(token) >= 4];\n    assert desc and desc not in banned and len(desc) >= 24, factor['id'];\n    assert not tokens or any(token in desc for token in tokens), factor['id']"</verify>
  <done>The scaffolded factor ontology carries real panel meaning and no longer relies on generic placeholder descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite every scaffold prompt with a shared but panel-specific structure</name>
  <files>prompts/panels/supply_product_operations/placeholder.md, prompts/panels/market_structure_growth/placeholder.md, prompts/panels/macro_industry_transmission/placeholder.md, prompts/panels/management_governance/placeholder.md, prompts/panels/financial_quality/placeholder.md, prompts/panels/external_regulatory/placeholder.md, prompts/panels/expectations_catalyst_realization/placeholder.md, prompts/panels/security_overlay/placeholder.md, prompts/panels/portfolio_fit/placeholder.md</files>
  <action>Replace the current one-note placeholder prompt bodies with a shared scaffold structure that calls out panel purpose, scaffold-only status, output contract (`PanelVerdict`), affected memo sections, factor coverage, evidence and provenance expectations, and a future implementation handoff note. Customize each file with that panel's actual domain framing and memo footprint. Keep the prompts clearly scaffold-only and do not imply the panel is already runnable.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; prompt_paths = ['prompts/panels/supply_product_operations/placeholder.md', 'prompts/panels/market_structure_growth/placeholder.md', 'prompts/panels/macro_industry_transmission/placeholder.md', 'prompts/panels/management_governance/placeholder.md', 'prompts/panels/financial_quality/placeholder.md', 'prompts/panels/external_regulatory/placeholder.md', 'prompts/panels/expectations_catalyst_realization/placeholder.md', 'prompts/panels/security_overlay/placeholder.md', 'prompts/panels/portfolio_fit/placeholder.md']; required = ['panel purpose', 'scaffold', 'panelverdict', 'affected memo sections', 'factor coverage', 'evidence', 'handoff'];\nfor path in prompt_paths:\n    text = Path(path).read_text(encoding='utf-8').lower();\n    assert all(term in text for term in required), path"</verify>
  <done>All remaining scaffold prompt files follow the same contract while still reading as distinct panel briefs rather than copy-paste placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Document and test the scaffold prompt contract</name>
  <files>docs/prompting_strategy.md, tests/test_prompt_assets.py</files>
  <action>Extend the prompt strategy doc with the shared scaffold prompt headings and the maintenance rule that placeholder prompts stay on disk, remain schema-aware, and preserve panel-specific memo and factor context. Add deterministic tests that assert each scaffold prompt contains the required headings, that the prompt inventory remains complete for every scaffold-only panel, that each prompt's memo-section list and factor coverage match the owning panel's config entries in `config/panels.yaml` and `config/factors.yaml`, and that scaffold-only factor descriptions clear a panel-specificity heuristic instead of merely dropping the exact placeholder phrase.</action>
  <verify>docker compose run --rm api pytest -q tests/test_prompt_assets.py</verify>
  <done>The repo documents the scaffold prompt contract and has tests that fail if placeholder prompts drift back toward generic boilerplate or diverge from configured memo sections and factor coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_prompt_assets.py`
- [ ] `docker compose run --rm api pytest -q tests/test_prompt_assets.py -k factor`
- [ ] `docker compose run --rm api python -c "from pathlib import Path; text = Path('docs/prompting_strategy.md').read_text(encoding='utf-8'); assert 'memo sections' in text.lower() and 'factor coverage' in text.lower()"`
- [ ] Every scaffold prompt contains the shared structure, still states that the panel is scaffold-only, and matches configured memo sections and factor coverage
</verification>

<success_criteria>

- All tasks completed
- Remaining factor descriptions are specific and durable
- Placeholder prompts are panel-specific, schema-aware, and structurally consistent
- Prompt content stays aligned with configured memo sections and factor mappings
- Factor descriptions clear a deterministic panel-specificity check instead of merely removing the old placeholder string
- Prompt and factor scaffolding remain editable without touching runtime code

</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-panel-scaffolds/03-02-SUMMARY.md`
</output>

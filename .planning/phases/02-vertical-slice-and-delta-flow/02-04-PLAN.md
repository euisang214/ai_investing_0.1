---
phase: 02-vertical-slice-and-delta-flow
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - scripts/generate_phase2_examples.py
  - tests/conftest.py
  - tests/test_analysis_flow.py
  - tests/test_generated_examples.py
  - examples/generated/README.md
  - examples/generated/ACME/initial/result.json
  - examples/generated/ACME/initial/memo.md
  - examples/generated/ACME/initial/delta.json
  - examples/generated/ACME/continued/result.json
  - examples/generated/ACME/continued/memo.md
  - examples/generated/ACME/continued/delta.json
  - examples/generated/ACME/rerun/result.json
  - examples/generated/ACME/rerun/memo.md
  - examples/generated/ACME/rerun/delta.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TOOLS-02
user_setup: []
must_haves:
  truths:
    - "The fake-provider vertical slice is regression-tested for gatekeeper stop, explicit continue, failed-gate provisional continue, rerun delta generation, disabled coverage skip behavior, and persisted tool logs."
    - "Checked-in sample artifacts let another engineer inspect a paused initial run, a continued run, and a rerun delta without having to infer the workflow from code."
    - "Test fixtures keep timestamps and generated outputs deterministic enough that checkpoint and rerun artifacts can be regenerated reliably."
    - "The final Phase 2 verification set still covers config loading, registry wiring, and ingestion parsing so monitoring/config changes do not silently regress baseline guarantees."
  artifacts:
    - path: scripts/generate_phase2_examples.py
      provides: Reproducible sample-artifact generation for the paused, continued, and rerun flows
      min_lines: 70
    - path: tests/test_analysis_flow.py
      provides: End-to-end fake-provider regression coverage for the full Phase 2 vertical slice
      min_lines: 170
    - path: tests/test_generated_examples.py
      provides: Verification that checked-in example artifacts exist, parse, and match the expected checkpoint workflow
      min_lines: 60
    - path: examples/generated/ACME/continued/result.json
      provides: Inspectable full-run output after explicit continuation past the gatekeeper checkpoint
    - path: examples/generated/ACME/rerun/delta.json
      provides: Inspectable structured rerun delta artifact with materiality-aware change reporting
  key_links:
    - from: scripts/generate_phase2_examples.py
      to: src/ai_investing/application/services.py
      via: reproducible analyze, continue, and rerun artifact generation
      pattern: "analyze_company|continue_run|refresh_company"
    - from: tests/test_analysis_flow.py
      to: scripts/generate_phase2_examples.py
      via: locking down the same public workflow that generates the checked-in artifacts
      pattern: "continue_run|show_delta|tool_logs"
    - from: tests/test_generated_examples.py
      to: examples/generated/ACME/continued/result.json
      via: artifact-shape verification
---

<objective>
Lock down the Phase 2 slice with deterministic regression coverage and inspectable sample outputs so the architecture proof is visible to another engineer, not only executable in theory.

Purpose: Phase 2 is incomplete if the new workflow only exists in source code and ad hoc tests. The repo needs stable artifacts and a broad enough regression suite to protect pause, resume, memo history, delta generation, and tool logging.
Output: Deterministic fixtures, end-to-end tests, and checked-in sample outputs for paused, continued, and rerun states.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-CONTEXT.md
@.planning/phases/02-vertical-slice-and-delta-flow/02-RESEARCH.md
@tests/conftest.py
@tests/test_analysis_flow.py
@examples/generated/ACME/initial/result.json
@examples/generated/ACME/rerun/result.json
@examples/generated/ACME/rerun/delta.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize fixtures and sample generation for checkpointed runs</name>
  <files>tests/conftest.py, scripts/generate_phase2_examples.py</files>
  <action>Introduce deterministic fixture controls for timestamps, run IDs where needed, and sample evidence landing zones so paused-run and rerun outputs can be regenerated without random drift. Put the artifact-export flow behind a small repo-local script rather than relying on a handwritten sequence of commands. Keep the script thin over existing services and fake-provider fixtures; do not create a separate example-only runtime.</action>
  <verify>pytest -q tests/test_generated_examples.py -k generation</verify>
  <done>Sample artifact generation is reproducible enough that the checked-in outputs can be refreshed intentionally instead of by manual editing.</done>
</task>

<task type="auto">
  <name>Task 2: Expand end-to-end regression coverage for the full Phase 2 workflow</name>
  <files>tests/test_analysis_flow.py, tests/test_generated_examples.py</files>
  <action>Add end-to-end tests that prove the system can stop after gatekeepers, continue later, continue provisionally after a failed gatekeeper, emit rerun deltas with the new materiality rules, skip disabled coverage in due runs, and persist useful tool-log references for the executed run. Keep the assertions focused on observable behavior and stored artifacts rather than brittle internal implementation details.</action>
  <verify>pytest -q tests/test_analysis_flow.py tests/test_generated_examples.py</verify>
  <done>The critical Phase 2 behaviors are protected by regression tests that would fail if checkpointing, memo history, delta generation, or tool logging regressed.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the checked-in Phase 2 sample artifacts</name>
  <files>examples/generated/README.md, examples/generated/ACME/initial/result.json, examples/generated/ACME/initial/memo.md, examples/generated/ACME/initial/delta.json, examples/generated/ACME/continued/result.json, examples/generated/ACME/continued/memo.md, examples/generated/ACME/continued/delta.json, examples/generated/ACME/rerun/result.json, examples/generated/ACME/rerun/memo.md, examples/generated/ACME/rerun/delta.json</files>
  <action>Regenerate the checked-in ACME artifacts so they demonstrate the actual Phase 2 story: the initial gatekeeper checkpoint stop, the explicit continuation into the full vertical slice, and a rerun that emits a materiality-aware delta. Add a concise artifact README that tells another engineer what each directory represents and how to inspect the outputs. Keep the artifacts deterministic and consistent with the tests; do not hand-edit JSON in ways the generation path cannot reproduce.</action>
  <verify>pytest -q tests/test_generated_examples.py</verify>
  <done>The repo contains inspectable Phase 2 artifacts for paused, continued, and rerun flows, and they line up with the implemented checkpoint semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_run_lifecycle.py tests/test_monitoring_semantics.py tests/test_analysis_flow.py tests/test_cli.py tests/test_api.py tests/test_generated_examples.py tests/test_config_and_registry.py tests/test_ingestion.py`
- [ ] `ruff check src tests`
- [ ] `python scripts/generate_phase2_examples.py`
- [ ] Checked-in example artifacts match the generator output
</verification>

<success_criteria>

- All tasks completed
- End-to-end tests cover the core pause, continue, rerun, and tool-log behaviors of the vertical slice
- Sample outputs are inspectable and reproducible
- Another engineer can validate Phase 2 behavior without reverse-engineering the codebase

</success_criteria>

<output>
After completion, create `.planning/phases/02-vertical-slice-and-delta-flow/02-04-SUMMARY.md`
</output>

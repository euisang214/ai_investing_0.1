---
phase: 05-scheduling-and-notifications
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - README.md
  - docs/architecture.md
  - docs/runbook.md
  - n8n/README.md
  - n8n/weekly_watchlist_refresh.json
  - n8n/weekly_portfolio_refresh.json
  - n8n/new_evidence_webhook.json
  - n8n/refresh_notification.json
  - scripts/generate_phase2_examples.py
  - examples/generated/README.md
  - examples/generated/ACME/initial/result.json
  - examples/generated/ACME/initial/memo.md
  - examples/generated/ACME/initial/delta.json
  - examples/generated/ACME/continued/result.json
  - examples/generated/ACME/continued/memo.md
  - examples/generated/ACME/continued/delta.json
  - examples/generated/ACME/rerun/result.json
  - examples/generated/ACME/rerun/memo.md
  - examples/generated/ACME/rerun/delta.json
  - tests/test_generated_examples.py
autonomous: false
requirements:
  - V2-03
  - V2-05
requirement_scope: slice
requirement_slice_id: V2-03B-V2-05B
user_setup: []
must_haves:
  truths:
    - "n8n remains an external scheduling, webhook, and notification layer. Examples must call stable API or webhook boundaries and may not coordinate panel sequencing, memo updates, or provisional continuation."
    - "Docs and examples must describe the reconciled gatekeeper policy honestly: `pass` and `review` auto-continue, `fail` enters a review queue with immediate notification, and provisional downstream work stays operator-only."
    - "Operator docs must explain queue summary, per-job status, retry, cancel, force-run, and bulk manual enqueue for selected companies, watchlist, and portfolio scopes."
    - "Notification documentation must cover one shared operator channel, immediate alerts for failed gatekeepers, worker failures, and material changes, plus a daily digest that explicitly includes companies with no key changes."
    - "The refreshed notification automation example must target the stable notification-delivery surface introduced in `05-02-PLAN.md`; it may not imply direct database reads, worker-internal callbacks, or n8n-owned business logic."
    - "Checked generated examples must no longer encode the retired universal-pause story; they need to match the post-Phase-5 lifecycle so docs, tests, and sample artifacts stay truthful."
  artifacts:
    - path: README.md
      provides: Top-level operator guidance updated for cadence policies, queue-based execution, and the new gatekeeper lifecycle
      min_lines: 120
    - path: docs/architecture.md
      provides: Honest system-boundary documentation for queue execution, review queue behavior, and external automation limits
      min_lines: 260
    - path: docs/runbook.md
      provides: Operator runbook for enqueueing work, inspecting queue or review state, retrying or canceling jobs, and handling provisional overrides
      min_lines: 120
    - path: n8n/README.md
      provides: Boundary-focused explanation of how the example workflows interact with the API and notification surfaces
      min_lines: 30
    - path: n8n/weekly_watchlist_refresh.json
      provides: Example workflow that schedules watchlist enqueue calls rather than inline reasoning execution
      min_lines: 1
    - path: n8n/refresh_notification.json
      provides: Example notification workflow for immediate alerts or digest dispatch against the stable notification-delivery boundary
      min_lines: 1
    - path: scripts/generate_phase2_examples.py
      provides: Regenerated sample-artifact workflow aligned to the post-Phase-5 gatekeeper lifecycle
      min_lines: 150
    - path: examples/generated/README.md
      provides: Narrative explanation of the checked example artifacts after the runtime policy change
      min_lines: 30
    - path: tests/test_generated_examples.py
      provides: Regression lock that checked-in generated examples match the updated generator and lifecycle story
      min_lines: 110
  key_links:
    - from: README.md
      to: docs/runbook.md
      via: operator-facing explanation of cadence policies, queue submission, and review handling
      pattern: "queue|review|provisional|cadence"
    - from: docs/architecture.md
      to: n8n/README.md
      via: explicit statement that n8n stays outside the reasoning runtime and only calls stable service boundaries
      pattern: "n8n|worker|notification|webhook"
    - from: n8n/weekly_watchlist_refresh.json
      to: src/ai_investing/api/main.py
      via: example schedule submission against additive queue-aware endpoints
      pattern: "enqueue|coverage|watchlist"
    - from: n8n/refresh_notification.json
      to: src/ai_investing/api/main.py
      via: example notification delivery against the stable outbox or dispatch endpoint instead of internal coupling
      pattern: "notification|outbox|deliver|digest|review"
    - from: scripts/generate_phase2_examples.py
      to: examples/generated/README.md
      via: reproducible checked artifacts that explain the post-Phase-5 lifecycle rather than the retired checkpoint-only story
      pattern: "initial|continued|rerun|gatekeepers"
    - from: tests/test_generated_examples.py
      to: examples/generated/ACME/initial/result.json
      via: regression lock that checked artifacts stay synchronized with the updated generator and docs
      pattern: "status|gate_decision|delta"
---

<objective>
Refresh the external-automation examples, runbook, and checked narrative artifacts so Phase 5 is documented truthfully once cadence, queue, and notification work land.

Purpose: The repo already documents that n8n is external, but the examples and checked generated artifacts still tell the retired universal-pause story. This plan updates the docs, automation examples, and generated reference outputs so operators and downstream workflows can rely on the post-Phase-5 contract without reverse-engineering implementation details.
Output: Updated README and runbook guidance, refreshed n8n workflow examples, and checked generated artifacts that match the new gatekeeper and background-execution behavior.
</objective>

<requirement_traceability>
Parent requirements: `V2-03`, `V2-05`
Slice id: `V2-03B-V2-05B`
Slice acceptance: The repo docs, n8n examples, and checked generated artifacts all describe and demonstrate the implemented cadence, queue, review, and notification boundaries accurately, without implying that n8n owns reasoning or that automation can trigger provisional continuation.
Deferred from parent requirements: Additional notification destinations, per-segment routing rules, and any future frontend operational UX remain outside Phase 5.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
@/Users/ethanshin/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scheduling-and-notifications/05-CONTEXT.md
@.planning/phases/05-scheduling-and-notifications/05-RESEARCH.md
@.planning/phases/05-scheduling-and-notifications/05-VALIDATION.md
@.planning/phases/05-scheduling-and-notifications/05-01-PLAN.md
@.planning/phases/05-scheduling-and-notifications/05-02-PLAN.md
@README.md
@docs/architecture.md
@docs/runbook.md
@src/ai_investing/application/notifications.py
@src/ai_investing/api/main.py
@n8n/README.md
@n8n/weekly_watchlist_refresh.json
@n8n/weekly_portfolio_refresh.json
@n8n/new_evidence_webhook.json
@n8n/refresh_notification.json
@scripts/generate_phase2_examples.py
@examples/generated/README.md
@tests/test_generated_examples.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the operator docs and generated example story around the implemented Phase 5 lifecycle</name>
  <files>README.md, docs/architecture.md, docs/runbook.md, scripts/generate_phase2_examples.py, examples/generated/README.md, examples/generated/ACME/initial/result.json, examples/generated/ACME/initial/memo.md, examples/generated/ACME/initial/delta.json, examples/generated/ACME/continued/result.json, examples/generated/ACME/continued/memo.md, examples/generated/ACME/continued/delta.json, examples/generated/ACME/rerun/result.json, examples/generated/ACME/rerun/memo.md, examples/generated/ACME/rerun/delta.json, tests/test_generated_examples.py</files>
  <action>Update the repo-facing docs and checked generated artifacts so they describe the actual Phase 5 runtime instead of the retired universal-pause checkpoint story. The top-level docs should explain cadence policies, queue-aware execution, review-queue outcomes, additive operator controls, and the operator-only provisional path after failed gatekeepers. Rework the generated example script and checked artifacts so they remain reproducible but now illustrate the post-Phase-5 lifecycle, including the absence of a manual resume step for pass or review outcomes and the continued distinction between normal completion and explicit provisional follow-up after failures.</action>
  <verify>docker compose run --rm api python -c "from pathlib import Path; files = [Path('README.md'), Path('docs/architecture.md'), Path('docs/runbook.md')]; text = '\\n'.join(path.read_text(encoding='utf-8').lower() for path in files); required = ['worker', 'notification', 'checkpoint', 'cadence']; missing = [item for item in required if item not in text]; assert not missing, missing" && docker compose run --rm api pytest -q tests/test_generated_examples.py</verify>
  <done>Docs and checked generated artifacts now match the implemented Phase 5 lifecycle rather than a deprecated pause-and-resume default.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the n8n examples so they demonstrate honest external automation boundaries</name>
  <files>n8n/README.md, n8n/weekly_watchlist_refresh.json, n8n/weekly_portfolio_refresh.json, n8n/new_evidence_webhook.json, n8n/refresh_notification.json</files>
  <action>Rewrite the example n8n workflows so they call stable enqueue, webhook-intake, and notification surfaces rather than implying that n8n coordinates reasoning steps. The watchlist and portfolio examples should schedule background refresh submission, not run panel logic inline. The notification example should demonstrate immediate alert and daily digest delivery through the stable notification-delivery surface added in Phase 5, including the requirement that the daily digest explicitly includes companies with no key changes. The README should explain where n8n stops and the service begins, especially around queue execution, checkpoint state, notification-delivery boundaries, and the ban on automated provisional continuation.</action>
  <verify>docker compose run --rm api python -c "import json; from pathlib import Path; names = ['weekly_watchlist_refresh.json', 'weekly_portfolio_refresh.json', 'new_evidence_webhook.json', 'refresh_notification.json']; payloads = [json.loads((Path('n8n') / name).read_text(encoding='utf-8')) for name in names]; assert all('nodes' in payload and payload['nodes'] for payload in payloads); assert all('connections' in payload for payload in payloads); readme = Path('n8n/README.md').read_text(encoding='utf-8').lower(); required = ['n8n', 'webhook', 'notification', 'queue', 'provisional']; missing = [item for item in required if item not in readme]; assert not missing, missing"</verify>
  <done>The checked n8n examples and README show honest API and webhook integration boundaries for scheduling, ingest triggers, and notifications.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated operator docs, refreshed n8n workflows, and regenerated checked examples that describe the post-Phase-5 cadence, queue, review, and notification model.</what-built>
  <how-to-verify>Review `README.md`, `docs/architecture.md`, `docs/runbook.md`, `n8n/README.md`, and the refreshed `n8n/*.json` examples. Confirm that n8n is limited to scheduling, webhook intake, and notification delivery; the service owns queue execution, run state, and provisional override logic; `pass` and `review` auto-continue while `fail` enters a review queue and notifies immediately; the daily digest includes companies with no key changes; and no example suggests n8n or a worker can auto-trigger provisional continuation.</how-to-verify>
  <resume-signal>Type "approved" or describe the external-boundary issue that still needs to change.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api python -c "from pathlib import Path; files = [Path('README.md'), Path('docs/architecture.md'), Path('docs/runbook.md'), Path('n8n/README.md')]; text = '\\n'.join(path.read_text(encoding='utf-8').lower() for path in files); required = ['n8n', 'worker', 'notification', 'checkpoint', 'cadence']; missing = [item for item in required if item not in text]; assert not missing, missing"`
- [ ] `docker compose run --rm api python -c "import json; from pathlib import Path; names = ['weekly_watchlist_refresh.json', 'weekly_portfolio_refresh.json', 'new_evidence_webhook.json', 'refresh_notification.json']; payloads = [json.loads((Path('n8n') / name).read_text(encoding='utf-8')) for name in names]; assert all('nodes' in payload and payload['nodes'] for payload in payloads); assert all('connections' in payload for payload in payloads)"`
- [ ] `docker compose run --rm api pytest -q tests/test_generated_examples.py`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] Docs and examples explicitly distinguish n8n scheduling or notification behavior from the service-owned reasoning, queue execution, and provisional override boundaries
- [ ] The notification example and docs reference the stable delivery surface added in Phase 5 rather than database access or worker-internal hooks
- [ ] The checked generated example set no longer depends on the retired universal pause after `gatekeepers`
</verification>

<success_criteria>

- All tasks completed
- Docs accurately describe the implemented cadence, queue, review, and notification boundaries
- n8n examples call stable API or webhook surfaces and keep reasoning outside n8n
- Operators have a truthful runbook for bulk enqueue, queue inspection, review handling, and provisional overrides
- Checked generated examples and tests stay synchronized with the new Phase 5 lifecycle
- Final phase sign-off includes a blocking human boundary review, not just parsing and test passes

</success_criteria>

<output>
After completion, create `.planning/phases/05-scheduling-and-notifications/05-03-SUMMARY.md`
</output>

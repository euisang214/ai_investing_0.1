---
phase: 03-remaining-panel-scaffolds
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/ai_investing/application/services.py
  - tests/test_analysis_flow.py
  - tests/test_run_lifecycle.py
  - tests/test_cli.py
  - tests/test_api.py
autonomous: true
requirements:
  - V2-01
requirement_scope: prerequisite_slice
requirement_slice_id: V2-01C
user_setup: []
must_haves:
  truths:
    - "This plan delivers the execution-boundary prerequisite slice of `V2-01`; it preserves the non-runnable scaffold posture until later phases implement real panel flows."
    - "Future-facing policies and explicit panel requests may remain visible in config, but execution must still fail fast when they target unimplemented panels."
    - "Service, CLI, and API entrypoints all preserve the same non-runnable scaffold boundary instead of letting one interface bypass it."
    - "Phase 3 expands topology visibility without weakening the gatekeeper-first checkpoint or making scaffold-only panels runnable by accident."
  artifacts:
    - path: src/ai_investing/application/services.py
      provides: Conditional minimal rejection-path stabilization only if the new regressions expose a real defect in the existing runtime boundary
      min_lines: 420
    - path: tests/test_analysis_flow.py
      provides: End-to-end service-level coverage that future-facing policies remain loadable but blocked at execution time
      min_lines: 80
    - path: tests/test_run_lifecycle.py
      provides: Run-entrypoint coverage that scaffold-only panels cannot slip past the existing lifecycle boundary
      min_lines: 80
    - path: tests/test_cli.py
      provides: CLI contract coverage for scaffold-only panel rejection
      min_lines: 80
    - path: tests/test_api.py
      provides: API contract coverage for scaffold-only panel rejection
      min_lines: 140
  key_links:
    - from: src/ai_investing/application/services.py
      to: tests/test_analysis_flow.py
      via: explicit panel-resolution failures for unimplemented panels and future-facing policies
      pattern: "full_surface|implemented|run_panel"
    - from: tests/test_cli.py
      to: tests/test_api.py
      via: consistent user-facing rejection semantics across interfaces
      pattern: "not implemented|panel"
---

<objective>
Harden the execution boundary around scaffold-only panels so the repo can expose the full future surface in config without allowing those panels to run.

Purpose: The Phase 3 scaffold is only safe if the runtime continues to reject unimplemented panels consistently across service, CLI, and API flows.
Output: Direct regression coverage for non-runnable scaffold panels, with runtime changes allowed only if the new tests expose a real defect in the existing rejection boundary. This is a prerequisite slice of `V2-01`, not full panel productionization.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01C`
Slice acceptance: Service, CLI, and API entrypoints all continue rejecting scaffold-only panels and future-facing policies that include them, so Phase 3 expands config visibility without weakening runtime safety.
Deferred from parent requirement: Executable implementations for the remaining panels and any runtime support needed for their full production flows.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-panel-scaffolds/03-CONTEXT.md
@.planning/phases/03-remaining-panel-scaffolds/03-RESEARCH.md
@config/run_policies.yaml
@src/ai_investing/config/models.py
@src/ai_investing/application/services.py
@tests/test_analysis_flow.py
@tests/test_run_lifecycle.py
@tests/test_cli.py
@tests/test_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service-level regressions and stabilize the rejection path only if they expose a real gap</name>
  <files>src/ai_investing/application/services.py, tests/test_analysis_flow.py, tests/test_run_lifecycle.py</files>
  <action>Add direct service-level tests that prove future-facing policies such as `full_surface` can still load from config while execution rejects the unimplemented panel set. Cover explicit panel selection and any adjacent run entrypoint that might otherwise bypass the check. If those regressions expose a real defect or an unusably vague failure contract, make the smallest possible `services.py` change before verifying the task. Do not relax the guardrail, add new execution fallbacks, or introduce special handling for specific scaffolded panels. If the regressions pass without code changes, leave runtime code untouched.</action>
  <verify>docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py</verify>
  <done>Service-level execution paths fail clearly when scaffold-only panels are requested, without forcing config load failures, and any service-layer change happened only because the new regressions proved it was necessary.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CLI and API tests to expose the same scaffold boundary</name>
  <files>tests/test_cli.py, tests/test_api.py</files>
  <action>Extend the CLI and API suites so operators see the same rejection contract when they request an unimplemented panel or a policy that includes one. The tests should confirm that the interface does not silently filter scaffold-only panels away or proceed with partial execution. Keep the assertions aligned with actual user-facing payloads and messages rather than internal helper behavior.</action>
  <verify>docker compose run --rm api pytest -q tests/test_cli.py tests/test_api.py</verify>
  <done>Both interfaces expose the non-runnable scaffold boundary clearly and consistently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_analysis_flow.py tests/test_run_lifecycle.py tests/test_cli.py tests/test_api.py`
- [ ] `docker compose run --rm api ruff check src tests`
- [ ] `full_surface` remains loadable in config, but any attempt to execute it still fails before scaffold-only panels run
</verification>

<success_criteria>

- All tasks completed
- Non-runnable scaffold panels are enforced consistently across interfaces
- Future-facing policy visibility is preserved without weakening runtime safety
- No new orchestration branches are introduced for scaffolded panels

</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-panel-scaffolds/03-03-SUMMARY.md`
</output>

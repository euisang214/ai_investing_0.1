---
phase: 01-foundation-and-contracts
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - config/run_policies.yaml
  - src/ai_investing/application/services.py
  - src/ai_investing/graphs/company_refresh.py
  - src/ai_investing/graphs/subgraphs.py
  - src/ai_investing/graphs/state.py
  - tests/test_analysis_flow.py
autonomous: true
requirements:
  - ORCH-01
user_setup: []
must_haves:
  truths:
    - "Company refresh composition is driven by panel config and reusable subgraph builders instead of inline topology conditionals."
    - "Disabled, unimplemented, or unsafe panels and run-policy options cannot be executed accidentally."
    - "Panel outputs still flow into memo updates before monitoring diffs and final IC synthesis, and failed runs are persisted as failed rather than stranded in running."
    - "The initial gatekeeper and demand vertical slice continues to run end-to-end against the fake provider after orchestration hardening."
  artifacts:
    - path: src/ai_investing/graphs/company_refresh.py
      provides: Top-level company refresh graph composition
      min_lines: 40
    - path: src/ai_investing/application/services.py
      provides: Run lifecycle management and policy-aware panel selection
      min_lines: 250
    - path: src/ai_investing/graphs/subgraphs.py
      provides: Reusable panel, memo, monitoring, and IC subgraphs
      min_lines: 70
    - path: tests/test_analysis_flow.py
      provides: Regression coverage for graph composition and end-to-end fake-provider behavior
      min_lines: 80
  key_links:
    - from: src/ai_investing/graphs/company_refresh.py
      to: src/ai_investing/graphs/subgraphs.py
      via: subgraph-builder lookup and graph composition
      pattern: "build_.*subgraph|compile"
    - from: src/ai_investing/graphs/company_refresh.py
      to: src/ai_investing/application/services.py
      via: refresh runtime execution and memo/delta reconciliation
      pattern: "RefreshRuntime|update_memo_for_panel|compute_monitoring_delta|reconcile_ic_memo"
    - from: config/run_policies.yaml
      to: src/ai_investing/application/services.py
      via: panel selection and memo/monitoring safety flags
---

<objective>
Make the Phase 1 orchestration layer genuinely reusable and config-driven so later panels do not require core graph rewrites.

Purpose: The current vertical slice works, but inline subgraph branching and nested `.invoke()` wrappers will become a maintenance trap as soon as more panel families arrive.
Output: Config-driven graph composition, clearer subgraph boundaries, and stronger orchestration regression tests.
</objective>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-VALIDATION.md
@config/panels.yaml
@config/run_policies.yaml
@src/ai_investing/application/services.py
@src/ai_investing/graphs/company_refresh.py
@src/ai_investing/graphs/subgraphs.py
@src/ai_investing/graphs/state.py
@tests/test_analysis_flow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline panel branching with a subgraph-builder registry</name>
  <files>src/ai_investing/graphs/company_refresh.py, src/ai_investing/graphs/subgraphs.py</files>
  <action>Introduce a builder lookup keyed by `panel.subgraph` so the top-level refresh graph consumes declared subgraph types from config instead of encoding topology choices with inline `if/else` logic. Pair that with explicit panel-safety checks so disabled or unimplemented panels cannot slip into runnable selections. Keep the registry small and explicit, and do not hardcode future panel families into `company_refresh.py`.</action>
  <verify>pytest -q tests/test_analysis_flow.py</verify>
  <done>Adding a new supported subgraph family becomes a registry extension instead of a bespoke conditional rewrite, and unsafe panel selections fail clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Make graph execution policy-aware and failure-safe</name>
  <files>config/run_policies.yaml, src/ai_investing/application/services.py, src/ai_investing/graphs/company_refresh.py, src/ai_investing/graphs/subgraphs.py, src/ai_investing/graphs/state.py</files>
  <action>Refactor the refresh path so memo reconciliation and monitoring execution honor run-policy flags, panel/memo/monitoring/IC subgraphs are composed explicitly rather than hidden inside opaque `.invoke()` wrappers where LangGraph can no longer reason about them, and graph failures persist `RunStatus.FAILED` with useful metadata instead of leaving runs stranded in `running`. Preserve existing memo and delta semantics while clarifying graph structure.</action>
  <verify>pytest -q tests/test_analysis_flow.py</verify>
  <done>The refresh graph remains sequential in the right places, policy flags are honored, and run lifecycle state is correct on both success and failure.</done>
</task>

<task type="auto">
  <name>Task 3: Expand orchestration regression coverage for the hardened graph</name>
  <files>tests/test_analysis_flow.py</files>
  <action>Add regression tests that prove the config-driven builder lookup works, memo updates still occur after each panel, and monitoring plus final memo synthesis still run in the correct order for the fake-provider vertical slice. Keep the tests focused on observable runtime behavior, not brittle implementation trivia.</action>
  <verify>pytest -q tests/test_analysis_flow.py</verify>
  <done>Graph hardening is locked down by tests that would fail if topology slipped back into hidden bespoke orchestration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_analysis_flow.py`
- [ ] `ruff check src tests`
- [ ] End-to-end fake-provider analysis still produces panel outputs, memo, and delta
</verification>

<success_criteria>

- All tasks completed
- Orchestration topology is driven by config-declared subgraph types
- Memo updates, monitoring diffs, and IC synthesis still connect in the intended order
- The vertical slice remains green after graph hardening

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-04-SUMMARY.md`
</output>

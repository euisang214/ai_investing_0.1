---
phase: 04-monitoring-and-connectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_investing/config/models.py
  - src/ai_investing/config/loader.py
  - src/ai_investing/ingestion/base.py
  - src/ai_investing/ingestion/registry.py
  - src/ai_investing/application/services.py
  - tests/test_config_and_registry.py
  - tests/test_connector_runtime.py
autonomous: true
requirements:
  - V2-02
requirement_scope: slice
requirement_slice_id: V2-02A
user_setup: []
must_haves:
  truths:
    - "Connector dispatch no longer depends on hardcoded public-versus-private branches inside `IngestionService`; runtime resolves connectors through registry-backed config."
    - "The current `public_file_connector` and `private_file_connector` remain valid defaults, so existing Phase 2 and Phase 3 entrypoints keep working without caller rewrites."
    - "Connector config becomes expressive enough to declare additive settings, live-refresh posture, and extraction or attachment policies without breaking the current file-bundle YAML shape."
    - "Typed normalization still ends at `CompanyProfile` plus `EvidenceRecord`; Phase 4 must not move connector-specific payloads into graph orchestration or memo-writing paths."
  artifacts:
    - path: src/ai_investing/config/models.py
      provides: Backward-compatible connector config schema that can express current file bundles plus additive connector settings and evidence-handling policy
      min_lines: 150
    - path: src/ai_investing/config/loader.py
      provides: Registry validation that rejects unknown connector kinds and malformed connector settings before runtime
      min_lines: 130
    - path: src/ai_investing/ingestion/base.py
      provides: Stable source-connector contract plus typed request or context objects for config-driven dispatch
      min_lines: 30
    - path: src/ai_investing/ingestion/registry.py
      provides: Factory or registry layer that maps config-defined connector ids to concrete implementations
      min_lines: 70
    - path: src/ai_investing/application/services.py
      provides: Ingestion service path that resolves connector ids through the registry while preserving public and private convenience wrappers
      min_lines: 430
    - path: tests/test_connector_runtime.py
      provides: Runtime regressions for default connector fallback, explicit connector selection, and invalid-config failure cases
      min_lines: 90
  key_links:
    - from: src/ai_investing/config/models.py
      to: src/ai_investing/config/loader.py
      via: typed connector config and pre-runtime validation for additive settings
      pattern: "SourceConnectorConfig|kind|settings|capabilities"
    - from: src/ai_investing/ingestion/registry.py
      to: src/ai_investing/application/services.py
      via: service-level connector resolution instead of hardcoded branching
      pattern: "connector_id|resolve|ingest"
    - from: tests/test_connector_runtime.py
      to: src/ai_investing/application/services.py
      via: regression lock on backward-compatible defaults and explicit connector-id routing
      pattern: "public_file_connector|private_file_connector|connector_id"
---

<objective>
Establish the reusable connector runtime seam before Phase 4 broadens the adapter surface.

Purpose: The repo already advertises configurable connectors, but ingestion still hardcodes the file-bundle defaults. This plan turns connector dispatch into a config-driven runtime so later public, private, and lightweight live adapters can land without widening orchestration logic.
Output: Typed connector config and validation, a registry or factory for connector resolution, and service regressions that preserve current public and private defaults.
</objective>

<requirement_traceability>
Parent requirement: `V2-02`
Slice id: `V2-02A`
Slice acceptance: Connector ids resolve through a registry-backed ingestion seam, existing file-bundle defaults remain valid, and malformed connector definitions fail before execution.
Deferred from parent requirement: Representative public and private adapter expansion, media-specific evidence handling, and connector-facing docs updates are handled in `04-02-PLAN.md`.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-and-connectors/04-CONTEXT.md
@.planning/phases/04-monitoring-and-connectors/04-RESEARCH.md
@.planning/phases/04-monitoring-and-connectors/04-VALIDATION.md
@config/source_connectors.yaml
@src/ai_investing/config/models.py
@src/ai_investing/config/loader.py
@src/ai_investing/ingestion/base.py
@src/ai_investing/ingestion/file_connectors.py
@src/ai_investing/application/services.py
@tests/test_config_and_registry.py
@tests/test_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand connector config and validation without breaking the current YAML</name>
  <files>src/ai_investing/config/models.py, src/ai_investing/config/loader.py, tests/test_config_and_registry.py</files>
  <action>Refactor the source-connector config model so current file-bundle fields remain accepted while additive connector settings can be represented explicitly. The model should be able to express connector kind, optional live-refresh metadata, and evidence-handling policy without forcing all connectors into the same `manifest_file` plus `raw_landing_zone` shape. Keep the existing YAML valid, add validation for supported connector kinds and required settings, and fail malformed definitions at config-load time rather than later inside the service layer.</action>
  <verify>docker compose run --rm api pytest -q tests/test_config_and_registry.py -k "connector or registry"</verify>
  <done>Connector config can represent more than the current hardcoded file-bundle case, and registry loading rejects unsupported or malformed connector definitions before runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Add a connector registry and route ingestion through it</name>
  <files>src/ai_investing/ingestion/base.py, src/ai_investing/ingestion/registry.py, src/ai_investing/application/services.py</files>
  <action>Create a reusable registry or factory in `src/ai_investing/ingestion/` that maps configured connector ids to concrete connector implementations. Extend the ingestion contract with a typed request or context object if needed, but keep `ingest_public_data()` and `ingest_private_data()` backward compatible through compatibility wrappers or sensible defaults. Update `IngestionService` so it resolves connectors by id instead of branching directly on company type and instantiating `FileBundleConnector` inline. Keep this change service-scoped; do not widen LangGraph orchestration for ingestion in this phase.</action>
  <verify>docker compose run --rm api pytest -q tests/test_connector_runtime.py tests/test_ingestion.py -k "connector or runtime or ingest"</verify>
  <done>The ingestion runtime resolves connectors through a reusable seam, while existing public and private entrypoints continue to work for callers that do not specify a connector id.</done>
</task>

<task type="auto">
  <name>Task 3: Lock backward compatibility and failure behavior with targeted regressions</name>
  <files>tests/test_connector_runtime.py, tests/test_config_and_registry.py</files>
  <action>Add focused regressions for three compatibility cases: default public and private connector fallback, explicit connector-id selection, and clear failure behavior for unknown connector ids or invalid connector settings. Keep the assertions at the registry and service boundaries so future connector additions cannot silently reintroduce hardcoded runtime logic. Do not add live-network assumptions in this plan.</action>
  <verify>docker compose run --rm api pytest -q tests/test_config_and_registry.py tests/test_connector_runtime.py</verify>
  <done>The repo has a narrow regression suite that would fail if connector dispatch reverts to hardcoded service logic or config validation becomes a silent no-op.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_config_and_registry.py tests/test_connector_runtime.py`
- [ ] `docker compose run --rm api pytest -q tests/test_ingestion.py -k "public or private or manifest"`
- [ ] `docker compose run --rm api pytest -q && docker compose run --rm api ruff check src tests`
- [ ] IngestionService still supports the existing public and private convenience entrypoints without callers constructing connector classes directly
- [ ] Unknown connector ids and malformed connector settings fail during validation or service resolution, not halfway through ingestion
</verification>

<success_criteria>

- All tasks completed
- Connector runtime no longer depends on hardcoded service branches for connector resolution
- Current file-bundle connectors stay backward compatible
- Config validation makes future connector additions explicit and observable
- The phase preserves structured evidence normalization and keeps ingestion outside graph orchestration

</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-and-connectors/04-01-SUMMARY.md`
</output>

agents:
- id: gatekeeper_advocate
  name: Gatekeeper Advocate
  panel_id: gatekeepers
  parent_id: null
  role_type: specialist
  goal: Build the base-case gatekeeper claim set from evidence.
  enabled: true
  prompt_path: prompts/gatekeepers/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: gatekeeper_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - gatekeepers
  - advocate
- id: gatekeeper_skeptic
  name: Gatekeeper Skeptic
  panel_id: gatekeepers
  parent_id: gatekeeper_advocate
  role_type: skeptic
  goal: Pressure-test gatekeeper claims and surface disconfirming evidence.
  enabled: true
  prompt_path: prompts/gatekeepers/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: gatekeeper_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - gatekeepers
  - skeptic
- id: gatekeeper_durability
  name: Gatekeeper Durability
  panel_id: gatekeepers
  parent_id: gatekeeper_advocate
  role_type: durability
  goal: Evaluate whether the gatekeeper thesis persists through stress and time.
  enabled: true
  prompt_path: prompts/gatekeepers/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: gatekeeper_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - gatekeepers
  - durability
- id: gatekeeper_judge
  name: Gatekeeper Judge
  panel_id: gatekeepers
  parent_id: gatekeeper_advocate
  role_type: judge
  goal: Consolidate gatekeeper claims into a single panel verdict.
  enabled: true
  prompt_path: prompts/gatekeepers/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: GatekeeperVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: gatekeeper_research
  model_profile: quality
  scope: both
  tags:
  - vertical_slice
  - gatekeepers
  - judge
- id: gatekeeper_panel_lead
  name: Gatekeeper Panel Lead
  panel_id: gatekeepers
  parent_id: gatekeeper_judge
  role_type: lead
  goal: Decide whether the company clears gatekeeping for deeper memo work.
  enabled: true
  prompt_path: prompts/gatekeepers/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: GatekeeperVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - vertical_slice
  - gatekeepers
  - lead
- id: demand_advocate
  name: Demand Advocate
  panel_id: demand_revenue_quality
  parent_id: null
  role_type: specialist
  goal: Build the base-case demand and revenue quality thesis.
  enabled: true
  prompt_path: prompts/panels/demand_revenue_quality/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: demand_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - demand
  - advocate
- id: demand_skeptic
  name: Demand Skeptic
  panel_id: demand_revenue_quality
  parent_id: demand_advocate
  role_type: skeptic
  goal: Stress-test demand quality claims and find disconfirming evidence.
  enabled: true
  prompt_path: prompts/panels/demand_revenue_quality/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: demand_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - demand
  - skeptic
- id: demand_durability
  name: Demand Durability
  panel_id: demand_revenue_quality
  parent_id: demand_advocate
  role_type: durability
  goal: Evaluate the time durability of the demand thesis.
  enabled: true
  prompt_path: prompts/panels/demand_revenue_quality/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: demand_research
  model_profile: balanced
  scope: both
  tags:
  - vertical_slice
  - demand
  - durability
- id: demand_judge
  name: Demand Judge
  panel_id: demand_revenue_quality
  parent_id: demand_advocate
  role_type: judge
  goal: Consolidate demand and revenue quality claims into a verdict.
  enabled: true
  prompt_path: prompts/panels/demand_revenue_quality/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: demand_research
  model_profile: quality
  scope: both
  tags:
  - vertical_slice
  - demand
  - judge
- id: demand_panel_lead
  name: Demand Panel Lead
  panel_id: demand_revenue_quality
  parent_id: demand_judge
  role_type: lead
  goal: Translate the demand verdict into memo-relevant judgment.
  enabled: true
  prompt_path: prompts/panels/demand_revenue_quality/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - vertical_slice
  - demand
  - lead
- id: memo_section_updater
  name: Memo Section Updater
  panel_id: memo_updates
  parent_id: null
  role_type: synthesizer
  goal: Update only the memo sections affected by completed panel verdicts.
  enabled: true
  prompt_path: prompts/memo_updates/section_update.md
  input_channels:
  - panel_verdicts
  - prior_memo
  output_schema: MemoSectionUpdate
  memory_read_namespaces:
  - company/{company_id}/memos/current
  - company/{company_id}/verdicts/{panel_id}
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/memos/current
  - company/{company_id}/memos/history
  allowed_tool_bundle: memo_updater
  model_profile: balanced
  scope: both
  tags:
  - memo
  - vertical_slice
- id: ic_synthesizer
  name: IC Synthesizer
  panel_id: ic
  parent_id: null
  role_type: synthesizer
  goal: Reconcile the final memo snapshot and overall recommendation.
  enabled: true
  prompt_path: prompts/ic/synthesizer.md
  input_channels:
  - panel_verdicts
  - memo_sections
  - monitoring_delta
  output_schema: ICMemo
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  - company/{company_id}/memos/current
  - company/{company_id}/monitoring
  memory_write_namespaces:
  - company/{company_id}/memos/current
  - company/{company_id}/memos/history
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - ic
  - vertical_slice
- id: monitoring_diff_synthesizer
  name: Monitoring Diff Synthesizer
  panel_id: monitoring
  parent_id: null
  role_type: synthesizer
  goal: Compare the current run with the prior active run and summarize thesis drift.
  enabled: true
  prompt_path: prompts/monitoring/delta.md
  input_channels:
  - claims
  - memo_sections
  - prior_run
  output_schema: MonitoringDelta
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/history
  - company/{company_id}/monitoring
  memory_write_namespaces:
  - company/{company_id}/monitoring
  allowed_tool_bundle: ic_judge
  model_profile: balanced
  scope: both
  tags:
  - monitoring
  - vertical_slice
- id: supply_advocate
  name: Supply Product Operations Advocate
  panel_id: supply_product_operations
  parent_id: null
  role_type: specialist
  goal: Build the base-case supply, product, and operating resilience thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/supply_product_operations/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: supply_ops_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - supply
  - advocate
- id: supply_skeptic
  name: Supply Product Operations Skeptic
  panel_id: supply_product_operations
  parent_id: supply_advocate
  role_type: skeptic
  goal: Pressure-test supply-side claims and surface fragility in vendors, throughput, and product execution.
  enabled: true
  prompt_path: prompts/panels/supply_product_operations/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: supply_ops_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - supply
  - skeptic
- id: supply_durability
  name: Supply Product Operations Durability
  panel_id: supply_product_operations
  parent_id: supply_advocate
  role_type: durability
  goal: Test whether supply, product, and execution advantages persist through stress and scale changes.
  enabled: true
  prompt_path: prompts/panels/supply_product_operations/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: supply_ops_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - supply
  - durability
- id: supply_judge
  name: Supply Product Operations Judge
  panel_id: supply_product_operations
  parent_id: supply_advocate
  role_type: judge
  goal: Consolidate supply, product, and operations claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/supply_product_operations/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: supply_ops_research
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - supply
  - judge
- id: supply_panel_lead
  name: Supply Product Operations Panel Lead
  panel_id: supply_product_operations
  parent_id: supply_judge
  role_type: lead
  goal: Translate the supply-side verdict into memo-ready durability and risk judgment.
  enabled: true
  prompt_path: prompts/panels/supply_product_operations/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - supply
  - lead
- id: market_growth_advocate
  name: Market Structure Growth Advocate
  panel_id: market_structure_growth
  parent_id: null
  role_type: specialist
  goal: Build the base-case market structure and growth thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/market_structure_growth/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: market_growth_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - market
  - advocate
- id: market_growth_skeptic
  name: Market Structure Growth Skeptic
  panel_id: market_structure_growth
  parent_id: market_growth_advocate
  role_type: skeptic
  goal: Pressure-test share gains, market sizing, and runway claims against contradictory evidence.
  enabled: true
  prompt_path: prompts/panels/market_structure_growth/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: market_growth_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - market
  - skeptic
- id: market_growth_durability
  name: Market Structure Growth Durability
  panel_id: market_structure_growth
  parent_id: market_growth_advocate
  role_type: durability
  goal: Test whether growth drivers and share gains persist through cycle shifts and competitive response.
  enabled: true
  prompt_path: prompts/panels/market_structure_growth/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: market_growth_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - market
  - durability
- id: market_growth_judge
  name: Market Structure Growth Judge
  panel_id: market_structure_growth
  parent_id: market_growth_advocate
  role_type: judge
  goal: Consolidate market structure and growth claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/market_structure_growth/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: market_growth_research
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - market
  - judge
- id: market_growth_panel_lead
  name: Market Structure Growth Panel Lead
  panel_id: market_structure_growth
  parent_id: market_growth_judge
  role_type: lead
  goal: Translate the market structure verdict into memo-ready growth language without implying expectations work ran.
  enabled: true
  prompt_path: prompts/panels/market_structure_growth/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - market
  - lead
- id: macro_transmission_advocate
  name: Macro Industry Transmission Advocate
  panel_id: macro_industry_transmission
  parent_id: null
  role_type: specialist
  goal: Build the base-case macro transmission thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/macro_industry_transmission/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: macro_transmission_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - macro
  - advocate
- id: macro_transmission_skeptic
  name: Macro Industry Transmission Skeptic
  panel_id: macro_industry_transmission
  parent_id: macro_transmission_advocate
  role_type: skeptic
  goal: Stress-test macro and industry transmission claims for weak causal links or overgeneralized cycle calls.
  enabled: true
  prompt_path: prompts/panels/macro_industry_transmission/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: macro_transmission_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - macro
  - skeptic
- id: macro_transmission_durability
  name: Macro Industry Transmission Durability
  panel_id: macro_industry_transmission
  parent_id: macro_transmission_advocate
  role_type: durability
  goal: Evaluate whether macro transmission paths remain durable across policy, commodity, and credit shifts.
  enabled: true
  prompt_path: prompts/panels/macro_industry_transmission/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: macro_transmission_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - macro
  - durability
- id: macro_transmission_judge
  name: Macro Industry Transmission Judge
  panel_id: macro_industry_transmission
  parent_id: macro_transmission_advocate
  role_type: judge
  goal: Consolidate macro transmission claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/macro_industry_transmission/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: macro_transmission_research
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - macro
  - judge
- id: macro_transmission_panel_lead
  name: Macro Industry Transmission Panel Lead
  panel_id: macro_industry_transmission
  parent_id: macro_transmission_judge
  role_type: lead
  goal: Translate the macro transmission verdict into memo-ready risk language without implying overlay work ran.
  enabled: true
  prompt_path: prompts/panels/macro_industry_transmission/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - macro
  - lead
- id: management_advocate
  name: Management Governance Capital Allocation Advocate
  panel_id: management_governance_capital_allocation
  parent_id: null
  role_type: specialist
  goal: Build the base-case management, governance, and capital allocation thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/management_governance/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: management_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - management
  - advocate
- id: management_skeptic
  name: Management Governance Capital Allocation Skeptic
  panel_id: management_governance_capital_allocation
  parent_id: management_advocate
  role_type: skeptic
  goal: Stress-test leadership credibility, governance quality, and capital allocation discipline.
  enabled: true
  prompt_path: prompts/panels/management_governance/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: management_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - management
  - skeptic
- id: management_durability
  name: Management Governance Capital Allocation Durability
  panel_id: management_governance_capital_allocation
  parent_id: management_advocate
  role_type: durability
  goal: Evaluate whether stewardship quality and governance protections persist through pressure and time.
  enabled: true
  prompt_path: prompts/panels/management_governance/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: management_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - management
  - durability
- id: management_judge
  name: Management Governance Capital Allocation Judge
  panel_id: management_governance_capital_allocation
  parent_id: management_advocate
  role_type: judge
  goal: Consolidate management, governance, and capital allocation claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/management_governance/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: management_research
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - management
  - judge
- id: management_panel_lead
  name: Management Governance Capital Allocation Panel Lead
  panel_id: management_governance_capital_allocation
  parent_id: management_judge
  role_type: lead
  goal: Translate the stewardship verdict into memo-ready durability and risk judgment.
  enabled: true
  prompt_path: prompts/panels/management_governance/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - management
  - lead
- id: financial_advocate
  name: Financial Quality Liquidity Economic Model Advocate
  panel_id: financial_quality_liquidity_economic_model
  parent_id: null
  role_type: specialist
  goal: Build the base-case financial quality, liquidity, and economic model thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/financial_quality/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: financial_quality_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - financial
  - advocate
- id: financial_skeptic
  name: Financial Quality Liquidity Economic Model Skeptic
  panel_id: financial_quality_liquidity_economic_model
  parent_id: financial_advocate
  role_type: skeptic
  goal: Stress-test accounting quality, liquidity resilience, and reinvestment claims.
  enabled: true
  prompt_path: prompts/panels/financial_quality/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: financial_quality_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - financial
  - skeptic
- id: financial_durability
  name: Financial Quality Liquidity Economic Model Durability
  panel_id: financial_quality_liquidity_economic_model
  parent_id: financial_advocate
  role_type: durability
  goal: Test whether cash generation, liquidity, and reinvestment quality persist through stress.
  enabled: true
  prompt_path: prompts/panels/financial_quality/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: financial_quality_research
  model_profile: balanced
  scope: both
  tags:
  - wave_1
  - financial
  - durability
- id: financial_judge
  name: Financial Quality Liquidity Economic Model Judge
  panel_id: financial_quality_liquidity_economic_model
  parent_id: financial_advocate
  role_type: judge
  goal: Consolidate financial quality, liquidity, and economic model claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/financial_quality/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: financial_quality_research
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - financial
  - judge
- id: financial_panel_lead
  name: Financial Quality Liquidity Economic Model Panel Lead
  panel_id: financial_quality_liquidity_economic_model
  parent_id: financial_judge
  role_type: lead
  goal: Translate the financial quality verdict into memo-ready economic spread, valuation, and risk judgment.
  enabled: true
  prompt_path: prompts/panels/financial_quality/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_1
  - financial
  - lead
- id: external_regulatory_advocate
  name: External Regulatory Geopolitical Advocate
  panel_id: external_regulatory_geopolitical
  parent_id: null
  role_type: specialist
  goal: Build the base-case regulatory and geopolitical risk thesis from evidence.
  enabled: true
  prompt_path: prompts/panels/external_regulatory/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/memos/current
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: regulatory_context_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - regulatory
  - advocate
- id: external_regulatory_skeptic
  name: External Regulatory Geopolitical Skeptic
  panel_id: external_regulatory_geopolitical
  parent_id: external_regulatory_advocate
  role_type: skeptic
  goal: Stress-test whether policy, legal, or geopolitical risks are more severe than the base case assumes.
  enabled: true
  prompt_path: prompts/panels/external_regulatory/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: regulatory_context_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - regulatory
  - skeptic
- id: external_regulatory_durability
  name: External Regulatory Geopolitical Durability
  panel_id: external_regulatory_geopolitical
  parent_id: external_regulatory_advocate
  role_type: durability
  goal: Test whether licenses, government counterparties, and geopolitical exposures remain manageable through shocks.
  enabled: true
  prompt_path: prompts/panels/external_regulatory/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims/{factor_id}
  memory_write_namespaces:
  - company/{company_id}/claims/{factor_id}
  allowed_tool_bundle: regulatory_context_research
  model_profile: balanced
  scope: both
  tags:
  - wave_2
  - regulatory
  - durability
- id: external_regulatory_judge
  name: External Regulatory Geopolitical Judge
  panel_id: external_regulatory_geopolitical
  parent_id: external_regulatory_advocate
  role_type: judge
  goal: Consolidate regulatory and geopolitical claims into one panel verdict.
  enabled: true
  prompt_path: prompts/panels/external_regulatory/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{factor_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: regulatory_context_research
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - regulatory
  - judge
- id: external_regulatory_panel_lead
  name: External Regulatory Geopolitical Panel Lead
  panel_id: external_regulatory_geopolitical
  parent_id: external_regulatory_judge
  role_type: lead
  goal: Translate the external regulatory verdict into memo-ready risk language without implying security overlay conclusions.
  enabled: true
  prompt_path: prompts/panels/external_regulatory/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_2
  - regulatory
  - lead
- id: expectations_catalyst_realization_advocate
  name: Expectations Catalyst Realization Advocate
  panel_id: expectations_catalyst_realization
  parent_id: null
  role_type: specialist
  goal: Build the base-case expectations and catalyst path from explicit consensus, milestone, and event evidence.
  enabled: true
  prompt_path: prompts/panels/expectations_catalyst_realization/advocate.md
  input_channels:
  - evidence
  - prior_claims
  - prior_memo
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims
  - company/{company_id}/memos
  memory_write_namespaces:
  - company/{company_id}/claims/{panel_id}
  allowed_tool_bundle: expectations_realization_research
  model_profile: quality
  scope: both
  tags:
  - wave_3
  - expectations
  - advocate
- id: expectations_catalyst_realization_skeptic
  name: Expectations Catalyst Realization Skeptic
  panel_id: expectations_catalyst_realization
  parent_id: expectations_catalyst_realization_advocate
  role_type: skeptic
  goal: Pressure-test the variant view, timeline, and catalyst chain against what is already priced or unsupported.
  enabled: true
  prompt_path: prompts/panels/expectations_catalyst_realization/skeptic.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims
  memory_write_namespaces:
  - company/{company_id}/claims/{panel_id}
  allowed_tool_bundle: expectations_realization_research
  model_profile: quality
  scope: both
  tags:
  - wave_3
  - expectations
  - skeptic
- id: expectations_catalyst_realization_durability
  name: Expectations Catalyst Realization Durability
  panel_id: expectations_catalyst_realization
  parent_id: expectations_catalyst_realization_advocate
  role_type: durability
  goal: Test whether milestone sequencing, kill criteria, and catalyst dependencies are durable enough to support the thesis.
  enabled: true
  prompt_path: prompts/panels/expectations_catalyst_realization/durability.md
  input_channels:
  - evidence
  - prior_claims
  output_schema: ClaimCard
  memory_read_namespaces:
  - company/{company_id}/evidence
  - company/{company_id}/claims
  memory_write_namespaces:
  - company/{company_id}/claims/{panel_id}
  allowed_tool_bundle: expectations_realization_research
  model_profile: quality
  scope: both
  tags:
  - wave_3
  - expectations
  - durability
- id: expectations_catalyst_realization_judge
  name: Expectations Catalyst Realization Judge
  panel_id: expectations_catalyst_realization
  parent_id: expectations_catalyst_realization_advocate
  role_type: judge
  goal: Reconcile the expectations debate into a bounded panel verdict covering variant view and realization path.
  enabled: true
  prompt_path: prompts/panels/expectations_catalyst_realization/judge.md
  input_channels:
  - claims
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/claims/{panel_id}
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: expectations_realization_research
  model_profile: quality
  scope: both
  tags:
  - wave_3
  - expectations
  - judge
- id: expectations_catalyst_realization_lead
  name: Expectations Catalyst Realization Lead
  panel_id: expectations_catalyst_realization
  parent_id: null
  role_type: lead
  goal: Translate the expectations verdict into memo-ready variant-view and catalyst language without implying overlay conclusions.
  enabled: true
  prompt_path: prompts/panels/expectations_catalyst_realization/panel_lead.md
  input_channels:
  - panel_verdict
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: ic_judge
  model_profile: quality
  scope: both
  tags:
  - wave_3
  - expectations
  - lead
- id: security_or_deal_overlay_placeholder_lead
  name: Security Or Deal Overlay Placeholder Lead
  panel_id: security_or_deal_overlay
  parent_id: null
  role_type: lead
  goal: Scaffold-only lead placeholder until the panel is implemented.
  enabled: false
  prompt_path: prompts/panels/security_overlay/placeholder.md
  input_channels:
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: demand_research
  model_profile: budget
  scope: both
  tags:
  - placeholder
  - scaffold_only
- id: portfolio_fit_positioning_placeholder_lead
  name: Portfolio Fit Positioning Placeholder Lead
  panel_id: portfolio_fit_positioning
  parent_id: null
  role_type: lead
  goal: Scaffold-only lead placeholder until the panel is implemented.
  enabled: false
  prompt_path: prompts/panels/portfolio_fit/placeholder.md
  input_channels:
  - evidence
  output_schema: PanelVerdict
  memory_read_namespaces:
  - company/{company_id}/evidence
  memory_write_namespaces:
  - company/{company_id}/verdicts/{panel_id}
  allowed_tool_bundle: demand_research
  model_profile: budget
  scope: both
  tags:
  - placeholder
  - scaffold_only

---
phase: 03-remaining-panel-scaffolds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agents.yaml
  - config/panels.yaml
  - config/factors.yaml
  - tests/test_config_and_registry.py
autonomous: true
requirements:
  - V2-01
requirement_scope: prerequisite_slice
requirement_slice_id: V2-01A
user_setup: []
must_haves:
  truths:
    - "This plan delivers the scaffold-enablement prerequisite slice of `V2-01`; runnable specialist trees and full panel productionization are deferred beyond Phase 3."
    - "Every scaffold-only top-level panel remains `implemented: false` and has exactly one disabled placeholder lead agent so the intended cohort topology is fully visible in config."
    - "Every scaffold-only panel has non-empty factor mappings, and those factor ids resolve back to factors owned by that same panel."
    - "Every scaffold-only panel's `panel.prompt_path` matches the corresponding disabled placeholder lead agent's `prompt_path`, so scaffold assets do not drift between panel and agent registries."
    - "Phase 3 expands the scaffold surface through registries only; it must not add bespoke orchestration branches, new subgraph types, or runtime-only panel wiring."
    - "Panel metadata, prompt paths, factor mappings, and memo-section bindings stay internally consistent so future implementation can happen by config and prompt changes instead of runtime rewrites."
  artifacts:
    - path: config/agents.yaml
      provides: One normalized disabled placeholder lead agent for each remaining scaffold-only top-level panel
      min_lines: 360
    - path: config/panels.yaml
      provides: Stable scaffold panel registry entries with aligned prompt, factor, and memo-section metadata
      min_lines: 140
    - path: config/factors.yaml
      provides: Stable factor ids that each scaffold-only panel now maps to explicitly through non-empty factor lists
      min_lines: 110
    - path: tests/test_config_and_registry.py
      provides: Registry assertions that every unimplemented panel has a single disabled lead placeholder, matching panel and agent prompt paths, valid prompt references, and non-empty panel-owned factor mappings
      min_lines: 120
  key_links:
    - from: config/panels.yaml
      to: config/agents.yaml
      via: panel ids and prompt paths that define the scaffold surface without runtime branching
      pattern: "implemented|prompt_path|panel_id"
    - from: config/agents.yaml
      to: tests/test_config_and_registry.py
      via: placeholder-agent normalization and uniqueness checks for scaffold-only panels
      pattern: "role_type: lead|enabled: false|placeholder"
---

<objective>
Normalize the remaining scaffold panel registries so the full future panel topology is present, inspectable, and config-driven without making the new panels runnable.

Purpose: Phase 3 cannot succeed if the repo only partially reflects the future panel surface or if adding the remaining placeholder leads would require runtime rewrites later.
Output: Complete scaffold-only panel and agent registry coverage, plus tests that lock the topology contract as a prerequisite slice of `V2-01` rather than claiming the remaining panels are productionized.
</objective>

<requirement_traceability>
Parent requirement: `V2-01`
Slice id: `V2-01A`
Slice acceptance: All scaffold-only top-level panels have explicit config-backed topology, non-empty owned factor mappings, matching panel and placeholder-agent prompt paths, and exactly one disabled placeholder lead while remaining non-runnable.
Deferred from parent requirement: Full specialist trees, judges, real execution prompts, and production-ready panel flows.
</requirement_traceability>

<execution_context>
@/Users/ethanshin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ethanshin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-panel-scaffolds/03-CONTEXT.md
@.planning/phases/03-remaining-panel-scaffolds/03-RESEARCH.md
@config/panels.yaml
@config/agents.yaml
@config/factors.yaml
@src/ai_investing/application/context.py
@src/ai_investing/application/services.py
@tests/test_config_and_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing scaffold-only placeholder lead agents</name>
  <files>config/agents.yaml</files>
  <action>Add one disabled lead placeholder agent for each scaffold-only top-level panel that is still missing one. Normalize IDs, names, tags, prompt paths, model profile defaults, tool bundle defaults, input channels, output schema, and memory namespace shape against the existing scaffold pattern already used for the two placeholder-backed panels. Keep every new agent disabled and clearly scaffold-only. Do not add specialists, skeptics, durability roles, or judges for these panels in Phase 3.</action>
  <verify>docker compose run --rm api python -c "import yaml; from pathlib import Path; agents_data = yaml.safe_load(Path('config/agents.yaml').read_text(encoding='utf-8')); panels_data = yaml.safe_load(Path('config/panels.yaml').read_text(encoding='utf-8')); agents = agents_data if isinstance(agents_data, list) else agents_data.get('agents', []); panels = panels_data if isinstance(panels_data, list) else panels_data.get('panels', []); scaffold_ids = [p['id'] for p in panels if not p.get('implemented', False)]; placeholder_counts = {panel_id: 0 for panel_id in scaffold_ids}; [placeholder_counts.__setitem__(agent['panel_id'], placeholder_counts[agent['panel_id']] + 1) for agent in agents if agent.get('panel_id') in placeholder_counts and agent.get('role_type') == 'lead' and agent.get('enabled') is False]; assert placeholder_counts and all(count == 1 for count in placeholder_counts.values()), placeholder_counts"</verify>
  <done>All remaining scaffold-only panels have exactly one disabled placeholder lead agent and no deeper tree has been added prematurely.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile panel registry metadata with the normalized scaffold shape</name>
  <files>config/panels.yaml, config/factors.yaml</files>
  <action>Review the scaffold-only panel entries and tighten any metadata drift that would make later productionization awkward. Prompt paths, factor lists, memo-section mappings, scope, display names, and `implemented: false` posture must stay consistent with the placeholder lead agents, `config/factors.yaml`, and the Phase 3 context. Add explicit `factor_ids` for each scaffold-only panel if they are still missing, and make sure each mapped factor belongs to the owning panel. Keep future-facing policies loadable by leaving the panel registry complete; do not introduce startup validation that rejects scaffold-only panels just because they are present in config.</action>
  <verify>docker compose run --rm api python -c "import yaml; from pathlib import Path; panels_data = yaml.safe_load(Path('config/panels.yaml').read_text(encoding='utf-8')); factors_data = yaml.safe_load(Path('config/factors.yaml').read_text(encoding='utf-8')); panels = panels_data if isinstance(panels_data, list) else panels_data.get('panels', []); factors = factors_data if isinstance(factors_data, list) else factors_data.get('factors', []); factor_owner = {factor['id']: factor['panel_id'] for factor in factors}; scaffold_panels = [panel for panel in panels if not panel.get('implemented', False)]; assert scaffold_panels; [(_ for _ in ()).throw(AssertionError(panel['id'])) for panel in scaffold_panels if not panel.get('factor_ids') or not all(factor_owner.get(factor_id) == panel['id'] for factor_id in panel.get('factor_ids', []))]"</verify>
  <done>The panel registry cleanly describes the full scaffold surface, explicitly maps each scaffold-only panel to its owned factors, and still treats the remaining panels as non-runnable.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the scaffold topology contract in registry tests</name>
  <files>tests/test_config_and_registry.py</files>
  <action>Add focused registry tests that assert every unimplemented top-level panel has exactly one disabled lead placeholder agent, that the panel's `prompt_path` matches that agent's `prompt_path`, that the referenced scaffold prompt path exists, that every scaffold-only panel has non-empty `factor_ids`, that those factor ids belong to that panel in `config/factors.yaml`, and that the loader can still materialize the full registry without runtime special casing. Keep the tests at the config-contract level; they should prove the extension surface is declarative rather than exercising full analysis execution.</action>
  <verify>docker compose run --rm api pytest -q tests/test_config_and_registry.py</verify>
  <done>The scaffold topology, prompt-path consistency, and panel-to-factor mappings are protected by deterministic registry tests instead of relying on manual inspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm api pytest -q tests/test_config_and_registry.py`
- [ ] `docker compose run --rm api ruff check tests`
- [ ] Registry tests prove all scaffold-only panels remain `implemented: false`, each has one disabled placeholder lead agent, each panel's `prompt_path` matches its placeholder lead `prompt_path`, and each has non-empty panel-owned `factor_ids`
</verification>

<success_criteria>

- All tasks completed
- The intended top-level panel surface is fully present in config
- Placeholder lead agents are normalized and unique per scaffold-only panel
- Scaffold-only panels have explicit factor mappings rather than implicit or empty panel contracts
- Panel and placeholder-agent prompt paths stay aligned for every scaffold-only panel
- Runtime topology remains config-driven and unchanged in principle

</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-panel-scaffolds/03-01-SUMMARY.md`
</output>
